# AI와 함께 이 프로젝트를 진행한 방식 기록

> 무엇을, 어떤 순서로, 어떻게 요구해서 여기까지 왔는지에 대한 기록.
> 목적 두 가지 — ① 나중에 "이 프로젝트를 어떻게 진행했나" 를 설명할 수 있게
> ② 다음 프로젝트에서 같은 방식을 다시 쓸 수 있게.

---

## 0. 대전제 — 역할을 먼저 나눴다 (2026-08-13 합의)

코드를 어디까지 AI 에게 맡길지를 **작업이 시작되기 전에** 정해뒀다. (`CLAUDE.md`)

| 구간 | 방식 | 이유 |
|---|---|---|
| React 화면 퍼블리싱 | **AI 가 직접 파일 작성** | 이미 아는 영역이라 타이핑으로 배울 게 없음 |
| 데이터 연결 (fetch / Zustand / 라우터) | 내가 직접 | |
| Express 서버 / SQL / 디바이스 파이썬 | **AI 는 설명만, 내가 타이핑** | 처음 배우는 영역이라 손으로 쳐야 남음 |

**이 선이 없으면 AI 가 전부 해버리고 나는 읽기만 하게 된다.**
"어디는 맡기고 어디는 내가 한다" 를 먼저 못박은 것이 이 프로젝트에서 가장 중요한 결정이었다.

---

## 1. 진행 순서 (시간순)

### ① 기획 문서부터 만들었다 — `spec.md`, `deployment-managed.md`

코드를 한 줄도 쓰기 전에 문서부터 만들었다. 내용은:

- 무엇을 만들고 **무엇을 안 만들지** (MVP 컷)
- 용어와 상태 정의 (`dose` 의 5단계, "약 설정" vs "복약 건")
- 화면 7장 명세 + 화면 이동 지도
- 프론트 작업 규칙 (퍼블리싱 / 데이터연결 경계선 3개)
- 테이블·API 초안
- **개발 순서 9단계**
- "나중에" 목록 — 좋아 보이지만 지금 안 할 것들
- **결정 기록** — 왜 그렇게 정했는지

**요구한 것:** "이 기능을 만들 건데 명세를 같이 정리하자."
**내가 판단한 것:** 무엇을 빼고 무엇을 넣을지, 왜 그렇게 하는지.
**AI 가 한 것:** 그 판단을 문서 형태로 정리, 놓친 부분 질문.

### ② 개발 환경 세팅

```
npm create vite@latest web -- --template react
npm install antd-mobile
```

막힌 것 → PowerShell 실행 정책, 린터 선택(Oxlint vs ESLint).
**요구한 것:** "이 오류가 왜 나는지" / "둘 중 뭘 골라야 하는지."
답만 받지 않고 **왜 그런지** 를 같이 물었다.

### ③ 화면 7장 퍼블리싱 — 묶음 A~E 로 쪼갬 (2026-08-13 ~ 14)

한 번에 7장을 다 요구하지 않고 **5개 묶음으로 쪼개서** 하나씩 확인하며 진행했다.

| 묶음 | 내용 |
|---|---|
| A | 템플릿 청소 + 공용 기반(목업·상태 색/아이콘·시간 포맷) + 화면 1 |
| B | 화면 2 + 하단 탭 |
| C | 화면 3 + 화면 5 (둘 다 입력 폼이라 묶음) |
| D | 화면 4 + 공용 팝업 |
| E | 화면 6 + 화면 7 |

**각 묶음이 끝날 때마다:**
- 브라우저에서 직접 확인
- 모르는 개념이 나오면 그 자리에서 질문 (아래 2장)
- `Memo.md` 에 폴더 구조와 파일 설명을 갱신하게 시킴

### ④ 진행 중 계속 던진 질문들

구현을 받기만 하지 않고, 나온 개념을 그때그때 물었다.

- CSS Modules 가 실제로 뭘 해주나
- `Picker` 는 왜 `trigger` / `ref` 를 따로 써야 하나 → **내 이해가 맞는지 검토를 요청**
- `detailDose` 상태가 정확히 뭘 담는 건가
- 여러 화면이 공유하는 상태는 Zustand 아닌가 → **AI 설명의 기준이 틀렸음을 지적하고 정정받음**

### ⑤ 인프라 방향 검토 (2026-08-14)

바로 다음 단계로 가지 않고, **다른 길이 있는지 먼저 확인**했다.

- Supabase 에 테이블을 만드는 게 실습했던 MySQL 과 뭐가 다른가
- Docker 로 내 PC 에서 3개 컨테이너를 띄우면 안 되나 (홈서버)
- 그러면 개발 기간이 얼마나 늘어나나 → **1~2주**
- CGNAT 가 뭔가

**결론:** 원래 계획(Vercel + Render + Supabase) 유지.
이유 — 늘어나는 1~2주가 전부 인프라 작업이고 **기능은 하나도 안 늘어난다.**
지금 짜는 코드는 나중에 홈서버로 옮겨도 그대로 쓰인다.

### ⑥ API 명세 확정 (`spec.md` §9 의 3단계)

화면을 다 만든 뒤 "이 화면엔 이 데이터가 필요한데 API 에 없네" 를 찾는 단계.
**초안에서 10가지가 바뀌었다** (`spec.md` §8.6).

여기서 **선택을 했다** — 내가 직접 초안을 잡을지(A), AI 가 쓸지(B).
→ **B 를 고르되, 별도로 연습용 학습지(`api-worksheet.md`) 를 만들어 달라고 했다.**

```
① api-worksheet.md 를 먼저 채운다  (빈칸 학습지, 답 없음)
② 그 다음 spec.md §8 을 연다        (확정본 = 답지)
③ 차이 나는 부분을 본다
```

공통 규칙(0번 절)은 **대화로 하나씩 물어가며 직접 결정**했다 —
시각 형식 / id 형식 / 인증 헤더 / 응답 최상위 모양 / 오류 모양 / 상태 코드.
AI 에게는 "선택지와 판단 기준" 만 요구하고 **고르는 건 내가 했다.**

---

## 2. 반복해서 쓴 요구 패턴 (다음에도 재사용)

### ① "한 번에 하나씩, 확인하고 다음"

한꺼번에 쏟아지면 검토를 못 한다. 검토를 못 하면 내 코드가 아니게 된다.
→ 묶음 A~E, 워크시트 절 단위로 쪼갠 이유.

### ② "왜 그렇게 했는지 이유도 같이"

결과만 받으면 다음에 혼자 못 한다.
→ 모든 설명에 **일상 비유**를 요구했다. (택배 상자, 옷장, 엘리베이터, 아파트 대표번호…)

### ③ "결정은 문서에 남겨라"

`Memo.md` / `spec.md` / 이 파일.
대화는 사라지지만 문서는 남는다. 새 대화를 시작해도 맥락이 이어진다.
→ "앞으로 새 파일을 만들면 `Memo.md` 에도 설명과 함께 추가해줘" 를 **규칙으로 못박았다.**

### ④ "내가 이해한 게 맞는지 검토해줘"

설명을 듣고 **내 말로 다시 정리해서 되물었다.**
→ 반쯤 맞고 반쯤 틀린 부분이 그때 드러난다. (`Picker` 의 `trigger`, 시각 형식 등)

### ⑤ "답 말고 학습지"

AI 가 답을 먼저 말해버리면 연습할 기회가 사라진다.
→ `api-worksheet.md` 는 **답이 하나도 없는 빈칸 파일**로 요구했다.

### ⑥ "다른 길은 없나"

시키는 대로만 가지 않고, 갈림길에서 **비용을 물었다.**
→ Docker / 홈서버 / `pg` vs `supabase-js`.
결과적으로 원래 길을 유지했지만, **왜 그 길인지 알고 가는 것**과 모르고 가는 건 다르다.

### ⑦ "초안과 달라지면 문서를 고쳐라"

구현하다 보면 명세가 안 맞는 곳이 나온다.
→ `DatePicker` → `Picker`, `Checkbox.Group` → `Selector`, 놓침 아이콘 분리.
전부 `spec.md` 와 `Memo.md` 에 **이유와 함께** 기록했다.

---

### ⑧ "선택지를 주고 내가 고르게 해라" (2026-08-20 추가)

빈칸을 처음부터 스스로 채우는 건 무리였다. 대신 이렇게 바꿨다.

```
AI 가 할 것  : 후보 2~4개 + 각각의 장단점 + 추천
내가 할 것   : 고르기. 그리고 왜 골랐는지 말하기
```

**답을 받는 것과 다르다.** 후보를 비교하는 과정에서 "왜 이게 나은가" 가 남는다.
0번 절(공통 규칙)을 이 방식으로 채웠고, 1~9번도 같은 방식으로 진행한다.

### ⑨ 원칙이 쌓이면 나머지는 "적용" 으로 채운다 (2026-08-23)

워크시트 0~4번(공통 규칙, 화면 2·3·4·5)을 대화로 하나씩 정하는 데 며칠이 걸렸다.
그 과정에서 **전역 원칙 4개**가 쌓였고, 그 시점부터는 남은 결정의 대부분이
**새 판단이 아니라 원칙의 적용**이 되었다.

그래서 5~9번(화면 6·7·팝업·디바이스·접근제어)은 AI 가 원칙대로 채우고,
**진짜로 다른 것 하나(디바이스 이벤트의 대상 특정)만 따로 설명을 듣고 넘어갔다.**

```
판단 기준 : "이게 이전과 결이 같은가, 아니면 새로운 종류의 결정인가"
  같다  → 원칙을 적용해 채우고 한 줄 이유만 읽는다
  다르다 → 멈추고 같이 본다
```

**시간을 어디에 쓸지도 결정이다.** API 설계는 백엔드의 한 조각이고,
덩어리가 큰 것은 4·5·7·8단계(DB·서버·인증·디바이스)다.
3단계에 며칠을 더 쓰는 것보다 거기서 직접 타이핑하며 배우는 편이 남는다.

---

## 앞으로 계속 지킬 요청 (AI 가 잊지 말 것)

1. **워크시트가 다 끝나면 PDF 로 정리해서 줄 것** — md 파일은 읽기 불편함
2. **이 파일(`withAi.md`)에 계속 기록할 것** — 새 단계, 새 요구 패턴이 생길 때마다
3. **선택지를 주고 내가 고르게 할 것** — 답을 먼저 말하지 말 것

---

## 3. 지금까지 만들어진 문서들

| 파일 | 역할 |
|---|---|
| `CLAUDE.md` | 새 대화가 시작돼도 AI 가 읽는 인수인계서. 역할 분담·주의사항 |
| `spec.md` | **모든 결정의 기준.** 화면·상태·API·개발 순서·결정 기록 |
| `deployment-managed.md` | 배포 구성 (Vercel + Render + Supabase + FCM) |
| `Memo.md` | 폴더 구조와 파일별 설명, 묶음 진행표 |
| `api-worksheet.md` | API 설계 연습용 학습지 |
| `withAi.md` | 이 파일 |

---

## 4. 남은 단계 (`spec.md` §9)

| 단계 | 내용 | 담당 | 상태 |
|---|---|---|---|
| 1 | 명세 확정 | — | 완료 |
| 2 | 화면 7장 퍼블리싱 | AI 퍼블 | 완료 |
| 3 | API 응답 모양 고정 | 함께 | **완료** — 워크시트 전부 채움. 앱용 8 + 디바이스용 2 = API 10개 |
| 4 | DB 테이블 설계 + Supabase 생성 | **내가 타이핑** | |
| 5 | 인증 없이 서버 API 구현 | **내가 타이핑** | |
| 6 | 화면에 데이터 연결 (Zustand·라우터·fetch) | 내가 직접 | |
| 7 | 인증 붙이기 (Supabase Auth + 접근 제어) | **내가 타이핑** | |
| 8 | 디바이스 — 음성 → IR → 카메라 | **내가 타이핑** | |
| 9 | Render / Vercel 배포 | 함께 | |

**4단계부터는 내가 타이핑하는 구간이 대부분이다.**
AI 에게 요구할 것은 "코드" 가 아니라 **"개념 설명 + 내가 쓴 것 검토"** 로 바뀐다.
