# API 설계 워크시트 (연습용)

> **사용법**
> 1. 이 파일을 **먼저** 채운다
> 2. 다 채운 뒤에 `spec.md` §8을 연다
> 3. 다르게 쓴 부분을 찾는다 — **그 차이가 배울 지점이다**
>
> ⚠️ 채우기 전에 `spec.md` §8을 열지 말 것. 열면 이 연습이 의미가 없어진다.
> (§8은 이미 확정본으로 다시 쓰여 있다. 여기 답이 다 들어 있다)
>
> 정답이 하나인 문제가 아니다. **다르게 썼다고 틀린 게 아니라, 왜 그렇게 썼는지 설명할 수 있으면 맞는 것**이다.
>
> **진행 상황: 전부 완료 (2026-08-23)**
> 0~4번(공통·화면 2·3·4·5)은 대화로 하나씩 정했고,
> 5~9번(화면 6·7·팝업·디바이스·접근제어)은 그때까지 정한 **전역 원칙 4개**를 적용해 채웠다.

---

## 시작 전에 — 화면 한 장에 던질 질문 4개

**① 이 화면이 처음 뜰 때, 서버에 몇 번 물어봐야 하나?**
요청 횟수 = 화면이 뜨는 속도. 2번이면 화면이 두 번 나눠 뜬다. 한 번으로 합칠 수 있나?

**② 화면에 보이는 글자 하나하나가 어디서 오나?**
손가락으로 짚어가며 확인한다. **출처를 못 대는 글자가 있으면 API에 빠진 것이다.**
(힌트: `spec.md` §7의 테이블 6개에 그 값이 실제로 들어 있는지 확인해 볼 것)

**③ 버튼을 누르면 무엇이 바뀌고, 화면을 갱신하려면 응답으로 뭘 받아야 하나?**
"저장은 됐는데 화면이 안 바뀐다"가 여기서 생긴다.

**④ 데이터가 0개일 때와 아주 많을 때는?**
0개 → 빈 화면은 뭘 보여주나 / 많을 때 → 다 내려주면 안 되지 않나?

---

## 참고 자료 — 답이 아니라 재료

| 볼 것 | 어디 |
|---|---|
| **각 화면이 요구하는 데이터** | 각 페이지 파일 **맨 위 `@param` 주석** |
| 화면 명세 | `spec.md` §5 |
| 상태·시각 규칙 | `spec.md` §3 |
| 저장할 테이블 초안 | `spec.md` §7 |
| 목업 데이터 | `web/src/mocks/data.js` |

**페이지 파일 위치**

```
web/src/pages/LoginPage.jsx             화면 1
web/src/pages/DeviceListPage.jsx        화면 2
web/src/pages/DeviceRegisterPage.jsx    화면 3
web/src/pages/DeviceDetailPage.jsx      화면 4
web/src/pages/MedicationPage.jsx        화면 5
web/src/pages/DoseHistoryPage.jsx       화면 6
web/src/pages/NotificationListPage.jsx  화면 7
web/src/components/DoseDetailDialog.jsx 공용 팝업
```

---

## 0. 공통 규칙 — ✅ 정함 (2026-08-19)

> 이 절은 대화로 함께 정했다. 아래는 **결정 + 어떤 선택지가 있었는지 + 왜 그걸 골랐는지**.

### 결정 요약

```
시각 형식        : UTC 기준 ISO 8601 문자열   "2026-08-13T23:00:00Z"
                   KST 변환은 화면이 담당
id 형식          : DB 는 UUID / JSON 으로는 문자열
인증 헤더 (앱)    : Authorization: Bearer <Supabase access token>
인증 헤더 (기기)  : X-Device-Key: <기기 API 키>
응답 최상위 모양  : 전부 객체로 감싼다. 하나만 돌려줄 때도.
                   { "device": { ... } }
오류 응답 모양   : { "error": { "type": "...", "code": "...", "message": "..." } }
사용할 상태 코드  : 400 / 401 / 403 / 404 / 409     (422 는 쓰지 않음)
자원 vs 조건     : 자원은 경로, 거르는 조건은 쿼리스트링
                   /api/devices/:id/doses?limit=30&before=...
```

---

### 왜 그렇게 정했나

#### ① 시각 형식

**전제:** JSON 에는 날짜 타입이 없다. 문자열·숫자·불린·배열·객체·null 뿐이라
시각은 반드시 **문자열이나 숫자로 바꿔서** 보내야 한다.

| 선택지 | 예 | 판단 |
|---|---|---|
| Unix 타임스탬프 (숫자) | `1755126000` | ✗ 사람이 못 읽고, 초/밀리초를 헷갈려 1000배 틀리는 사고가 잦다 |
| 시간대 표시 없는 문자열 | `"2026-08-14 08:00:00"` | ✗ **어느 나라 8시인지 알 수 없다** |
| **ISO 8601 문자열** | `"2026-08-13T23:00:00Z"` | ✅ 사람이 읽을 수 있고, 시간대가 명시되고, JS `new Date()` 가 바로 먹는다 |

**그리고 서버는 UTC 그대로 보낸다** (KST 로 바꿔 보내지 않는다).

- 변환은 **마지막에 한 번만** 하는 게 원칙이다. 서버가 미리 KST 로 바꾸면 원본을 되돌릴 수 없다
- `spec.md` §3.4 의 "DB 는 UTC, 화면은 KST" 규칙과 이어진다
- 이미 만들어 둔 `web/src/lib/format.js` 가 UTC 를 받아 KST 로 바꾸도록 되어 있다

> `Z` = UTC(세계 표준시). `+09:00` 은 UTC 보다 9시간 빠름 = 한국시간.
> `"2026-08-13T23:00:00Z"` 와 `"2026-08-14T08:00:00+09:00"` 은 **같은 순간**이다.

#### ② id 형식

**축이 두 개다 — DB 에서 뭘 쓸까 / JSON 으로 어떻게 내려줄까.**

| 선택지 | 판단 |
|---|---|
| 자동 증가 정수 (`1`, `2`, `3`) | ✗ **옆 번호를 찍어볼 수 있다.** `spec.md` §5 화면 3 의 "일련번호만으로는 남의 기기를 등록할 수 있다" 와 같은 문제 |
| **UUID** (`"d3f1a2b4-..."`) | ✅ 추측 불가. 자물쇠는 여러 겹인 게 좋다 |

| 선택지 | 판단 |
|---|---|
| JSON 에 숫자로 | ✗ JS 의 숫자는 **9,007,199,254,740,991** 까지만 정확하다. 그보다 큰 정수는 **오류 없이 조용히 값이 바뀐다** |
| **JSON 에 문자열로** | ✅ id 는 계산에 쓰지 않고 "이것을 가리킨다" 는 용도뿐이다 |

> 문자열로 내려두면 나중에 DB 를 정수 id 로 바꿔도 **응답 모양이 안 바뀐다.**

#### ③ 인증 헤더

| 어디에 실을까 | 판단 |
|---|---|
| URL (`?token=abc`) | ✗ 브라우저 방문기록·서버 접속 로그에 **그대로 남는다** |
| 본문(body) | ✗ **GET 요청에는 본문이 없다** (목록 조회에 못 씀) |
| **헤더** | ✅ 로그에 안 남고 모든 메서드에서 쓸 수 있다 |

**앱과 기기를 나눈 이유** — 하나로 합치면 서버가 토큰을 받을 때마다
"이게 사람 토큰인지 기기 키인지" 판별해야 한다. 헤더 이름이 다르면 **바로 갈린다.**

- `Authorization: Bearer <토큰>` — HTTP 표준. 거의 모든 API 가 이 이름을 쓴다
  - `Bearer` = "지참인". **이 표를 들고 온 사람에게 권한을 주시오** 라는 무기명 티켓
- `X-Device-Key: <키>` — 표준에 없는 헤더는 `X-` 로 시작하는 게 관례

**기기는 왜 키 방식인가** — 라즈베리파이에는 브라우저도 사람도 없어서
이메일/비밀번호를 입력할 수가 없다. 그래서 기기마다 긴 문자열을 하나씩 심어둔다.
(`CLAUDE.md` 의 "책 어디에도 없는 4가지" 중 하나)

> 이 프로젝트는 **쿠키가 아니라 헤더 방식**이다. 프론트(Vercel)와 서버(Render)의
> **도메인이 달라서** 쿠키를 쓰면 설정이 훨씬 까다로워진다.

#### ④ 응답 최상위 모양

```json
// (가) 안 감쌈 — 바로 기기다
{ "id": "d3f1", "nickname": "할아버지 기기" }

// (나) 감쌈 — 한 겹 벗겨야 기기다      ← 선택
{ "device": { "id": "d3f1", "nickname": "할아버지 기기" } }
```

**목록은 어차피 감싸야 한다.** 배열 옆에는 `has_more` 같은 값을 붙일 수 없기 때문이다.
문제는 "하나만 돌려주는 API" 였고, **전부 감싸기로 정했다.**

| | 전부 감싼다 (선택) | 목록만 감싼다 |
|---|---|---|
| 프론트 | 항상 한 겹 벗긴다 — **규칙이 하나** | API 마다 "이건 벗기는 거였나?" 를 기억해야 함 |
| 값 추가 | 키만 늘리면 됨 | 자원 필드와 메타 필드가 섞여 애매해짐 |
| 단점 | `.device` 를 한 번 더 써야 해 살짝 장황 | — |

**옷장에 어떤 옷은 걸고 어떤 옷은 접어 넣으면, 찾을 때마다 한 번 더 생각해야 한다.**

#### ⑤ 오류 응답 모양

**중요: 오류 본문 JSON 모양에는 표준이 없다.** (HTTP 상태 코드만 표준이다)
GitHub 은 `{ "message": ... }`, Stripe 는 `{ "error": { "type", "code", "message" } }` 로 서로 다르다.
`RFC 9457 (Problem Details)` 이라는 표준화 시도가 있지만 실무에서 잘 안 쓰인다.

**Stripe 방식을 골랐다.**

```json
{
  "error": {
    "type": "validation_error",
    "code": "INVALID_CODE",
    "message": "등록코드가 일치하지 않습니다"
  }
}
```

| 필드 | 누가 쓰나 |
|---|---|
| `message` | **화면에 그대로 출력**할 한국어 |
| `code` | 프론트가 **정확히 이 오류일 때만** 분기할 때 |
| `type` | 프론트가 **비슷한 오류를 묶어서** 같은 대응을 할 때 |

**왜 `message` 만으로는 안 되나** — 문구로 분기하면 문구를 고치는 순간 코드가 깨진다.

```js
if (error.message === '등록코드가 일치하지 않습니다') { ... }  // ← 문구 수정 시 깨짐
```

**간판 글씨로 건물을 찾는 것**과 같다. 간판을 바꿔 달면 못 찾는다. 주소(`code`)는 안 변한다.

**`type` 은 "프론트가 어떻게 대응해야 하는가" 로 묶는다.**

| type | 프론트의 대응 |
|---|---|
| `auth_error` | 로그인 화면으로 보낸다 |
| `permission_error` | 목록으로 되돌리고 안내한다 |
| `validation_error` | 그 화면에 머물며 입력을 고치게 한다 |
| `state_error` | 이미 처리됐거나 사라진 것 — 화면을 새로 불러온다 |

#### ⑥ 상태 코드 배정

**422 는 쓰지 않는다.** 400 과 422 의 경계는 사람마다 다르고, Express 진영은 400 으로
통일하는 팀이 많다. 규칙이 적을수록 서버 코드가 깔끔하다.
**409 는 넣는다.** "이미 있어서 안 됨" 은 프론트가 다르게 대응해야 하는 경우가 많다.

| HTTP | code | type | 언제 |
|---|---|---|---|
| 400 | `VALIDATION_ERROR` | `validation_error` | 필수값 누락, 형식 오류 |
| 400 | `SERIAL_NOT_FOUND` | `validation_error` | 등록 폼 — 그런 일련번호가 없음 |
| 400 | `INVALID_CODE` | `validation_error` | 등록 폼 — 등록코드 불일치 |
| 400 | `DOSE_NOT_DUE` | `validation_error` | 아직 오지 않은 복약 건을 복용 처리하려 함 |
| 401 | `UNAUTHORIZED` | `auth_error` | 토큰이 없거나 만료됨 |
| 401 | `INVALID_DEVICE_KEY` | `auth_error` | 기기 API 키가 틀림 |
| 403 | `FORBIDDEN` | `permission_error` | **내 기기/내 알림이 아님** |
| 404 | `DEVICE_NOT_FOUND` | `state_error` | 경로의 기기 id 가 없음 |
| 404 | `NOT_FOUND` | `state_error` | 경로의 복약 건·알림 id 가 없음 |
| 409 | `ALREADY_REGISTERED` | `state_error` | 이미 등록된 기기 |
| 409 | `ALREADY_TAKEN` | `state_error` | 이미 복용 처리된 복약 건 |

**401 과 403 의 차이** — 401 은 **"출입증을 안 보여줬다"**(인증), 403 은 **"출입증은 봤는데 이 층은 못 들어간다"**(인가).
남의 기기를 조회하면 서버는 이 사람이 **누군지는 아는** 상태이므로 403 이다.

**404 를 두 갈래로 나눈 이유** — 등록 폼에서 일련번호를 잘못 친 것은
"주소가 없다" 가 아니라 **"입력값이 틀렸다"** 이므로 400 + `validation_error` 로 보낸다.
그래야 프론트가 그 화면에 머물며 입력칸만 고치게 안내할 수 있다.
404 는 **URL 경로의 자원이 없을 때만** 쓴다.

> **남겨둔 논점 하나** — `SERIAL_NOT_FOUND` 와 `INVALID_CODE` 를 나누면,
> "그 일련번호가 실제로 존재하는지" 를 외부에 알려주는 셈이 된다
> (로그인에서 "아이디 또는 비밀번호가 틀렸습니다" 라고 뭉뚱그리는 것과 반대).
> 이 프로젝트는 **등록코드가 진짜 자물쇠**이고 일련번호는 기기 뒷면에 인쇄돼 있어
> 위험이 낮다고 보고, **보호자가 무엇을 잘못 쳤는지 알 수 있는 쪽**을 택했다.

---

## 1. 화면 2 — 기기 목록

```
【열릴 때】
  메서드·경로 : GET /api/devices                    ← 정함
  쿼리        : ?limit=20&offset=0                  ← 정함
                 · 둘 다 선택. 없으면 limit=20, offset=0
                 · 정렬은 별명 가나다순 고정 (쿼리로 못 바꿈)
  응답 모양   : B안 — 배열 두 개로 분리 + unread_count   ← 정함
                {
                  "devices":     [ { ...기기... } ],
                  "today_doses": [ { "device_id": "d3f1", ... } ],
                  "unread_count": 2,
                  "has_more": false
                }



  화면이 쓰는 필드 : id, serial, nickname, today_doses[].status/dispensed_at
                    (응답에는 patient_phone, timezone 도 오지만 화면 2 는 안 씀)

  응답 필드 (고정) : Device — id, serial, nickname, patient_phone, timezone
                    Dose   — id, device_id, scheduled_at, status,
                             notified_at, dispensed_at, taken_at, taken_source
                    ★ registration_code, device_api_key 는 절대 안 내려줌

  빈 상태         : 200 + { "devices": [], "today_doses": [],
                            "unread_count": 0, "has_more": false }
                    (기기 0대는 오류가 아니라 정상 상태이므로 404 아님)

【동작할 때】
  · 당겨서 새로고침 → 같은 GET 을 다시 부른다
  · 30초마다 폴링   → 같은 GET 을 다시 부른다 (배지 + 오늘 상태가 함께 갱신됨)

【질문 ①】 이 화면이 뜨려면 요청이 몇 번 필요한가?
  → 1번. GET /api/devices 하나로 카드 + 배지를 다 받는다.

【질문 ②】 "오늘 ✕ 약을 안 꺼냄" 이 줄은 어느 테이블에서 오나?
  → doses 테이블. devices 에는 그런 컬럼이 없다.
    서버가 조인해서 today_doses 배열로 함께 내려준다.

```

> ⚠️ `spec.md` §8 에는 아직 이전 버전(A안 + 별도 unread-count API)이 적혀 있다.
> 워크시트를 다 채운 뒤 §8 을 여기 결정대로 한 번에 갱신한다.

### 메서드·경로 — 왜 그렇게 정했나

**메서드는 `GET`.** 이 화면은 읽기만 하고 서버의 무엇도 바꾸지 않는다.
HTTP 메서드가 이미 "가져오기" 라는 동사를 말하고 있으므로,
**주소에는 동사를 쓰지 않는다.** (`/api/getDevices` 는 동사를 두 번 말하는 셈)

**경로는 `/api/devices`.** 다른 후보는 `/api/users/me/devices` 였다.

| | `/api/devices` (선택) | `/api/users/me/devices` |
|---|---|---|
| 주소에 사용자 | 안 드러남 | `me` 로 드러남 |
| 길이 | 짧다 | **모든 API 가 길어진다** |
| 관리자가 남의 기기를 볼 때 | 별도 경로가 필요 | `/api/users/:id/devices` 로 자연스럽게 확장 |

**고른 이유** — 이 프로젝트에는 관리자 기능이 없다.
`spec.md` §2 의 MVP 범위에도, §10 의 "나중에" 목록에도 없다.
확장성 이점은 관리자가 생겨야 발휘되는데, 그 계획이 없다면
**지금 모든 주소를 길게 만드는 값만 치르는 셈**이다.

### ★ "내 기기만" 은 어떻게 골라지나 — 주소가 아니라 토큰이 정한다

경로에 사용자가 안 보여도 모호하지 않은 이유다.

```
[브라우저]
   fetch('/api/devices', {
     headers: { Authorization: 'Bearer eyJhbGciOiJIUzI1NiIs...' }
   })
        │
        ▼
[Express 서버]
   ① Authorization 헤더에서 토큰을 꺼낸다
   ② 토큰을 열어본다 (Supabase Auth 가 서명한 것이므로 위조 확인 가능)
        → 안에 user_id 가 들어 있다   "academy2257 = a1b2c3..."
   ③ 그 user_id 로 DB 를 조회한다

      SELECT d.* FROM devices d
      JOIN user_devices ud ON ud.device_id = d.id
      WHERE ud.user_id = 'a1b2c3...'        ← ★ 여기서 "내 것" 이 걸러진다

   ④ 그 결과만 JSON 으로 돌려준다
```

**핵심: 클라이언트는 "누구의 기기를 달라" 고 말하지 않는다.**
말할 수 있게 두면 남의 user_id 를 적어 보내는 순간 뚫린다.
**누구인지는 오직 토큰만이 말한다.**

호텔에 비유하면 — 프런트에 "302호 열쇠 주세요" 라고 말하는 게 아니라,
**카드키를 대면 기계가 알아서 그 사람 방만 열어주는 것**이다.
방 번호를 입으로 말할 수 있으면 아무 방이나 요구할 수 있게 된다.

> 토큰은 위조가 안 된다. Supabase Auth 가 서명해서 발급하고,
> 서버는 그 서명을 검증한다. 내용을 손대면 서명이 깨져서 바로 걸린다.
> (5단계·7단계에서 직접 구현할 부분)

이 원칙이 `spec.md` §8.5 의 접근 제어 규칙과 이어진다 —
`GET /api/devices/:id` 처럼 **id 를 직접 받는 API** 에서는
"그 기기가 이 토큰 주인의 것인지" 를 서버가 매번 다시 확인해야 한다.

### 쿼리 — 왜 그렇게 정했나

세 가지를 차례로 정했다. **① 자를 것인가 ② 정렬을 열 것인가 ③ 어떻게 자를 것인가.**

#### ① 페이지네이션을 붙인다

**판단 기준: "이 목록, 1년 뒤에 몇 줄이 될까?"**

| 목록 | 1년 뒤 | 자르기 |
|---|---|---|
| 기기 | 그대로 몇 대 (보호자가 직접 등록할 때만 늚) | 사실 불필요 |
| 복약 기록 | **+365줄** (날마다 1줄씩, §3.1) | 필수 |
| 알림 | 놓칠 때마다 | 필수 |

기기 목록만 놓고 보면 **안 붙여도 된다.** 그런데도 붙인 이유는 **목록 API 를 전부 같은 모양으로 만들기 위해서**다.

```
GET /api/devices        → { "devices": [...],       "has_more": false }
GET /api/.../doses      → { "doses": [...],         "has_more": true  }
GET /api/notifications  → { "notifications": [...], "has_more": false }
```

0번에서 "전부 감싼다" 를 고른 것과 같은 결 — **프론트가 규칙 하나로 모든 목록을 다룰 수 있다.**
치르는 값은 지금 당장 `has_more` 가 항상 `false` 로 나가는 것뿐이다.

#### ② 정렬은 가나다순으로 고정. 쿼리 옵션은 안 연다

**카카오톡 친구목록이 레퍼런스.** 친구목록은 가나다순으로 나오지만,
사용자가 "등록순으로 보기" 를 고르는 메뉴는 없다. **앱이 정해둔 순서**다.

여기서 두 가지를 구분해야 했다.

| | 뜻 | 카톡 친구목록 |
|---|---|---|
| 정렬 순서 | 서버가 어떤 순서로 줄 것인가 | 가나다순 |
| 쿼리 옵션 | 클라이언트가 그 순서를 **바꿀 수 있는가** | **없음** |

`spec.md` §5 화면 2 에 정렬 UI 가 없으므로 `?sort=` 를 열어둬도 아무도 안 부른다.
**쿼리는 나중에 추가해도 기존 호출이 안 깨지므로**, 정렬 UI 를 만들 때 그때 열면 된다.

> `?sort=` 를 연다면 서버가 값을 **허용 목록으로 검증**해야 한다.
> 아무 컬럼명이나 받아서 `ORDER BY` 에 넣으면 위험하다.

#### ③ 자르는 방법은 offset

**두 방식은 서버가 실행하는 SQL 이 다르다.**

```sql
-- offset 방식 : 몇 개 "건너뛸지" 세어서 자른다
SELECT * FROM devices ORDER BY nickname LIMIT 20 OFFSET 20;

-- 커서 방식 : "값을 비교해서" 자른다
SELECT * FROM devices WHERE nickname > '김할머니 기기' ORDER BY nickname LIMIT 20;
                            └── 커서가 여기 들어간다
```

**그래서 커서 = 정렬 기준 컬럼의 값이다.**

| 정렬 기준 | WHERE 절 | 커서는 |
|---|---|---|
| `ORDER BY nickname` | `WHERE nickname > ?` | **별명** |
| `ORDER BY scheduled_at DESC` | `WHERE scheduled_at < ?` | **시각** |

**offset 의 약점** — 목록이 도중에 바뀌면 순번이 밀린다.

```
1페이지 (?offset=0&limit=3)   →   8/20, 8/19, 8/18
그 사이에 8/21 이 맨 위에 생김
2페이지 (?offset=3&limit=3)   →   8/18 ★또 나옴, 8/17, 8/16
```

**앞사람이 새치기하면 내 순번이 밀리는 것**과 같다. 커서는 순번이 아니라
"8/18 다음 것" 이라고 내용을 지목하므로 안 밀린다.

**그런데 기기 목록에서는 offset 이 맞다.**

1. **목록이 실시간으로 안 바뀐다** — 복약 기록은 서버가 날마다 자동으로 쌓지만,
   기기는 보호자가 직접 등록할 때만 늘어난다. 페이지를 넘기는 몇 초 사이에
   본인이 기기를 등록할 일은 없다
2. **커서로 쓸 값이 마땅치 않다** — 정렬이 가나다순이라 커서가 **별명**이 되는데,
   - 별명은 **선택 입력이고 중복을 막지 않는다.** 같은 별명이 두 대면
     `WHERE nickname > '할아버지 기기'` 가 **둘 다 건너뛴다.** `>=` 로 바꾸면 이번엔 중복
   - **글자의 "크다"는 DB 정렬 규칙(collation)이 정한다.** `'가'` 와 `'까'`,
     `'2호기'` 와 `'가호기'` 중 뭐가 큰지가 DB 설정에 따라 달라진다.
     개발용 DB 와 배포용 DB 설정이 다르면 페이지가 어긋난다

시각은 **유일하고 순서가 명확해서** 커서에 잘 맞는다.
그래서 **복약 기록·알림은 커서, 기기 목록은 offset** 으로 갈린다.

| | 요청 (자르는 방법) | 응답 (모양) |
|---|---|---|
| 기기 목록 | `?limit=20&offset=0` | `{ "devices": [...], "has_more": false }` |
| 복약 기록 | `?limit=30&before=...` | `{ "doses": [...], "has_more": true }` |
| 알림 목록 | `?limit=30&before=...` | `{ "notifications": [...], "has_more": false }` |

**응답 모양은 전부 같고, 자르는 방법만 목록 성격에 따라 다르다.**

### 응답 모양 — 왜 그렇게 정했나

#### 문제: 카드 한 장에 두 테이블이 섞여 있다

```
┌──────────────────────────┐
│ 할아버지 기기             │ ← devices 테이블
│ PB-2026-0001             │ ← devices 테이블
│ 오늘  ✕ 약을 안 꺼냄       │ ← doses 테이블 (다른 테이블!)
└──────────────────────────┘
```

`spec.md` §7 을 보면 `devices` 가 담는 것은
**일련번호·등록코드·별명·타임존·환자 전화번호·기기 API 키** 뿐이다.
**"오늘 약을 꺼냈는지" 는 없다.** 그건 `doses`(복약 건 = 출석부)에 있다.

서버가 조인해서 합쳐 주는 것은 정해져 있고,
**남은 문제는 "합친 결과를 어떤 모양으로 담을 것인가"** 였다.

#### 후보 셋

```json
// A안 — 기기 객체 안에 today 를 끼워 넣는다
{ "devices": [ { "id": "d3f1", "nickname": "...", "today": { "status": "missed" } } ] }

// B안 — 배열 두 개로 분리한다                       ← 선택
{ "devices":     [ { "id": "d3f1", "nickname": "..." } ],
  "today_doses": [ { "device_id": "d3f1", "status": "missed" } ] }

// C안 — 오늘 상태를 아예 안 준다
{ "devices": [ { "id": "d3f1", "nickname": "..." } ] }
```

| | 짝 맞추기를 누가 하나 | 비고 |
|---|---|---|
| A | **서버**가 미리 합쳐서 준다 | 화면이 기기 하나만 보면 끝 |
| **B (선택)** | **화면 쪽**이 `device_id` 로 맞춘다 | **기기와 복약 건이 안 섞인다 — 각 테이블 모양이 그대로 유지됨** |
| C | 안 준다 | ✗ `spec.md` §5 화면 2 가 "오늘 상태 아이콘" 을 요구하므로 명세를 고쳐야 함 |

A 와 B 는 담는 정보가 같다. **일을 어디서 하느냐만 다르다.**

#### ★ 화면 파일은 안 고쳐도 된다 — 규칙 1 의 첫 시험대

지금 만들어 둔 `DeviceListPage.jsx` 는 `device.today` 를 쓴다. 즉 **A안 모양을 기대한다.**
그런데 B안을 골랐으니 서버 응답에는 `today` 필드가 아예 없다.

**그래도 화면 7장은 하나도 안 고친다.** 데이터 주입 지점에서 조립해 주면 되기 때문이다.

```js
// 6단계에서 store 안에 들어갈 코드
const { devices, today_doses } = await res.json()      // 서버가 준 B안 모양

const merged = devices.map(d => ({                     // 화면이 원하는 A안 모양으로 조립
  ...d,
  today: today_doses.find(t => t.device_id === d.id) ?? null
}))
```

```
[서버] ──B안 모양──▶ [store = 통역사] ──A안 모양──▶ [화면 7장]
                          ↑
                  여기만 적응하면 된다
```

**규칙 1 이 진짜로 지키는 것은 "props 를 쓰느냐" 가 아니라
"응답 모양을 아는 코드가 한 곳에 모여 있느냐" 다.**

거꾸로 말하면 — **응답 모양을 A안이든 B안이든 자유롭게 고를 수 있는 것 자체**가 규칙 1 덕분이다.
화면마다 `fetch` 가 박혀 있었다면 "화면 고치기 싫으니 A안으로 가자" 는 압박을 받았을 것이다.

### 화면이 쓰는 필드 — 왜 그렇게 정했나

#### 먼저, 선택의 여지가 없는 것을 걸러낸다

| `devices` 컬럼 | 내려줄까 |
|---|---|
| `id` / `serial` / `nickname` / `timezone` / `patient_phone` | 가능 |
| **`registration_code`** (등록코드) | **절대 안 됨** |
| **`device_api_key`** (기기 API 키) | **절대 안 됨** |

아래 둘은 정하는 게 아니라 **규칙**이다.

- **등록코드가 새면** — 이 코드는 "기기 뒷면 스티커를 본 사람만 안다" 는 전제로 작동하는 자물쇠다.
  앱 화면에 뜨는 순간 기기 근처에 가본 적 없는 사람도 알게 되어 **전제 자체가 깨진다**
  (폰을 잠깐 빌려준 사람, 스크린샷, 화면 공유)
- **API 키가 새면** — 터미널에서 이 한 줄로 거짓 보고가 가능해진다

  ```bash
  curl -X POST https://우리서버/api/device/events \
    -H "X-Device-Key: dk_9f8e7d..." \
    -d '{ "scheduled_at": "...", "status": "taken", "taken_source": "ir" }'
  ```

  실제로는 안 드셨는데 앱에는 **✓ 복용 완료**가 뜬다. 알림도 안 가고 확인 전화도 안 한다.
  `spec.md` §4.1 의 **"미탐이 오탐보다 아프다"** 가 정확히 이 상황이다.

> **원칙: DB 테이블의 컬럼 목록 ≠ API 응답의 필드 목록**
> 냉장고 재료를 손님상에 다 올리지 않는 것과 같다.

#### 남은 5개는 — A안(항상 같은 필드)

| | A안 (선택) | B안 (화면이 쓰는 것만) |
|---|---|---|
| 얻는 것 | Device 모양이 **하나**. 화면에 필드를 추가할 때 **서버를 안 고쳐도 됨** | 응답이 최소 |
| 치르는 것 | 안 쓰는 필드가 오감 (**기기 20대 × 2필드 ≈ 400바이트**) | 같은 "기기" 인데 API 마다 모양이 다름 |

화면별로 실제 쓰는 것은 이렇게 다르다 — 그래도 응답은 항상 같은 모양으로 준다.

```
화면 2 : id, serial, nickname
화면 4 : id, nickname, patient_phone
화면 5 : nickname (헤더)
화면 6 : nickname (헤더)
```

> `timezone` 은 **어느 화면도 안 쓴다.** 서버가 "매일 08:00" 을 UTC 로 바꿀 때 쓰는
> 내부 계산용 값이다. 화면은 KST 고정으로 표시한다 (`lib/format.js`).
> 나중에 해외 거주 보호자를 지원하면 그때 화면도 쓰게 된다.

#### ★ 단서 — "민감하지 않으면 다 준다" 를 자동 규칙으로 두지 않는다

실무 보안 원칙은 오히려 **필요한 것만 준다** 이다.
`devices` 에 컬럼이 늘어날 것이기 때문이다 — `spec.md` §10 의 `paused_until`(외출 일시정지),
기기 상태 로그, 펌웨어 버전 등. "민감하지 않으니 자동 포함" 이면 의도치 않은 게 샌다.

**그래서 응답 필드 목록을 명시적으로 고정한다.**
지금 A안을 고른 것은 "현재 5개를 다 준다" 는 결정이지,
"앞으로 뭐가 생기든 다 준다" 는 결정이 아니다.
새 컬럼이 생기면 그때 넣을지 판단한다.

#### `patient_phone` 은 숫자만 저장한다

```
DB 저장   : "01012345678"        ← 숫자만
화면 표시 : "010-1234-5678"      ← lib/format.js 의 formatPhone() 이 변환
```

**저장은 원본, 표시는 변환** — 시각을 UTC 로 저장하고 화면에서 KST 로 바꾸는 것과 같은 원칙이다.
하이픈을 넣어 저장하면 `"010-1234-5678"` / `"010 1234 5678"` / `"01012345678"` 이
**같은 번호인데 DB 에선 다른 값**이 되어 검색·중복확인이 안 걸린다.

`tel:` 링크는 하이픈이 있든 없든 동작한다.
국제 표준(E.164 `"+821012345678"`)은 SMS 발송을 붙일 때 필요한데 `spec.md` §10 에 계획이 없다.
필요해지면 앞자리 `0` 을 `+82` 로 바꾸는 규칙 하나면 된다.

---

### 질문 ① 요청 횟수 — 왜 1번인가

화면 아래 하단 탭에 **안 읽은 알림 배지**가 있다. 이 숫자는 `notifications` 테이블에서 온다.

```sql
SELECT COUNT(*) FROM notifications
WHERE user_id = ? AND read_at IS NULL;    -- 읽으면 read_at 이 채워진다
```

`spec.md` §5 화면 7 이 **"30초마다 폴링"** 을 요구하므로,
화면 2 에 머무는 동안에도 배지가 갱신돼야 한다.

> **폴링(polling)** = 일정 간격으로 계속 물어보는 것.
> 택배가 왔나 30초마다 현관에 나가보는 것. 반대는 **초인종 = 푸시(FCM)**.
> `spec.md` §10 에서 푸시를 마지막으로 미뤘기 때문에 지금은 폴링이다.
> (그래서 §10 에 "이게 없으면 5분이 의미를 잃는다" 고 적혀 있다 —
>  폴링은 앱이 켜져 있을 때만 동작한다)

#### 후보 둘

| | A안 — 따로 부른다 | **B안 — devices 응답에 얹는다 (선택)** |
|---|---|---|
| 화면 열릴 때 | 요청 2번 (`Promise.all` 로 동시에) | **요청 1번** |
| 30초 폴링 | `unread-count` 만 반복 (아주 가벼움) | `GET /api/devices` 를 반복 |
| 폴링으로 갱신되는 것 | **배지만** | **배지 + 오늘 복약 상태** |

#### B안을 고른 이유

**① 별도 API 가 필요 없다**

배지가 보이는 화면은 **딱 두 개**다. 하단 탭은 화면 2·7 에만 있고,
화면 3~6 은 파고 들어가는 화면이라 탭이 없다 (묶음 B 에서 정함).

```
화면 2 (탭 보임) → GET /api/devices       에 unread_count
화면 7 (탭 보임) → GET /api/notifications 에 unread_count
화면 3~6 (탭 없음) → 배지가 안 보이므로 갱신할 필요 자체가 없음
```

**② 폴링할 때 카드도 같이 갱신된다**

```
30초 전 : 할아버지 기기   오늘 ⏳ 꺼냄
지금    : 할아버지 기기   오늘 ✓ 복용 완료    ← 카드도 함께 갱신
```

A안이면 배지 숫자만 바뀌고 카드는 그대로다. 보호자가 화면 2 를 보는 동안
할아버지가 약을 드셔도 당겨서 새로고침해야 반영된다.

**③ 자원 경계는 이미 지나간 문제였다**

"기기 목록 응답에 알림 정보가 섞인다" 는 걱정은, **이미 섞여 있다.**

```json
{
  "devices":     [...],   // devices 테이블
  "today_doses": [...],   // doses 테이블        ★ 이미 다른 자원
  "unread_count": 2       // notifications 테이블
}
```

응답 모양을 B안(배열 분리)으로 고른 시점에, 이 응답은 이미
**"순수한 기기 자원" 이 아니라 "화면 2 가 필요로 하는 묶음"** 이 됐다.

> 검토했다가 버린 것 — `GET /api/bootstrap` 처럼 "앱 시작에 필요한 것 전부" 를
> 한 번에 주는 API. 앱 시작이 무거운 서비스(설정·권한·기능 플래그·프로필을
> 한꺼번에 받아야 하는 경우)에 쓰는 방식인데, 이 프로젝트의 첫 화면은
> 기기 목록 하나뿐이라 묶을 게 없다.

### Dose 필드 — notified_at 도 포함한다

화면별로 실제 쓰는 것은 이렇다.

| 화면 | 쓰는 필드 |
|---|---|
| 2 기기 목록 | `status`, `dispensed_at` (놓침 두 종류 구분) |
| 4 기기 상세 | `status`, `dispensed_at` |
| 6 복약 기록 | `scheduled_at`, `status`, `dispensed_at`, `taken_at` |
| 공용 팝업 | `id`, `scheduled_at`, `dispensed_at`, `taken_at`, `taken_source`, `status` |
| 화면 2 짝 맞추기 | `device_id` (B안이라 필요) |

**`notified_at` 은 어느 화면도 안 쓴다.** 공용 팝업이 `spec.md` §5 의 그림대로
**예정 / 꺼냄 / 복용 / 상태** 4줄만 보여주기 때문이다. `timezone` 과 같은 경우다.

**그래도 포함시킨다** — `spec.md` §3.3 이 정의한 복약 건의 본질적 필드이고,
나중에 팝업에 `안내 08:00` 줄을 넣고 싶어져도 서버를 안 고친다. (전역 원칙 ①)

---

## ★ 여기서 확정한 전역 원칙 (2026-08-20)

1~9번을 채우는 내내 이 원칙을 적용한다.

```
① 응답 모양은 항상 같게 한다
   · 하나만 돌려줄 때도 객체로 감싼다
   · ★ 하나만 돌려줄 때도 배열에 담는다  { "devices": [ {...} ] }
     → 클라이언트 코드가 항상 같다. devices.map() 이 어디서나 안전
     → "없음" 을 null 이 아니라 빈 배열로 표현 → null 체크가 사라진다
     → 경로가 가리키는 자원 자체가 없으면 404, 부속물이 없으면 200 + []
   · 같은 자원은 어느 API 에서든 같은 필드 목록을 갖는다
   · 안 쓰는 필드가 몇 개 오가는 비용은 감수한다

② 지금 안 쓰는 기능은, "다른 API 와 모양이 맞춰질 때만" 미리 만든다
   · 페이지네이션 → 만든다 (복약기록·알림이 반드시 필요하므로 셋이 같은 모양이 됨)
   · 정렬 옵션    → 안 만든다 (맞출 대상이 없어 순수 추가 부담)

③ 요청 파라미터는 목록 성격에 맞춘다 (offset / 커서)
   · 응답 모양은 그래도 동일하다

④ 보안 민감 정보는 예외 없이 제외하고, 응답 필드 목록을 명시적으로 고정한다
```

---

## 2. 화면 3 — 기기 등록

> 이 화면은 **열릴 때 서버를 부르지 않는다.** 빈 폼이므로 【열릴 때】 칸이 없다.

```
【동작할 때】
  메서드·경로 : POST /api/user-devices              ← 정함
  요청 본문   : { "serial": "PB-2026-0001",        ← 정함
                  "code":   "A1B2C3",
                  "nickname": "할아버지 기기" }
                · nickname 은 선택. 비면 서버가 serial 을 별명으로 채운다
                · serial / code 는 서버가 대소문자·공백·하이픈을 무시하고 찾는다

  성공 응답   : 201                                  ← 정함
                { "devices": [ { ...Device... } ] }
                · 새로 만들었으므로 200 이 아니라 201 Created
                · 방금 등록한 기기는 약 설정도 복약 건도 없으므로 dose 는 안 담는다

  실패 응답   :                                      ← 정함
     400 { "error": { "type": "validation_error", "code": "VALIDATION_ERROR",
                      "message": "일련번호를 입력해 주세요" } }
     400 { "error": { "type": "validation_error", "code": "VALIDATION_ERROR",
                      "message": "등록코드는 영문·숫자 6자리입니다" } }
     400 { "error": { "type": "validation_error", "code": "SERIAL_NOT_FOUND",
                      "message": "해당 일련번호의 기기를 찾을 수 없습니다" } }
     400 { "error": { "type": "validation_error", "code": "INVALID_CODE",
                      "message": "등록코드가 일치하지 않습니다" } }
     409 { "error": { "type": "state_error", "code": "ALREADY_REGISTERED",
                      "message": "이미 등록된 기기입니다" } }

     · 401(토큰 만료)은 모든 API 공통이라 0번 표에만 두고 여기 반복하지 않는다



【질문 ③】 등록에 성공하면 화면 2로 돌아간다.
          기기 목록을 다시 부를 것인가, 응답으로 받은 것을 끼워 넣을 것인가?
  → (가) 목록을 다시 부른다. 프론트가 GET /api/devices 를 한 번 더 호출한다.

```

{
  "error":{
    "type":"",
    "code":"400",
    "message":"필수값 누락",
  }
}

### 메서드·경로 — 왜 그렇게 정했나

**메서드는 `POST`.** 새로 만드는 동작이다.
`PUT` 이 아닌 이유 — `PUT` 은 "이 주소에 이걸 놔둬라" 라서 **만들어질 것의 주소를 미리 알아야** 하는데,
등록 결과로 생기는 줄의 주소는 서버가 만들어 주기 전엔 모른다.

**경로는 `/api/user-devices`.** 핵심은 이것 —
**이 API 가 실제로 만드는 것은 기기가 아니라 "연결" 이다.**

```
devices          (공장에서 이미 만들어져 DB 에 있음)
  PB-2026-0001

user_devices     (등록하면 여기에 한 줄 생김)      ← 이게 진짜 만들어지는 것
  academy2257 ── PB-2026-0001
```

#### 후보 둘

| | 등록 | 나중에 "등록 해제" 를 만들 때 |
|---|---|---|
| ① 내 목록에 추가 | `POST /api/devices` — GET 과 주소가 같아 짝이 맞음 | `DELETE /api/devices/:id` ← **"기기를 삭제한다"** 로 읽힘 |
| **② 연결을 만든다 (선택)** | `POST /api/user-devices` — 주소를 하나 더 둠 | `DELETE /api/user-devices/:id` ← **"연결을 끊는다"** 로 읽힘 |

①의 `DELETE /api/devices/:id` 는 기기 자체를 지우는 것처럼 보이지만
실제로는 **내 연결만 끊는다.** 다른 보호자는 여전히 그 기기를 보고 있다.

**어느 쪽이든 만드는 동작은 하나다.** 차이는 주소를 새로 둘 것인가뿐이고,
주소가 하나 늘어나는 대신 **그 주소가 하는 일이 정확히 드러난다.**

> **★ 주의 — 경로 이름과 DB 구조는 별개다.**
> 주소를 뭐라고 짓든 서버가 하는 일은 똑같다.
>
> ```
> ① devices 에서 serial 로 기기를 찾는다
> ② code 가 맞는지 확인한다
> ③ user_devices 에 (user_id, device_id) 한 줄 추가   ← 여기만 쓴다
> ```
>
> **`devices` 테이블은 손대지 않는다.** `user_id` 를 붙일 자리도 없다 —
> 그래서 중간 테이블을 따로 둔 것이다 (`spec.md` §7).
> **URL 은 문패일 뿐이고, 집 안 구조는 §7 이 정한다.**

### 요청 본문 — 왜 그렇게 정했나

#### ① 별명을 비우면 서버가 일련번호로 채운다

| 후보 | 저장 | 화면 2 에 보이는 것 |
|---|---|---|
| 비워둔다 | `nickname: null` | `이름 없는 기기` (화면이 대체 문구를 넣음) |
| **일련번호로 채운다 (선택)** | `nickname: "PB-2026-0001"` | `PB-2026-0001` |

`DeviceListPage.jsx` 가 이미 `device.nickname \|\| '이름 없는 기기'` 라서 **둘 다 동작한다.**

**갈린 지점은 정렬이다.** 별명 가나다순으로 정렬하기로 했는데 `null` 은 어디에 놓일지가
DB 설정에 따라 다르다 — 맨 앞이나 맨 뒤로 몰린다. 일련번호로 채우면 그 문제가 없다.

#### ② serial / code 는 서버가 관대하게 찾는다

사용자는 이렇게 칠 수 있다.

```
"pb-2026-0001"      소문자
" PB-2026-0001 "    앞뒤 공백
"pb 2026 0001"      하이픈 대신 공백
```

**어느 쪽이든 찾아준다.** 사용자가 입력 형식을 신경 쓰지 않아도 되게 한다.

> **★ 중요 — 이건 "저장 형식" 이 아니라 "비교 방법" 이다.**
> 일련번호는 우리가 새로 저장하는 값이 아니다. `devices` 에 공장에서 넣은
> `PB-2026-0001` 이 이미 있고, 우리는 **찾기만** 한다.
> (전화번호는 우리가 저장하는 값이라 "숫자만 저장" 이 저장 형식 결정이었다)

```sql
SELECT * FROM devices
WHERE UPPER(REPLACE(REPLACE(serial, '-', ''), ' ', ''))
    = UPPER(REPLACE(REPLACE($1,     '-', ''), ' ', ''));
```

#### ★ 서버는 클라이언트를 믿지 않는다

`DeviceRegisterPage.jsx` 가 보내기 전에 `trim().toUpperCase()` 를 하지만,
**서버도 반드시 해야 한다.** 화면을 거치지 않고 이렇게 보낼 수 있기 때문이다.

```bash
curl -X POST .../api/user-devices -d '{"serial":"pb-2026-0001", ...}'
```

**화면의 정리는 사용자 편의, 서버의 정리는 필수.** 이건 선택이 아니라 원칙이다.

#### 4단계에서 반영할 것 — `serial_normalized` 컬럼

컬럼에 함수를 씌우면(`UPPER(REPLACE(...))`) **인덱스를 못 쓴다.**
기기가 수만 대가 되면 매번 전체를 훑는다. 정규화된 값을 담는 컬럼을 하나 더 둔다.

```
serial            "PB-2026-0001"   ← 사람에게 보여주는 원본
serial_normalized "PB20260001"     ← 찾기용 (인덱스를 여기에)
```

`CLAUDE.md` 의 **"DB 컬럼 추가는 싸다"** 가 정확히 이런 경우다.

### 성공 응답 · 질문 ③ — 왜 그렇게 정했나

#### 목록은 다시 부른다

```js
// (가) 목록을 다시 부른다                    ← 선택
await fetch('/api/user-devices', { method: 'POST', ... })
setScreen('devices')
await fetchDevices()          // GET /api/devices 를 한 번 더

// (나) 응답으로 받은 것을 끼워 넣는다
const { devices } = await res.json()
addDeviceToStore(devices[0])  // 서버를 안 부르고 목록 배열에 추가
```

**(나)를 어렵게 만드는 것이 셋 있다.**

1. **정렬이 가나다순** — 새 기기를 목록 어디에 끼울지 프론트가 한글 정렬을 직접 계산해야 하는데,
   **서버(DB)의 정렬 규칙과 미묘하게 다를 수 있다.** 커서 페이지네이션을 포기했던 그 `collation` 문제와 같은 뿌리
2. **페이지네이션이 있다** — 1페이지만 보고 있는데 새 기기가 가나다순 25번째라면?
   지금 화면에 끼우면 안 되는데 프론트는 그걸 판단할 수 없다
3. **등록은 평생 몇 번** — 요청 하나 아끼는 이득이 거의 없다

#### 성공 응답에 dose 는 안 담는다

방금 등록한 기기는 **약 설정도 없고 복약 건도 아직 없다.** 담을 게 없다.
((나)를 골랐다면 화면 2 카드가 오늘 상태를 그려야 하므로 `today: null` 을 같이 줘야 했을 것이다)

#### 이름 정리 — Device / device / devices

헷갈리기 쉬운 지점이라 적어둔다.

| 표기 | 무엇 | 어디에 나오나 |
|---|---|---|
| **`Device`** (대문자·단수) | 우리가 정한 **객체 모양의 이름** | **명세 문서에만.** JSON 에는 안 나옴 |
| **`devices`** (소문자·복수) | 응답 JSON 의 **키 이름** | 실제 JSON |
| `devices` 테이블 | DB 테이블 이름 | SQL |
| `/api/devices` | URL 경로 | 주소창 |

`Device` 는 **붕어빵 틀**이고 `devices` 는 **그 붕어빵을 담은 봉지의 라벨**이다.
봉지에 "붕어빵틀" 이라고 쓰지 않는다.

`api-decisions.md` 의 `{ "...Device..." }` 는 "여기에 Device 모양이 통째로 들어간다" 는 줄임 표기다.

#### 공통 객체는 4개로 끝

| 객체 | 상태 | 쓰는 곳 |
|---|---|---|
| `Device` | ✅ | 화면 2·3·4·5·6 |
| `Dose` | ✅ | 화면 2·4·6·7·팝업 |
| `Medication` | ⬜ 화면 5 에서 | |
| `Notification` | ⬜ 화면 7 에서 | |

응답에는 객체가 아닌 값도 섞인다 — `unread_count`, `has_more`, `next_before`, `week_start` 등.
**한 곳에서만 쓰이거나 값 하나뿐이라 "공통 객체" 로 만들 이유가 없다.**

공통 객체로 만드는 기준은 **"여러 화면이 같은 모양을 재사용하는가"** 다.
그래야 화면 4 는 `dose` 라 부르고 화면 6 은 `record` 라 부르는 일이 안 생긴다.

> **디바이스용 API 는 별개다.** 라즈베리파이가 자기 별명이나 환자 전화번호를 알 필요가 없다.
> `Device` 를 통째로 주면 불필요한 노출이므로, 거기선 필요한 것만 담은 다른 모양을 쓴다.

### 실패 응답 — 짚고 갈 것

#### HTTP 상태 코드는 본문 안에 있는 게 아니다

응답은 **층이 셋**이다.

```
HTTP/1.1 400 Bad Request              ← ① 상태 라인
Content-Type: application/json        ← ② 헤더
                                      ← ③ 빈 줄
{ "error": { "code": "...", ... } }   ← ④ 본문(body)
```

`code` 에는 `400` 이 아니라 **`VALIDATION_ERROR` 같은 문자열**이 들어간다.
`400` 은 "요청이 잘못됐다" 까지만 말하는데, 화면 3 에서 400 이 뜨는 경우가 **네 가지**나 되므로
프론트가 구분하려면 `code` 가 필요하다.

```js
res.status(400).json({ error: { code: 'VALIDATION_ERROR', ... } })
    │              │
    │              └─ Content-Type 을 자동으로 붙이고 객체를 JSON 으로 바꿔 본문에 넣는다
    └─ 상태 라인을 "400 Bad Request" 로 만든다
```

> 이유 구절(`Bad Request`, `Conflict` …)은 번호마다 다르고 Express 가 자동으로 붙인다.
> 사실상 아무도 안 본다 — HTTP/2 부터는 아예 없어졌다.

#### message 는 사용자가 읽을 문구다

```
"필수값 누락"              ← 개발자 말투. 보호자가 보면 곤란
"일련번호를 입력해 주세요"   ← 화면 상단 빨간 띠에 그대로 찍힌다
```

| 필드 | 누가 읽나 |
|---|---|
| `message` | **사용자** |
| `code` | 프로그램 — 정확히 이 오류일 때 분기 |
| `type` | 프로그램 — 비슷한 오류를 묶어 같은 대응 |

#### type 이 갈리는 지점

```js
if (error.type === 'validation_error') { /* 폼에 머물며 입력을 고치게 */ }
if (error.type === 'state_error')      { setScreen('devices') }
```

앞의 넷은 입력을 고치면 되지만, `ALREADY_REGISTERED` 는 **입력을 고쳐도 소용없다.**
이미 등록된 기기이므로 화면 2 로 보내주는 게 친절하다.
**`code` 를 하나하나 나열하지 않고도 대응이 갈린다** — `type` 을 넣기로 한 값이 여기서 나온다.

> JSON 에는 **마지막 쉼표를 못 쓴다.** JS 객체는 허용하지만 JSON 은 문법 오류다.

---

## 3. 화면 4 — 기기 상세 (이번 주)

> ★ 이 API 는 **화면 6(복약 기록)과 공용**이다. 같은 자원(복약 건)을 기간만 다르게 본다.

```
【열릴 때】
  메서드·경로 : GET /api/devices/:id/doses            ← 정함 (화면 6 과 공용)
                · :id 는 UUID (serial 아님)
  쿼리        : ?from=2026-08-17&to=2026-08-23        ← 정함. 날짜 범위 하나로 통일
                · 화면 4    이번 주 월~일
                · 화면 6    최근 30일
                · [더 보기]  그 이전 30일
                · 프론트가 날짜를 계산해서 보낸다 (MVP 기준 한국 전용)
  응답 모양   : {
                  "devices":     [ { ...Device 5개... } ],
                  "doses":       [ { ...Dose 8개... } ],
                  "medications": [ { ...Medication... } ],
                  "has_more": false
                }




  화면이 쓰는 필드 : Device — nickname, patient_phone (id 는 [약 설정]·[복약 기록] 이동에)
                    Dose   — id(팝업의 taken 처리), scheduled_at, status, dispensed_at
                    medications.length (약 미설정 판단)
                    · 7칸 날짜는 프론트가 보낸 from~to 를 그대로 쓴다
                    · timezone, serial, notified_at, device_id 는 응답에 오지만 이 화면은 안 씀

  빈 상태          : 기기가 없으면          404 DEVICE_NOT_FOUND
                    복약 건이 없으면        200 + "doses": []
                    약을 아직 설정 안 했으면 200 + "medications": []

【질문 ①】 이 화면은 "기기 별명"과 "전화번호"도 쓴다. 그건 어디서 오나?
  → 같은 응답의 devices 배열에 담아 준다. 요청을 2번 하지 않기 위해.
    화면 2 를 거쳐 왔다고 가정하면 안 된다 — 라우터를 붙이면
    /devices/:id 로 바로 들어오거나 새로고침할 수 있다.

【질문 ④】 복용 요일이 월·수·금이면 복약 건은 몇 개 오나?
  → 3개. 화·목·토·일에는 복약 건 자체가 안 만들어진다 (출석부는 있었던 날만 줄이 생김).
    화면은 자기가 보낸 from ~ to 로 날짜 7칸을 먼저 그리고,
    각 날짜에 해당하는 복약 건을 찾아 끼운다. 없는 칸은 점선.

【어려운 것】 주간에 복약 건이 0개다. 두 경우를 구분할 수 있나?
  → medications 배열로 갈린다.
     medications: []      → (가) 약 미설정      → "약 설정하기" 버튼
     medications: [{...}] → (나) 이번 주 복용일 없음 → 그냥 빈 주

```

### 화면 4 — 왜 그렇게 정했나

#### 경로 — 화면 이름이 아니라 자원 이름으로

처음 초안은 `GET /api/device-detail` 이었다. 두 가지가 문제였다.

1. **어느 기기인지가 없다.** 서버는 화면 2 에서 카드를 눌러 들어왔다는 사실을 모른다
2. **`device-detail` 은 화면 이름이지 자원 이름이 아니다**
   - 화면 이름이 바뀌면 URL 도 바꿔야 하나?
   - 화면 6 도 **같은 기기의 복약 건**을 본다 → `/api/dose-history` 가 되면
     하나의 데이터에 주소가 두 개가 된다

**"이 API 가 무엇을 가져오는가" 로 다시 물으면** — *그 기기의, 어떤 기간의 복약 건*.
그래서 `/api/devices/:id/doses` + 기간 조건이다.

#### ★ 화면 4 와 화면 6 은 같은 API 를 쓴다

MVP 기준에서 가장 크게 아낀 지점. 둘은 **같은 자원을 기간만 다르게** 본다.

```
화면 4      ?from=2026-08-17&to=2026-08-23    이번 주 월~일
화면 6      ?from=2026-07-25&to=2026-08-23    최근 30일
[더 보기]   ?from=2026-06-25&to=2026-07-24    그 이전 30일
```

**세 경우가 전부 같은 모양이다.** 서버는 라우터 하나만 만들면 되고, 화면 6 은 공짜로 딸려온다.

**덤 — 페이지네이션 문제가 사라진다.** `offset` 의 약점은 "목록이 도중에 바뀌면 순번이 밀린다" 였는데,
`7/25 ~ 8/23` 은 새 기록이 쌓여도 **같은 구간**이다. 커서 방식의 장점을 그냥 얻는다.
그래서 `limit` · `before` · `offset` 이 아예 필요 없다.

`has_more` 만 서버가 알려준다 — `from` 이전에 더 오래된 기록이 있는지.
화면 6 의 [더 보기] 버튼을 보일지 판단해야 하기 때문이다.

검토했다가 버린 후보:

| | 문제 |
|---|---|
| `GET /api/devices/:id/week` | "이번 주" 라는 이름을 붙이면 화면 6 용 API 를 또 만들어야 함 |
| `GET /api/devices/:id` | "기기 하나" 를 요청했는데 이번 주가 왜 오나. 화면 6 은 여전히 별도 |

#### `:id` 에는 UUID 가 들어간다 (serial 아님)

```
GET /api/devices/d3f1a2b4-9c8e-4f21-.../doses?range=week
```

| | 쓰임 | 어디에 |
|---|---|---|
| `serial` `PB-2026-0001` | **사람이 눈으로 보고 입력**하는 값 | 화면 3 의 등록 폼 |
| `id` (UUID) | **프로그램이 가리키는** 값 | 그 외 전부 |

`serial` 은 순차적이라 URL 에 쓰면 **옆 번호를 찍어볼 수 있다.**
0번 절에서 id 를 UUID 로 정한 이유가 사라진다.

> `devices.id` 는 **기기**를 가리키지 사람을 가리키지 않는다.
> 아들이 보든 딸이 보든 같은 값이고, **누가 요청했는지는 토큰이 말한다.**
> 사람별 구분은 `user_devices` 테이블의 `user_id` 가 한다.

#### 날짜는 프론트가 계산한다 — MVP 한정

한 번 뒤집었다가 되돌린 결정이라 이유를 남겨둔다.

**걱정했던 것** — 프론트가 `new Date()` 로 이번 주를 구하면 **보호자 브라우저 기준**이 된다.

```
같은 순간을 두 곳에서 보면
  서울    2026-08-24 (월) 09:00
  뉴욕    2026-08-23 (일) 20:00      ← 날짜도 요일도 다르다

뉴욕의 아들이 앱을 열면
  new Date() → 8/23(일)  →  "이번 주" = 8/17 ~ 8/23 로 계산
  그런데 서울은 이미 8/24(월). 오늘 08:00 에 복약 안내가 나갔고
  그 복약 건(8/24)은 요청 범위 밖 → 화면 4 에 안 보인다
```

**한국 월요일 00:00~13:00 사이(뉴욕은 아직 일요일)에 매일 어긋난다.**
그리고 화면 2 의 `today_doses` 는 **서버가** 기기 타임존으로 골라주므로 정상이라,
**같은 앱에서 두 화면이 다른 날짜 기준을 쓰게 된다.**

**그런데 MVP 에서는 이 일이 없다.** `spec.md` §3.4 가 "화면에는 KST 로 표시" 라고
못박았고 `lib/format.js` 도 KST 고정이다 — **이 앱은 지금 한국 전용**이다.
해외 보호자는 §10(나중에)에도 없다. **아직 없는 문제를 미리 풀려고 파라미터를 쪼갤 이유가 없다.**

나중에 필요해지면 **API 모양을 안 바꾸고** 해결된다 —
프론트가 `from`/`to` 를 안 보내면 서버가 기기 타임존 기준 기본값을 채우도록 한 줄 추가하면 된다.

#### 약 미설정 구분 — `medications` 배열로

후보는 둘이었다.

| | |
|---|---|
| `has_medication: true/false` | 응답이 가볍다 |
| **`medications: []` / `[{...}]`** | **0번의 "없음 = 빈 배열" 원칙이 그대로 적용된다.** 나중에 화면 4 에 "혈압약 1정 · 매일 08:00" 을 표시하고 싶어져도 서버를 안 고친다 |

> 약 설정은 기기당 1줄이라 배열에 **0개 또는 1개**뿐이다. `map` 을 쓰지 않고
> `medications.length === 0` 으로 판단하거나 `medications[0]` 으로 꺼낸다.
> "항상 배열" 원칙의 대가지만, `null` 체크가 사라지고 다루는 방식이 통일된다.

---

## 4. 화면 5 — 약 설정

```
【열릴 때】
  메서드·경로 : GET /api/devices/:id/medications
  응답 모양   : { "devices":     [ { ...Device... } ],
                  "medications": [ { ...Medication... } ] }

  아직 약을 설정하지 않은 기기라면?
  → 200 + "medications": []   (404 아님)
  → 이유: 새 기기는 "없는 게 정상" 이지 오류가 아니다.
          0번의 "없음 = 빈 배열" 원칙 그대로. 프론트가 오류 분기를 안 만들어도 된다.

【저장할 때】
  메서드      : PUT     ← 멱등하기 때문
  경로        : /api/devices/:id/medications
  요청 본문   : { "name": "혈압약", "dosage": "1정",
                  "time": "08:00", "days": [1, 3, 5] }
  응답 모양   : { "medications": [ { ...Medication... } ] }

【질문】 저장 버튼을 실수로 두 번 누르면 약 설정이 두 줄이 되나?
  → 안 된다. 기기당 1줄이라 항상 덮어쓴다.
    PostgreSQL 의 ON CONFLICT (device_id) DO UPDATE.
    POST 였다면 두 번 보낼 때 두 줄이 생길 수 있다 — 그래서 PUT 이다.
```

### 화면 5 — 짚고 갈 것

#### ★ `time` 은 UTC 가 아니다 — 0번 규칙의 유일한 예외

0번에서 **"모든 시각은 UTC ISO 8601"** 로 정했지만, 복용 시간만 다르다.

```json
"time": "08:00"                      ← 벽시계 값
"time": "2026-08-21T23:00:00Z"       ← ✗ 이렇게 하면 안 된다
```

**"매일 08:00" 은 순간이 아니라 규칙이다.** UTC 로 바꾸려면 "어느 날의 08시" 인지가 있어야 하는데,
매일 반복되는 값에는 날짜가 없다. `spec.md` §3.4 가 이렇게 정해뒀다.

> "매일 08:00" 같은 벽시계 값은 **시각(`time`) + 기기 타임존**으로 저장하고,
> 그날의 복약 건을 만들 때 UTC 시각으로 계산한다

```
medications.time      "08:00"          ← 시간표 (벽시계)
devices.timezone      "Asia/Seoul"
        ↓ 서버가 그날의 복약 건을 만들 때 계산
doses.scheduled_at    "2026-08-20T23:00:00Z"   ← 출석부 (UTC 순간)
```

**시간표는 벽시계, 출석부는 UTC.** `spec.md` §3.1 의 구분이 시각 형식에서도 갈린다.

#### `POST` 가 아니라 `PUT`

| | 뜻 | 두 번 보내면 |
|---|---|---|
| `POST` | "새로 만들어라" | **두 개가 생길 수 있다** |
| **`PUT`** | "이 주소를 이 내용으로 만들어라" | 결과가 같다 (**멱등**) |

약 설정은 **기기당 1줄**이고 저장은 항상 덮어쓰기다. 그래서 PUT 이 맞다.
저장 버튼을 두 번 눌러도, 네트워크가 느려 요청이 두 번 나가도 결과가 같다.

---

## 5. 화면 6 — 복약 기록

> **화면 4 와 같은 API 를 쓴다.** 경로·응답은 3번 절에서 이미 정했다.

```
【열릴 때】
  메서드·경로 : GET /api/devices/:id/doses          ← 화면 4 와 동일
  쿼리        : ?from=2026-07-25&to=2026-08-23      최근 30일
  응답 모양   : { devices[], doses[], medications[], has_more }

  정렬        : scheduled_at 내림차순 (최근 것이 위)
  화면이 쓰는 필드 : Device — nickname (헤더)
                    Dose   — id, scheduled_at, status, dispensed_at, taken_at, taken_source
                    has_more ([더 보기] 버튼을 보일지)
  빈 상태          : 200 + "doses": []

【[더 보기]를 누를 때】
  메서드·경로 : 같은 API
  쿼리        : ?from=2026-06-25&to=2026-07-24      그 이전 30일

【질문 ④】 3년치 기록이 쌓였다. 한 번에 몇 개씩 줄 것인가?
          "다음 30개"를 어떻게 지정할 것인가?

  방법 A: offset  — "31번째부터"          → 목록이 도중에 바뀌면 순번이 밀린다
  방법 B: 커서    — "이 시각 이전부터"     → 안 밀린다
  방법 C: 날짜 범위 — "이 기간의 것"       ★ 고른 것

  고른 것과 이유:
    날짜 범위(from/to). 화면 4 와 파라미터를 통일할 수 있고,
    "7/25 ~ 8/23" 은 새 기록이 쌓여도 같은 구간이라 밀리는 문제가 없다.
    커서의 장점을 그냥 얻으면서 limit·before·offset 이 아예 필요 없어진다.
```

---

## 6. 화면 7 — 알림 목록

```
【열릴 때】
  메서드·경로 : GET /api/notifications
  쿼리        : ?limit=30      (선택, 기본 30)
  응답 모양   : { "notifications": [ { ...Notification... } ],
                  "devices":       [ { ...Device... } ],
                  "doses":         [ { ...Dose... } ],
                  "unread_count": 2,
                  "has_more": false }

  화면이 쓰는 필드 : Notification — device_id, dose_id, message, created_at, read_at
                    Device       — nickname (한 줄에 표시)
                    Dose         — 항목을 탭하면 열리는 공용 팝업이 씀
                    unread_count — 하단 탭 배지
  빈 상태          : 200 + 세 배열 모두 [], unread_count: 0

【항목을 탭할 때】  ← 두 가지가 동시에 일어난다
  (1) 읽음 처리
  (2) 공용 팝업 열기 (응답에 딸려온 doses 에서 dose_id 로 찾아 쓴다)

  메서드·경로 : POST /api/notifications/:id/read
  응답 모양   : { "notifications": [ { ...갱신된... } ], "unread_count": 1 }

【질문 ②】 "할아버지 기기 · 약을 안 꺼내셨어요 · 08:05"

  "할아버지 기기" 는 어디서 오나?
    → notifications 테이블에는 device_id 밖에 없다.
      devices 배열을 따로 담아 주고 화면이 device_id 로 짝을 맞춘다.
      (화면 2 의 devices / today_doses 와 똑같은 패턴)

  "약을 안 꺼내셨어요" 는 누가 만드나?
    → 서버가 만든다. FCM 푸시(§10)도 같은 문구를 써야 하는데,
      두 군데서 만들면 목록엔 "미수령", 푸시엔 "약을 안 꺼내셨어요" 처럼 어긋난다.
    → DB 에 저장하지 않고 응답할 때마다 type 으로부터 만든다.
      저장해두면 문구를 고쳤을 때 과거 알림까지 같이 바뀐다.

【어려운 것】 하단 탭의 빨간 배지는 화면 2에 있을 때도 갱신돼야 한다.
  → 화면 2 는 GET /api/devices 를 30초마다 다시 부른다.
    그 응답에 unread_count 가 얹혀 있으므로 별도 API 가 필요 없고,
    부르는 김에 카드의 오늘 상태도 같이 갱신된다.
    (배지가 보이는 화면은 2·7 둘뿐 — 3~6 은 하단 탭이 없다)
```

### 화면 7 — 짚고 갈 것

**배열 세 개로 나눈 것은 화면 2 와 같은 판단이다.**
`Notification` 안에 기기 별명과 복약 건을 박아 넣을 수도 있었지만,

- 같은 기기의 알림이 여러 개면 **별명이 중복해서 실린다**
- 나중에 알림 목록에 기기 아이콘이나 전화 버튼을 넣고 싶어지면 **서버를 고쳐야 한다**

배열을 나눠 주면 화면이 `device_id` / `dose_id` 로 짝을 맞춘다.
화면 2 에서 이미 하고 있는 일이라 새로 배울 것도 없다.

**`?limit=` 인 이유** — 복약 기록은 날짜 범위(`from`/`to`)로 잘랐는데 알림은 개수로 자른다.
알림은 **드물게 쌓여서** "최근 30일" 이 2건일 수도 있다. 전역 원칙 ③ — 요청 파라미터는
목록 성격에 맞춘다. (응답 모양은 그래도 동일하다)

---

## 7. 공용 팝업 — 복약 상세

```
【열릴 때】
  서버를 부르나?  (부른다면 무엇을, 안 부른다면 왜)


【먹었어요로 표시】
  메서드·경로 : POST /api/doses/:id/taken
  요청 본문   : 없음
                (무엇으로 바꿀지는 서버가 안다 — status='taken',
                 taken_source='manual', taken_at=현재시각)
  응답 모양   : { "doses": [ { ...갱신된 Dose... } ] }

【질문 ③】 처리 후 화면 4의 ⚠ 를 ✓ 로 바꾸려면?

  방법 A: 주간 전체를 다시 부른다
  방법 B: 응답으로 받은 그 한 건만 갈아끼운다        ★ 고른 것
  고른 것과 이유:
    B. 응답이 갱신된 Dose 를 돌려주므로 목록 전체를 다시 안 불러도 된다.
    같은 팝업을 화면 4·6·7 이 공유하므로, 어느 화면에서 눌렀든
    그 화면의 배열에서 id 가 같은 항목만 교체하면 된다.

    (화면 3 의 기기 등록은 반대로 "다시 부르기" 를 골랐다.
     거기는 가나다순 정렬 + 페이지네이션 때문에 끼워 넣을 위치를 몰랐지만,
     여기는 이미 목록에 있는 항목을 제자리에서 바꾸는 것이라 위치 문제가 없다)

【질문】 화면은 미래 날짜면 버튼을 감춘다. 서버도 막아야 하나?
  → 막아야 한다. 400 DOSE_NOT_DUE.
    화면이 버튼을 감춰도 curl 로 직접 보낼 수 있다.
    화면 3 의 "서버는 클라이언트를 믿지 않는다" 와 같은 이유.

  실패 응답:
    400 DOSE_NOT_DUE    validation_error   아직 오지 않은 복약 건
    403 FORBIDDEN       permission_error   그 복약 건의 기기가 내 것이 아님
    404 NOT_FOUND       state_error        그런 복약 건 없음
    409 ALREADY_TAKEN   state_error        이미 복용 처리됨
```

---

## 8. 디바이스용 API

앱 화면이 아니라 라즈베리파이가 부르는 API다. `spec.md` §4 를 먼저 읽을 것.

```
【스케줄 받아가기】
  메서드·경로 : GET /api/device/schedule
                (인증은 X-Device-Key 헤더)
  응답 모양   : { "devices":     [ { "id": "d3f1...", "timezone": "Asia/Seoul" } ],
                  "medications": [ { "name": "혈압약", "dosage": "1정",
                                     "time": "08:00", "days": [1, 3, 5] } ] }

  약 설정이 없는 기기라면?
  → "medications": []   → 디바이스는 아무 안내도 하지 않는다

  ★ Device 객체를 통째로 주지 않는다.
    별명·일련번호·환자 전화번호는 기기에게 불필요한 노출이다.
    앱용 API 와 객체 모양을 공유하지 않는 유일한 자리.

【상태 보고】
  메서드·경로 : POST /api/device/events
  요청 본문   : { "scheduled_at": "2026-08-12T23:00:00Z",   ← ★ 어느 복약 건인지
                  "status": "dispensed",
                  "occurred_at": "2026-08-12T23:03:12Z",    ← 실제로 일어난 시각
                  "taken_source": null }
  응답 모양   : { "doses": [ { ...Dose... } ] }

【가장 어려운 것】

  디바이스가 "약을 꺼냈다"고 보고했다.
  서버는 이걸 doses 테이블의 어느 줄에 적어야 하나?

  → 디바이스는 dose_id 를 모른다. 그 번호는 서버가 만든 것이다.
  → 그래서 요청에 scheduled_at(그 복약 건의 예정 시각)을 실어 보낸다.
    디바이스는 스케줄을 받아 갔으니 "오늘 08:00분" 이 언제인지 안다.

  ★ spec §8 초안은 { status, occurred_at, taken_source? } 뿐이었다.
    셋 다 "어느 건인지" 를 말해주지 않는다.
    5단계에서 발견했으면 디바이스 파이썬 코드까지 같이 고쳐야 했다.

  → 해당 복약 건이 아직 없으면 서버가 그때 만든다.
    (스케줄대로 미리 만들어두는 크론이 필요 없어진다)

【질문】 인터넷이 끊겨서 3시간 뒤에 보고가 도착했다.
        서버는 "받은 시각"과 "일어난 시각" 중 무엇을 기록해야 하나?
  → 일어난 시각(occurred_at). spec §3.4 마지막 줄이 그렇게 못박았다.
    "서버는 받은 시각을 쓰지 않는다."
    나중에 오프라인 큐(§10)를 붙여 밀린 기록을 한꺼번에 보내도 시각이 안 어긋난다.

  실패 응답:
    401 INVALID_DEVICE_KEY   auth_error         기기 API 키가 틀림
    400 VALIDATION_ERROR     validation_error   필수값 누락, status 값 오류
```

---

## 9. 접근 제어 — 모든 API 공통

`spec.md` 는 "요청한 보호자가 그 기기와 연결돼 있는지 매번 확인한다" 고 되어 있다.

```
【질문】 아래 두 API 는 :id 가 기기가 아니다. 그럼 무엇을 확인해야 하나?

  POST /api/doses/:id/taken
    → 그 복약 건의 device_id 를 꺼내서, 그 기기가 내 것인지 확인
      (doses → devices → user_devices 로 거슬러 올라간다)

  POST /api/notifications/:id/read
    → 그 알림의 user_id 가 나인지 확인
      (notifications 에 user_id 가 직접 있으므로 한 단계로 끝난다)

【질문】 남의 기기 id 로 요청이 왔다. 403 과 404 중 무엇을 줄까?
  → 403 으로 통일. (0번 절에서 정함)
    404 의 장점 = "그런 기기가 존재하는지" 조차 안 알려줘서 더 안전하다.
    403 을 고른 이유 = 디버깅이 쉽다. "없는 건가 / 권한이 없는 건가" 가 구분된다.
    이 프로젝트는 등록코드라는 자물쇠가 따로 있어 존재 여부 노출의 위험이 낮다.

【디바이스도 같다】
  API 키에 묶인 기기의 데이터만 읽고 쓸 수 있다.
  POST /api/device/events 의 scheduled_at 으로 복약 건을 찾을 때도
  "그 키의 기기" 로 범위를 좁혀서 찾아야 한다.
```

### 접근 제어 — 왜 매번 확인해야 하나

**클라이언트가 보낸 id 를 그대로 믿으면 뚫린다.**

```
아들이 자기 앱에서 개발자도구를 열어
  GET /api/devices/남의기기UUID/doses?from=...&to=...
를 보내면?

  → 확인 없이 처리하면 남의 할아버지 복약 기록이 그대로 온다
```

**누구인지는 토큰이 말하고, 무엇을 요청했는지는 URL 이 말한다.**
서버는 매번 **"이 토큰의 주인이 이 기기와 연결돼 있나"** 를 `user_devices` 에서 확인한다.

`CLAUDE.md` 에 **"다중사용자(접근 제어) — ★ 이 프로젝트의 심장"** 이라고 적어둔 부분이
이것이다. 7단계에서 실제로 구현할 때 다시 자세히 다룬다.

---

## 마무리 — 무엇을 배웠나

전부 채웠다. 되돌아보면 API 설계에서 반복해서 나온 질문은 **네 가지**였다.

**① 이 API 가 가져오는 "자원" 이 무엇인가**
화면 이름(`/api/device-detail`)이 아니라 자원 이름(`/api/devices/:id/doses`)으로 짓는다.
그러면 화면 4·6 이 같은 자원을 본다는 게 드러나 **API 하나가 줄었다.**

**② 요청 한 번으로 화면이 다 뜨는가**
필요한 것을 응답에 같이 담는다. 그래서 모든 화면이 **열릴 때 요청 1번**이다.
화면 2 의 배지도 별도 API 대신 `GET /devices` 응답에 얹었다.

**③ 쓰기 API 가 갱신된 것을 돌려주는가**
`taken` / `read` / `medications` 저장이 전부 바뀐 객체를 응답한다.
목록 전체를 다시 안 불러도 화면이 갱신된다.

**④ 없을 때와 많을 때가 정의돼 있는가**
없으면 빈 배열(`404` 아님), 많으면 날짜 범위나 `limit` 으로 자른다.

그리고 **한 번 뒤집은 결정이 두 개** 있었다 — 배지(따로 부르기 → 응답에 얹기),
날짜 계산(서버 → 프론트). 둘 다 **"MVP 에 아직 없는 문제를 미리 풀려던 것"** 이 원인이었다.
`CLAUDE.md` 의 판단 기준 — **"끝까지 완성하는 것이 최우선"** 이 실제로 작동한 지점이다.

### 남은 것

- `spec.md` §8 을 `api-decisions.md` 내용으로 동기화 (§8 은 아직 옛날 초안)
- 4단계 — DB 테이블 설계 + Supabase 생성 (**직접 타이핑 구간**)
