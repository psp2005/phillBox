# 복약기 프로젝트 — 작업 인수인계

> 이 파일은 이 폴더에서 Claude를 열 때 자동으로 읽힌다. 대화가 새로 시작돼도 맥락이 이어지게 하는 용도.

## 프로젝트

노인 복약 관리 시스템. 복약기(라즈베리파이 5)가 음성으로 약을 안내하고 IR 센서·카메라로 복약을 확인해 서버에 보고하면, 보호자가 PWA로 확인한다.

- **화면/기능 명세: `spec.md`** ← 모든 결정의 기준. 작업 전 반드시 읽을 것
- **배포 구성: `deployment-managed.md`** (Vercel + Render + Supabase + FCM)

```
web/     React PWA (Vite + JS + antd-mobile + CSS Modules)
server/  Express (아직 없음)
device/  라즈베리파이 파이썬 (아직 없음)
```

## 사용자에 대해

- Unity/C#은 배우는 중인 초보. **리액트는 실무 경험 있음(React + Zustand).**
- **백엔드 경험 없음.** Node.js 책으로 웹서버·파일저장·MySQL·Express 미들웨어까지 공부함.
- 한국어로 대화. 전문용어는 **그 자리에서 일상 비유로** 풀어줄 것. 용어만 툭 던지지 말 것.
- 막혔을 때 추측으로 이것저것 건드리기보다 **원인부터 좁히는** 진단을 선호함.

## 작업 분담 — ★ 2026-08-26 전면 변경

> **이제부터 코드·설정·터미널 명령은 전부 사용자가 직접 한다. Claude는 설명만 한다.**
> 2026-08-13에 정했던 **"React 퍼블리싱은 Claude가 직접 파일 작성"은 폐기됐다.**

| 구간 | 방식 |
|---|---|
| **모든 파일 작성·수정** (React 화면 포함) | **Claude는 Edit/Write 하지 말 것.** "어느 파일 어디에 무엇을" 을 정확히 알려주고 사용자가 타이핑 |
| **모든 터미널 명령** (`npm run build`, `git`, 배포 등) | **Claude가 대신 실행하지 말 것.** 명령과 "무엇을 봐야 하는지"만 안내 |
| **Claude가 해도 되는 것** | 읽기 전용 조사(파일 읽기·검색, `git status` 같은 상태 확인)와 **문서 갱신**(`spec.md`·`plan.md`·이 파일) |

**설명할 때 반드시 지킬 것 (사용자 요청, 2026-08-26 / 08-28 보강):**
1. **"이게 뭐하는 건지 / 왜 필요한지" 를 먼저** 설명하고, 그다음에 방법
2. 방법만 툭 던지지 않는다. 전문용어는 그 자리에서 일상 비유로 푼다
3. 한 번에 하나씩. 확인받고 다음으로
4. **★ 새 용어를 쓰기 전에 반드시 그 자리에서 뜻을 밝힌다.** 아무리 사소해 보여도 예외 없다.
   - 2026-08-28에 `auth.uid()` · `create policy` · `service_role` · `anon 키` · `BYPASSRLS` · `pg_class` · `relrowsecurity` · "시스템 카탈로그" 를 설명 없이 던져서 지적받았다
   - **왜 문제인가**: 모르는 용어가 나오면 그것부터 되물어야 하고, 그러다 **원래 질문이 무엇이었는지조차 흐려진다.** 설명이 오히려 이해를 방해한다
   - 확실치 않으면 "이 단어는 아시나요?" 라고 묻지 말고 **그냥 한 줄로 풀어 쓴다.** 아는 내용이면 사용자가 넘어가면 된다
5. **어림짐작으로 단정하지 않는다.** ("여러 번 다시 실행하게 됩니다" 처럼 근거 없이 던진 뒤 되물음을 받았다) 왜 그런지 설명할 수 없으면 애초에 쓰지 않는다
6. **★ 코드를 제시하기 전에 그 파일을 반드시 읽는다.** 함수 시그니처·props 이름·컬럼 이름을 **기억으로 적지 않는다.**
   - 2026-09-13에 `DeviceListPage` 의 props 를 기억으로 적어 **`unreadCount`(실제로는 `TabBarLayout` 의 props)를 넣고 `onRefresh` 를 빠뜨려** 지적받았다
   - "다를 수 있습니다" 라는 단서를 달 바에는 **읽고 정확히 적는다.** 읽기는 허용된 작업이고 비용도 거의 없다
   - 사용자는 그 코드를 **그대로 타이핑**한다. 틀린 코드를 주면 타이핑·디버깅 시간을 통째로 낭비시킨다

퍼블리싱과 데이터 연결의 경계, 지켜야 할 규칙 3개는 **`spec.md` §6** 에 있음.

## 공부하지 않고 넘어간 것 — 필요해지면 그 자리에서 설명할 것

Node.js 책의 아래 챕터를 **의도적으로 건너뛰었다.** "책에서 배웠겠지"라고 가정하지 말고, 해당 개념이 코드에 등장하는 순간 짧은 비유로 먼저 설명한 뒤 진행할 것.

- **쿠키와 인증** — `Secure`/`HttpOnly`. 토큰 저장 위치를 정할 때 필요
- **세션과 인증** — 이 프로젝트는 세션이 아니라 토큰 방식(Supabase Auth)이라 대부분 불필요
- **Passport.js** — Supabase Auth가 대체하므로 안 씀
- **다중사용자(접근 제어)** — ★ 이 프로젝트의 심장. 보호자 A가 보호자 B의 기기를 못 보게 막는 로직(`spec.md` §8.3)을 짤 때 **반드시 개념부터 설명할 것**

> 왜 건너뛰었나: 책은 서버가 HTML을 렌더링하는 세션/쿠키 방식(MPA) 전제인데, 이 프로젝트는 React(Vercel) ↔ Express(Render)가 다른 도메인에서 JSON을 주고받는 토큰 방식이다. 구조가 달라 그대로 배우면 오히려 헷갈린다.

## 책에 없어서 반드시 막힐 4가지 — 해당 단계에 닿으면 꺼내 설명할 것

미리 몰아서 가르치지 말 것. 사용자는 한 번에 하나씩 진행하는 방식을 선호한다.

1. **JSON API 만들기** — 책은 `res.send(HTML)`, 이 프로젝트는 `res.json({...})`. `fetch`로 받는 쪽은 이미 익숙하고 만드는 쪽만 처음이다
2. **CORS** — Vercel ↔ Render 도메인 통신을 브라우저가 막는 문제. `cors` 패키지 한 줄이지만 모르면 몇 시간을 잃는다
3. **`.env` 환경변수** — Supabase service_role 키를 코드에 박지 않고 관리하기
4. **디바이스 인증** — 브라우저가 없어 이메일/비번을 못 쓴다. 기기별 API 키 발급 방식. **책 어디에도 없다**

## 판단 기준

- **DB 컬럼 추가 = 싸다** (지금 해두면 나중에 테이블 갈아엎기를 막음)
- **화면 / 외부 서비스 추가 = 비싸다** (뒤로 미룰 수 있는지 먼저 물을 것)
- 최우선 목표는 **끝까지 완성하는 것.** 좋아 보이는 기능은 `spec.md` §10으로 미룬다

## 문서 지도 — 어느 문서가 무엇을 담나

| 문서 | 담는 것 | 언제 읽나 |
|---|---|---|
| **`spec.md`** | **무엇을 만드나** — 화면·API·DB 명세. **모든 결정의 기준** | 작업 전 항상 |
| **`plan.md`** | **언제 만드나** — 4주 일정, 주차별 목표, 겪은 함정 기록 | 다음 할 일을 정할 때 |
| `deployment-managed.md` | 배포 구성 (Vercel + Render + Supabase + FCM) | 배포 단계에서 |
| `api-decisions.md` / `api-worksheet.md` | API 명세가 왜 그 모양인지 (결정 과정) | 명세가 이해 안 될 때 |
| `Memo.md` | `web/` 파일 지도 | 화면 코드를 찾을 때 |
| `withAi.md` | AI와 협업한 방식 기록 | — |

## 현재 위치 (2026-08-26 갱신)

**시연이 2026-09-25 경으로 잡혀 남은 기간 약 4주.** 일정과 컷 라인은 `plan.md`에 있다.

`spec.md` §9 개발 순서 기준:

| 단계 | 상태 |
|---|---|
| 1 명세 확정 | ✅ |
| 2 화면 7장 퍼블리싱 (목업) | ✅ |
| 3 API 응답 모양 고정 | ✅ |
| **0주차 — 깃 + GitHub** | ✅ **2026-08-26 완료** — https://github.com/psp2005/phillBox |
| **1주차 — Vercel 배포** | ✅ **2026-08-26 완료** — https://phill-box.vercel.app (Root Directory `web`, 깃 푸시 → 자동 배포) |
| **1주차 — PWA 설치** | ✅ **2026-08-26 완료** — `public/manifest.json` + 아이콘 3장. 폰 홈 화면에서 **주소창 없이** 열리는 것 확인 |
| **1주차 — Supabase + DB 테이블 (4단계)** | ✅ **2026-08-29 완료** — 프로젝트 `pillbox`(Seoul) + 테이블 5개. SQL 원본은 `server/sql/001_init.sql` |
| **1주차 — `server/` 최소 Express + Render 배포** | ✅ **2026-08-30 완료** — https://pillbox-server-wdjx.onrender.com/api/health → `{"ok":true}` |
| **2주차 — 앱용 서버 API 8개 + Swagger (5단계)** | ✅ **2026-09-12 완료.** 인증 없이 `DEV_USER_ID` 하드코딩 · `/api-docs` 에서 전부 시험 가능 |
| **3주차 — 화면 연결 + 인증 (6·7단계)** | ⏳ **지금 여기** |
| 8 디바이스 (파이썬 + 디바이스용 API 2개) | ⬜ 4주차 |

**서버 구조 (2026-09-12)**

```
server/
├─ index.js              app.use 로 라우터를 끼우는 목차
├─ db.js                 pg 연결 풀 (Session pooler + ssl)
├─ errors.js             fail(res, status, code, message) — type 은 표에서 자동
├─ swagger.js            @openapi 주석을 읽어 /api-docs 생성
├─ sql/                  001_init.sql (스키마) · dev_seed.sql (개발용 데이터)
└─ routes/
   ├─ devices.js         5개 + requireMyDevice 미들웨어 (§8.5 접근 제어)
   ├─ notifications.js   2개
   └─ user_devices.js    1개
```

- **접근 제어는 `requireMyDevice` 한 곳에서만** 한다. 통과하면 `req.device` 에 Device 정보를 담아 넘기므로 라우트에서 다시 조회하지 않는다
- **`POST /api/devices/:id/doses/taken`** — 명세의 원래 경로(`POST /api/doses/:id/taken`)에서 바뀌었다. 이유는 `spec.md` §8.2 8번
- **오류 형식 통일이 남아 있다.** `user_devices.js` 만 `fail()` 을 쓰고 나머지는 `{ error: { message } }`
| **(곁가지) 라즈베리파이 실물 셋업** | 🔶 **2026-09-04 — SSH 접속 + IR센서→LED 예행연습까지 완료.** 상세는 `plan.md` 4주차 "라즈베리파이 실물 셋업" |

**라즈베리파이 (2026-09-15 기준)** — `ssh academy@192.168.0.8` (호스트명 `pillbox`. **`.local` 이름은 WSL에서 안 되니 IP를 쓴다**)
- ✅ **시계** — NTP(123번) 차단이라 **HTTP `Date` 헤더로 부팅 시 자동 맞춤** (`http-timesync.service`). 검증은 `journalctl -u http-timesync -b`
- ✅ **스피커** — 블루투스 스피커 + `mpg123` (`~/audio/m1~m3.mp3`)
- **하드웨어 구성이 바뀌었다** — 아두이노 Mega 2560(USB 시리얼 `/dev/ttyACM0`)에 **요일별 7칸 IR·LED**, USB 웹캠. 핀 배치·정해 둔 동작은 `plan.md` 4주차 "요일별 칸 배치"
- **다음 할 일** — 목데이터 시간표로 복약 흐름 만들기 (메가 스케치를 "LED는 파이 명령으로" 분리하는 것부터)

**배포처 주소 (2026-08-30 기준)**

| | 주소 | Root Directory |
|---|---|---|
| 프론트 (Vercel) | https://phill-box.vercel.app | `web` |
| 서버 (Render) | https://pillbox-server-wdjx.onrender.com | `server` |
| DB (Supabase) | https://ptloyqxytijidbwbmeco.supabase.co | — |

**셋 다 무료 플랜이라 잠든다.** Render는 15분 무접속 시(재시작 30초+), Supabase는 일주일 무접속 시(Resume 1~2분). **시연 전날 Supabase, 시연 5분 전 Render를 깨울 것.**

**Ⓐ 라우터 완료 (2026-09-13).** `react-router` 도입으로 화면 7장에 주소가 생겼고, **안드로이드 뒤로가기 문제가 해결**됐다.

- 화면 컴포넌트 7장은 **안 건드렸다.** `App.jsx` 에 화면마다 얇은 껍데기(`LoginScreen`·`DeviceListScreen` …)를 두고 그 안에서 `useNavigate`·`useParams` 를 쓴다. **Ⓑ에서 `fetch` 가 들어갈 자리가 그 껍데기다**
- 하단 탭(화면 2·7)은 **중첩 라우트 + `<Outlet />`** 으로 공유한다(`TabLayout`). 화면 3·4·5·6은 그 바깥이라 탭이 안 보인다
- **히스토리 규칙** — 안으로 들어가기는 기본(쌓기), **되돌아 나오기·완료 후는 `navigate(-1)`**, 다른 곳으로 떠나기(로그인·로그아웃·탭 전환)는 `{ replace: true }`. 완료 후에 `replace` 를 쓰면 **같은 주소가 두 칸 쌓여** 뒤로가기를 두 번 눌러야 하므로 `-1` 을 쓴다
- `web/vercel.json` 의 rewrite 가 없으면 **배포본에서 새로고침 시 404** 가 난다 (로컬 Vite 개발 서버는 알아서 처리해줘서 안 보인다)
- 개념 정리 문서: `docs/react-router-notes.html`

**Ⓑ 진행 중 (2026-09-14)** — 준비 3개(`VITE_API_URL`·`lib/api.js`·CORS) + **화면 2(기기 목록) 연결 완료**, 배포본에서도 확인. 다음은 **화면 4(기기 상세)**. 상세 기록은 `plan.md` Ⓑ "진행 기록"

- 껍데기는 이제 `App.jsx` 가 아니라 **`web/src/screens/~Screen.jsx`** 에 있다
- Vercel 대시보드에 `VITE_API_URL` 이 들어가 있다. **`VITE_` 값을 바꾸면 반드시 Redeploy**
- 서버 CORS 허용 출처는 `server/index.js` 에 하드코딩 (로컬 5173 + Vercel 주소)

**아직 안 되는 것:** 앱을 껐다 켜면 로그인 화면부터 시작하고, `/devices` 를 주소창에 직접 쳐도 들어가진다. **Ⓒ(인증)에서 해결** — `plan.md` 참고.

**최근에 바뀐 것 (2026-08-25~26):**
- **보호자 공동 관리를 MVP에 포함**했다 → `nickname`·`patient_phone`이 `devices`가 아니라 **`user_devices`** 컬럼이 됐다. `spec.md` §7·§8.6 참고. **§8.1 공통 객체 4개의 응답 모양은 안 바뀌었다**
- 화면 3 등록 폼에 **환자 전화번호(선택)** 칸이 추가됐다
- **왜 배포를 9단계에서 1주차로 앞당겼나** — 배포는 처음 하면 반드시 막힌다(환경변수·빌드·경로·CORS). 그 문제를 **코드가 단순할 때** 만나야 원인을 좁힐 수 있다. 서버 코드를 다 짜고 첫 배포를 하면 "내 코드 문제인가 / 설정 문제인가"를 못 가른다. 자세한 이유는 `plan.md` §0
