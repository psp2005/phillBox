# 복약기 프로젝트 — 화면/기능 명세 (MVP)

> 작성 2026-08-13. 배포 구성은 `deployment-managed.md` 참고.
> **원칙: 끝까지 완성하는 것이 최우선.** 좋아 보여도 지금 안 해도 되는 것은 전부 §10(나중)으로 뺀다.
> 판단 기준 — **DB 컬럼 추가는 싸다**(나중에 테이블 갈아엎기를 막아줌) / **화면·외부서비스 추가는 비싸다**(뒤로 미룰 수 있는지 먼저 확인).

---

## 1. 한 줄 요약

복약기(라즈베리파이 5)가 정해진 시간에 음성으로 약을 안내하고, IR 센서와 카메라로 복약 여부를 확인해 서버에 보고한다.
보호자는 PWA로 **여러 대의 기기**를 등록해 복약 현황과 놓친 기록을 확인하고, 필요하면 전화하거나 직접 복용 처리를 한다.

```
[복약기]  --REST-->  [Express 서버]  <--REST--  [보호자 PWA(React)]
                          |
                      [Supabase = PostgreSQL]
```

---

## 2. MVP 범위 확정

### 이번에 만드는 것
- 보호자 이메일 회원가입 / 로그인
- 기기 등록 (일련번호 + 등록코드)
- 기기 1대당 **약 1종, 하루 1회** 복약 설정
- 복약 상태 자동 기록 (예정 → 안내 → 꺼냄 → 복용/놓침)
- 주간 현황 / 복약 기록 / 알림 목록 조회
- 보호자의 수동 복용 처리(manual)
- 환자에게 전화 걸기
- **카메라 복용 판정 (MediaPipe · 손목이 입 근처로 가는지)** — 디바이스 개발의 마지막 단계

### 의도적으로 뺀 것 (§10에 이유와 함께)
- FCM 푸시 발송 · 외출/알림 일시정지 · 소셜 로그인
- 약 여러 종 / 하루 여러 번 복용 · 디바이스 오프라인 큐

> ⚠️ **뺀 것들이 나중에 붙을 자리는 이 명세에 미리 만들어 둔다.** 나중에 화면이나 테이블을 갈아엎지 않기 위해서다.

---

## 3. 용어와 상태 (모든 화면·API가 이 단어를 공유한다)

### 3.1 설정표 vs 출석부 — 반드시 구분

| | 약 설정 (medication) | 복약 건 (dose) |
|---|---|---|
| 뜻 | "매일 08시에 감기약 1정" | "8월 13일 08시분" 낱개 기록 |
| 개수 | 기기당 1줄, 잘 안 바뀜 | **날마다 1줄씩 쌓임** |
| 상태 | 없음 | scheduled/notified/... 가 여기 붙음 |
| 비유 | 학기 초에 짜는 시간표 | 날마다 도장 찍는 출석부 |

화면 5는 시간표를 고치고, 화면 4·6·7은 출석부를 본다.

### 3.2 복약 건의 상태 5단계

| 값 | 뜻 | 누가 바꾸나 |
|---|---|---|
| `scheduled` | 예정. 아직 시간이 안 됨 | 서버(생성 시 기본값) |
| `notified` | 음성 안내를 했음 | 디바이스 보고 |
| `dispensed` | 약을 꺼냈음 (IR 감지) | 디바이스 보고 |
| `taken` | 복용 완료 | 디바이스 보고 또는 보호자 수동 |
| `missed` | 놓침 | 디바이스 보고 |

```
scheduled ──복약시각──> notified ──IR감지──> dispensed ──카메라 판정──> taken
                           │                    │                       ↑
                    5분 내 안 꺼냄        5분 내 안 먹음           보호자 수동 체크
                           └────────> missed <──┘                       │
                                        └──────────────────────────────┘
```

### 3.3 상태와 함께 기록하는 값

`missed`는 두 종류인데 상태값만으로는 구분이 안 되므로 **시각을 같이 남긴다.**

| 컬럼 | 뜻 |
|---|---|
| `notified_at` | 음성 안내한 시각 |
| `dispensed_at` | 칸이 열린 시각. **비어 있으면 "꺼내지도 않음"** |
| `taken_at` | 복용 확인 시각 |
| `taken_source` | `ir` / `camera` / `manual` — 무엇이 복용을 판정했나 |

- `dispensed_at`이 **비었는데** `missed` → "약을 안 꺼내셨어요"
- `dispensed_at`이 **있는데** `missed` → "꺼내셨는데 아직 안 드셨어요"
- `taken_source`는 카메라가 완성되기 전에는 `ir`(꺼냈으면 복용으로 간주)과 `manual`만 쓰인다. 카메라가 붙으면 값 하나가 늘 뿐, 서버·앱 코드는 바뀌지 않는다.

### 3.4 시간 처리 규칙 (어기면 반드시 터짐)

- DB에는 **UTC**로 저장 (`timestamptz`)
- 화면에는 **한국시간(KST)** 으로 변환해 표시
- 복약 예정 시각처럼 "매일 08:00" 같은 벽시계 값은 **시각(`time`) + 기기 타임존** 으로 저장하고, 그날의 복약 건을 만들 때 UTC 시각으로 계산한다
- **디바이스는 이벤트를 보낼 때 "실제로 일어난 시각"을 같이 실어 보낸다. 서버는 "받은 시각"을 쓰지 않는다.** (나중에 오프라인 큐를 붙여도 기록이 안 어긋나게 하려는 것)

---

## 4. 디바이스 동작 흐름

**5분 타이머는 디바이스가 잰다.** 서버는 판정하지 않고 보고를 받아 적기만 한다.
(서버가 재려면 계속 시계를 보는 크론이 필요해 복잡해지고, 인터넷이 끊기면 음성 안내조차 못 하게 된다.)

1. 부팅 시 · 이후 주기적으로 서버에서 **자기 기기의 복약 스케줄**을 받아온다
2. 복약 시각 도달 → **음성 안내** → 서버에 `notified` 보고
3. 5분 내 IR 감지 없음 → `missed` 보고 (`dispensed_at` 없음)
4. IR 감지 → `dispensed` 보고 → **카메라 켜기**
5. 카메라가 5분 내 복용 감지 → `taken` (`taken_source=camera`) / 감지 못하면 → `missed` 보고

### 4.1 디바이스 개발 순서 (앱·서버와 독립)

| | 내용 | 없으면? |
|---|---|---|
| ① | 스케줄 받아오기 + 음성 안내 + `notified` 보고 | 여기까지만 돼도 서버·앱 전체를 테스트할 수 있다 |
| ② | IR 센서 → `dispensed` 보고 | — |
| ③ | **카메라 + MediaPipe → `taken` 판정** | ②까지가 폴백. 꺼냄을 곧 복용으로 간주(`taken_source=ir`)하면 제품은 굴러간다 |

**카메라 판정 방식:** MediaPipe로 손목과 입의 좌표를 얻어 **둘 사이 거리가 일정 값 이하로 가까워졌는지**만 본다. "약을 삼켰는지"는 판정하지 않는다.

**주의할 점**
- **미탐이 오탐보다 아프다.** 실제로 드셨는데 손목이 프레임 밖이라 못 잡으면 보호자에게 잘못된 알림이 간다 → 그래서 보호자의 **수동 체크(manual)가 안전망**이다
- 카메라는 `dispensed` 이후 **5분 동안만** 켠다 (성능·프라이버시)
- 초당 5프레임이면 충분하다. 라즈베리파이 5(16GB)는 성능이 남는다
- 실제 병목은 **조명**과 **카메라 화각** — 손목과 얼굴이 한 화면에 들어와야 한다

---

## 5. 화면 명세 (7장 + 공용 팝업 1개)

### 화면 1 — 로그인 / 회원가입

| 항목 | 내용 |
|---|---|
| 구성 | 이메일 / 비밀번호 입력, [로그인], [회원가입] |
| 동작 | 로그인 성공 → 화면 2로 이동 |
| 인증 | Supabase Auth (이메일+비밀번호). 발급받은 토큰을 서버 API 호출 시 헤더에 실어 보낸다 |
| 비고 | "또는 소셜 로그인" 자리만 남겨둠(§10). 나중에 추가해도 화면 구조는 안 바뀜 |

### 화면 2 — 기기 목록 (첫 화면)

| 항목 | 내용 |
|---|---|
| 구성 | 등록된 기기 배너 목록, 우상단 [+ 등록] |
| 배너 1개 | 기기 별명(예: 할아버지 기기), 일련번호, **오늘 상태 아이콘** |
| 동작 | 배너 탭 → 화면 4 / [+ 등록] → 화면 3 |
| 비고 | 등록된 기기가 없으면 안내 문구 + 등록 버튼만 표시 |

### 화면 3 — 기기 등록

| 항목 | 내용 |
|---|---|
| 구성 | **일련번호** 입력, **등록코드(6자리)** 입력, 기기 별명(선택), **환자 전화번호(선택)**, [등록하기] [취소] |
| 안내문 | "일련번호와 등록코드는 기기 뒷면 스티커에 있습니다" |
| 동작 | 서버가 둘 다 맞는지 확인 → 보호자-기기 연결 생성 → 화면 2로 |
| 실패 | 일련번호/코드 불일치, **내가** 이미 등록한 기기 → 오류 메시지 |
| **별명·전화번호는 보호자별 값** | 같은 기기를 공동 관리해도 아들은 "할아버지 기기", 딸은 "아버지 기기"로 다르게 부를 수 있다. `user_devices`에 저장한다(§7) |
| **왜 코드가 필요한가** | 일련번호만으로 등록되면 옆번호를 찍어 남의 복약 기록을 볼 수 있다. 주소(일련번호)와 열쇠(코드)를 함께 요구한다 |

### 화면 4 — 기기 상세 (이번 주)

| 항목 | 내용 |
|---|---|
| 헤더 | `{기기 별명} - 이번주` |
| 주간 현황 | 월~일 7칸. 상태 색 + **아이콘** 표시 (§6.4) |
| 칸 탭 | **공용 팝업** 열기 (아래) |
| 버튼 | **[약 설정]** → 화면 5 / **[복약 기록]** → 화면 6 |
| 전화 | 환자 전화번호가 등록돼 있으면 [전화 걸기] (`tel:` 링크) |
| 비고 | 약이 1종·1회이므로 "약 등록"과 "시간 설정"을 **[약 설정] 하나로 통합** |

### 공용 팝업 — 복약 상세 (화면 4·6·7에서 공유)

```
┌──────────────────────┐
│  8월 12일 (목)        │
│  예정   08:00         │
│  꺼냄   08:03         │
│  복용   —             │
│  상태   ⚠ 놓침         │
│                      │
│ [먹었어요로 표시] [닫기]│
└──────────────────────┘
```

- [먹었어요로 표시] → 확인 후 `taken` + `taken_source=manual` + `taken_at=현재시각`
- **미래 날짜면 [먹었어요로 표시] 버튼을 감춘다** (아직 오지 않은 복약을 체크할 수 없음)
- 이미 `taken` 이면 버튼 대신 완료 표시 + 판정근거(자동/수동) 표기
- **한 번 만들어 세 화면에서 재사용한다**

### 화면 5 — 약 설정

| 항목 | 내용 |
|---|---|
| 구성 | 약 이름, 용량(예: 1정), **복용 시간**(1개), **요일 선택**(월~일 체크), [저장] |
| 동작 | 저장 시 기존 설정을 덮어씀(기기당 1줄) |
| 비고 | 칸 번호 입력 없음 — 약이 1종이라 칸도 하나 |
| 반영 | 저장 이후의 복약 건부터 적용. 이미 생성된 과거 건은 바뀌지 않는다 |

### 화면 6 — 복약 기록

| 항목 | 내용 |
|---|---|
| 구성 | 날짜 내림차순 목록. `8월 12일 (목) 08:00 · ✅ 복용 08:03` 형태 |
| 표시 | ✅ 복용완료 / ❌ 놓침(꺼내지도 않음) / ⚠️ 놓침(꺼냈으나 미복용) / ⏳ 예정 |
| 항목 탭 | **공용 팝업** 열기 → 지난 날짜의 수동 체크도 여기서 |
| 범위 | 최근 30일. 더 필요하면 [더 보기] |

### 화면 7 — 알림 목록

| 항목 | 내용 |
|---|---|
| 구성 | **시간순 한 줄 목록** (기기별 묶음 아님 — 기기가 몇 대 안 되므로 묶으면 오히려 불편) |
| 한 줄 | `🔴 할아버지 기기 · 약을 안 꺼내셨어요 · 08:05` |
| 안 읽음 | 안 읽은 항목은 강조 표시. 하단 탭 아이콘에 **빨간 배지** |
| 항목 탭 | 읽음 처리 + **공용 팝업** 열기 |
| 갱신 | 앱이 열려 있는 동안 **30초마다 새 알림 조회(폴링)** |

> **★ 푸시와 알림 목록은 별개다.**
> FCM 푸시는 던지면 끝이라 사본을 남기지 않는다(문자를 보내도 내 폰에 발신함이 자동으로 생기지 않는 것과 같다).
> 그래서 화면 7이 읽어갈 **알림 테이블**이 서버에 따로 필요하다.
> 서버는 `missed` 판정을 받으면 **① 알림 테이블에 저장** 한다. 나중에 여기에 **② 푸시 발송** 한 줄만 추가하면 된다(§10).
>
> **★ 한 줄이 아니라 "그 기기에 연결된 보호자 수만큼" 저장한다.**
> `read_at`(읽음 표시)이 사람마다 달라야 하기 때문이다 — 아들이 읽었다고 딸의 빨간 배지가 사라지면 안 된다.
> 편지를 사람 수만큼 복사해 각자 우편함에 넣는 것과 같다.
> ```sql
> INSERT INTO notifications (user_id, device_id, dose_id, type, created_at)
> SELECT user_id, ?, ?, ?, now() FROM user_devices WHERE device_id = ?;
> ```

### 화면 이동 지도

```
[1 로그인] → [2 기기 목록] ─┬─ [+등록] → [3 기기 등록]
                            └─ 배너탭  → [4 기기 상세] ─┬─ [약 설정] → [5 약 설정]
                                            │            └─ [복약기록] → [6 복약 기록]
                                            └─ 요일칸 탭 → (공용 팝업)
              [7 알림 목록] ← 하단 탭 어디서나 → 항목 탭 → (공용 팝업)
```

---

## 6. 프론트엔드 작업 규칙

### 6.1 기술 선택

| 항목 | 선택 |
|---|---|
| 언어 | **JavaScript** (TypeScript 아님) |
| 빌드 | Vite + React |
| 컴포넌트 | **antd-mobile 우선**, 없는 것만 antd(ant design) |
| 스타일 | **CSS Modules** (Vite 기본 지원, 추가 설치 없음) |
| 테마 | **라이트(화이트)** |
| 상태관리 | Zustand (데이터 연결 단계에서 도입) |

### 6.2 작업 경계 — 퍼블리싱 / 데이터 연결

| 항목 | 담당 |
|---|---|
| 화면 마크업·스타일, 컴포넌트 분리 | 퍼블 |
| 폼 입력값(controlled input), 형식 검증(빈칸·이메일 모양) | 퍼블 |
| 팝업 열림/닫힘, 탭 선택 등 화면 안에서만 사는 상태 | 퍼블 |
| 화면당 4가지 상태 화면 (정상 / 비어있음 / 로딩 / 오류) | 퍼블 |
| 하단 탭 고정, 안전영역(노치), 스크롤 영역, 전환 애니메이션 | 퍼블 |
| **데이터 fetch, 상태관리(Zustand), 라우터** | 데이터 연결 |
| **서버 검증 결과 처리**(중복 이메일, 등록코드 불일치 등) | 데이터 연결 |

### 6.3 경계를 지키는 규칙 3개

**규칙 1 — 화면 컴포넌트 안에는 `fetch`도 `store`도 없다. 전부 props로 받는다.**

```jsx
// pages/DeviceListPage.jsx
export default function DeviceListPage({ devices, loading, error, onSelectDevice, onAddDevice }) {
  // 화면 그리기만. 데이터를 어디서 가져오는지 모른다.
}
```

데이터 주입은 `App.jsx` 한 곳에서만 한다. 목업을 실제 데이터로 바꿀 때 그 파일만 고치면 된다.

**규칙 2 — 목업 데이터의 모양은 §8의 API 응답 모양과 똑같이 만든다.**
그래야 나중에 `fetch` 결과를 그대로 꽂았을 때 화면을 다시 손볼 일이 없다.

**규칙 3 — 모든 동작은 `on___` props로 밖에 뚫어둔다.**
버튼을 눌렀을 때 무슨 일이 일어날지(라우터 이동 / store 호출)는 퍼블 단계에서 정하지 않는다.
데모용 화면 전환은 `App.jsx`의 `useState` 하나로 처리하고, 나중에 라우터로 교체할 때도 그 파일만 고친다.

### 6.4 색과 아이콘

| 상태 | 색 | 아이콘 |
|---|---|---|
| 복용완료 `taken` | `--adm-color-success` (초록) | ✓ |
| 놓침 `missed` | `--adm-color-danger` (빨강) | ⚠ |
| 진행중 `notified`/`dispensed` | `--adm-color-warning` (주황) | ⏳ |
| 예정 `scheduled` | 회색 | · |

**색만으로 구분하지 않는다. 반드시 아이콘을 같이 표시한다.**
적록색약이면 초록/빨강이 구분되지 않는데, 이 앱의 사용자는 중장년층 보호자일 가능성이 높다.

### 6.5 화면 → antd-mobile 컴포넌트 매핑 (초안 · 구현 직전 공식 문서로 검증)

| 화면 | 컴포넌트 |
|---|---|
| 공통 | `NavBar` `TabBar` `SafeArea` |
| 1 로그인 | `Form` `Input` `Button` |
| 2 기기 목록 | `List` `PullToRefresh` `ErrorBlock`(빈 상태) |
| 3 기기 등록 | `Form` `Input` `Button` `Toast` |
| 4 기기 상세 | `Grid`(요일 7칸) `Card` `Button` |
| 공용 팝업 | `Dialog` 또는 `CenterPopup` |
| 5 약 설정 | `Form` `Input` ~~`DatePicker`~~ **`Picker`**(시간) ~~`Checkbox.Group`~~ **`Selector`**(요일) |
| 6 복약 기록 | `List` |
| 7 알림 목록 | `List` `Badge` `PullToRefresh` |

#### 검증 결과 초안에서 바뀐 것 (2026-08-13, 화면 5 구현 시점)

| 항목 | 초안 → 실제 | 이유 |
|---|---|---|
| 복용 시간 | `DatePicker` → **`Picker`** (시/분 2칸) | antd-mobile 의 `DatePicker` 는 `precision` 이 **연 → 월 → 일 → 시 → 분** 순서로 칸을 *누적*하는 구조라, 앞의 날짜 칸을 빼고 "시각만" 고르게 만들 수 없다. `Picker` 로 시(00~23)·분(5분 단위) 2칸을 직접 구성했다 |
| 복용 요일 | `Checkbox.Group` → **`Selector`** (`multiple`) | 체크박스 7개는 세로로 쌓여 자리를 많이 먹는다. `Selector` 는 월~일 7개가 알약 모양으로 한 줄에 들어간다 |

> **`Picker` 를 `Form.Item` 안에 넣을 때 주의** — `Picker` 는 입력칸이 아니라 팝업이라 두 가지를 손으로 지정해야 한다.
> - `trigger="onConfirm"` — `Form.Item` 은 기본적으로 자식의 `onChange` 를 듣는데 `Picker` 에는 `onChange` 가 없다(`onSelect`/`onConfirm` 뿐). 어느 이벤트의 값을 폼에 넣을지 지정한다
> - `onClick={(e, ref) => ref.current?.open()}` — 팝업을 여는 것은 값이 아니라 명령이라 `ref` 로 호출한다. `Form.Item` 이 자식의 ref 를 두 번째 인자로 넘겨준다
>
> 반대로 값을 **내려주는** 쪽(`value`)은 기본값 그대로 맞으므로 `valuePropName` 은 건드릴 필요가 없다.

---

## 7. 저장할 데이터 (PostgreSQL · 다음 단계에서 상세 설계)

**DB는 PostgreSQL이다.** Supabase가 PostgreSQL만 제공하므로 선택의 여지가 없다.
공부한 MySQL과 95% 동일하며, 주요 차이는 `AUTO_INCREMENT`→`IDENTITY`, 백틱→큰따옴표, `DATETIME`→`timestamptz`, `IFNULL`→`COALESCE`, `ON DUPLICATE KEY UPDATE`→`ON CONFLICT` 정도다.
`timestamptz`는 §3.4의 시간 규칙을 DB가 대신 지켜주므로 오히려 이득이다.

| 테이블 | 담는 것 | 핵심 항목 |
|---|---|---|
| `users` | 보호자 계정 | Supabase Auth가 관리 |
| `devices` | 복약기 | 일련번호, 등록코드, 타임존, 기기 API 키 |
| `user_devices` | **보호자 ↔ 기기 연결** | user_id, device_id, **별명**, **환자 전화번호** |
| `medications` | 약 설정(시간표) | device_id, 약이름, 용량, 복용시각, 요일 |
| `doses` | 복약 건(출석부) | device_id, 예정일시, status, notified_at, dispensed_at, taken_at, taken_source |
| `notifications` | 알림 | user_id, device_id, dose_id, 종류, 생성시각, **read_at** |

**`user_devices`를 왜 따로 두나:** "아들과 딸이 할아버지를 공동 관리"를 **MVP에 포함**하기 때문이다. 기기 1대에 보호자 여러 명이 붙으므로 연결을 적을 자리가 따로 있어야 한다. `devices`에 `user_id` 컬럼 하나를 넣는 방식이었다면 기기 1대에 보호자 1명만 가능해 나중에 테이블을 갈아엎어야 했다.

**어느 컬럼이 어느 테이블에 가나 — 기준은 "기기의 성질인가, 보호자가 붙인 이름표인가":**

| 값 | 테이블 | 이유 |
|---|---|---|
| `serial` · `registration_code` · `timezone` · `device_api_key` | `devices` | 누가 보든 같은, **기기 자체**의 성질 |
| **`nickname` · `patient_phone`** | `user_devices` | **보호자마다 다를 수 있는 값** |

아파트 동호수(`serial`)는 누가 봐도 `101동 1502호`지만, 그 집을 뭐라 부르는지는 아들("본가")과 딸("친정")이 다르다. **동호수는 건물에, 호칭은 각자 수첩에** 적는다.

전화번호를 `user_devices`에 두는 이유는 하나 더 있다 — **한 사람의 오타가 다른 보호자에게 전파되지 않는다.** 각자 자기 줄에 쓰므로 아들이 잘못 입력해도 딸의 화면은 멀쩡하다. (등록 후 수정하는 API는 없다. §10 참고)

> **API 응답의 `Device` 객체 모양은 이 이사와 무관하게 그대로다**(§8.1). 서버가 두 테이블을 JOIN해 "이 보호자 시점의 Device"로 조립해 내려준다. 식당에서 나온 접시는 똑같고, 주방에서 어느 냉장고에서 재료를 꺼냈는지만 달라진 것이다.
> ```sql
> SELECT d.id, d.serial, ud.nickname, ud.patient_phone, d.timezone
> FROM user_devices ud JOIN devices d ON d.id = ud.device_id
> WHERE ud.user_id = ?;
> ```

---

## 8. API 명세 (확정 · 2026-08-23)

> 화면 7장을 실제로 만든 뒤 §9의 3단계로 확정했다.
> **여기 적힌 응답 모양이 계약이다.** 4단계 테이블 설계와 5단계 서버 구현은 이 모양을 만들 수 있게 짜면 된다.
> 결정 과정과 이유는 `api-worksheet.md`, 같은 내용의 짧은 버전은 `api-decisions.md`.
> 초안에서 바뀐 것은 §8.6.

### 8.0 모든 API 공통 규칙

| 항목 | 규칙 |
|---|---|
| Base URL | `https://<render 주소>/api` (개발 중엔 `http://localhost:3000/api`) |
| 앱 인증 | `Authorization: Bearer <Supabase access token>` |
| 디바이스 인증 | `X-Device-Key: <기기 API 키>` |
| 요청·응답 형식 | `application/json` |
| **모든 시각** | **UTC ISO 8601** (`"2026-08-13T23:00:00Z"`). KST 변환은 화면이 한다 (§3.4) |
| **예외 — 복용 시각** | `Medication.time`만 벽시계 값(`"08:00"`). 아래 §8.1 참고 |
| **모든 id** | **문자열**로 내려준다. JS의 숫자 정밀도 문제를 피하기 위해 |
| 전화번호 | 숫자만 저장·전달 (`"01012345678"`). 하이픈은 화면이 붙인다 |
| 응답 최상위 | **항상 객체로 감싼다** |
| **목록 표현** | **하나만 돌려줄 때도 배열에 담는다** — `{ "devices": [ {...} ] }` |
| **"없음" 표현** | `null`이 아니라 **빈 배열** — `{ "medications": [] }` |
| 404 vs 빈 배열 | 경로가 가리키는 **자원 자체**가 없으면 `404` / 그 자원의 **부속물**이 없으면 `200` + `[]` |
| 자원 vs 조건 | 자원은 경로, 거르는 조건은 쿼리스트링 |

**왜 하나여도 배열인가:** 클라이언트 코드가 항상 같아진다. `devices.map()`이 어디서나 안전하고, "없음"을 `null` 체크 없이 `length === 0`으로 판단할 수 있다.

#### 오류 응답 (모든 API 공통)

**오류 본문 JSON 모양에는 표준이 없다.** (표준인 것은 HTTP 상태 코드뿐) 이 프로젝트는 Stripe 방식을 따라 세 칸으로 나눈다.

```json
{
  "error": {
    "type": "validation_error",
    "code": "INVALID_CODE",
    "message": "등록코드가 일치하지 않습니다"
  }
}
```

| 필드 | 누가 읽나 |
|---|---|
| `message` | **사용자** — 화면 상단 오류 띠에 **그대로 출력**할 한국어 |
| `code` | 프로그램 — **정확히 이 오류일 때만** 분기 |
| `type` | 프로그램 — **비슷한 오류를 묶어** 같은 대응 |

문구(`message`)로 분기하면 문구를 고치는 순간 코드가 깨진다. 그래서 `code`가 따로 있다.

**`type`은 "프론트가 어떻게 대응해야 하는가"로 묶는다.**

| type | 프론트의 대응 |
|---|---|
| `auth_error` | 로그인 화면으로 보낸다 |
| `permission_error` | 목록으로 되돌리고 안내한다 |
| `validation_error` | 그 화면에 머물며 입력을 고치게 한다 |
| `state_error` | 이미 처리됐거나 사라진 것 — 화면을 새로 불러온다 |

#### 상태 코드 배정

**`422`는 쓰지 않는다.** 400과 422의 경계는 사람마다 다르고, Express 진영은 400으로 통일하는 팀이 많다. 규칙이 적을수록 서버 코드가 깔끔하다.

| HTTP | code | type | 언제 |
|---|---|---|---|
| 400 | `VALIDATION_ERROR` | `validation_error` | 필수값 누락, 형식 오류 |
| 400 | `SERIAL_NOT_FOUND` | `validation_error` | 등록 폼 — 그런 일련번호가 없음 |
| 400 | `INVALID_CODE` | `validation_error` | 등록 폼 — 등록코드 불일치 |
| 400 | `DOSE_NOT_DUE` | `validation_error` | 아직 오지 않은 복약 건을 복용 처리하려 함 |
| 401 | `UNAUTHORIZED` | `auth_error` | 토큰이 없거나 만료됨 |
| 401 | `INVALID_DEVICE_KEY` | `auth_error` | 기기 API 키가 틀림 |
| 403 | `FORBIDDEN` | `permission_error` | **내 기기/내 알림이 아님** (§8.5) |
| 404 | `DEVICE_NOT_FOUND` | `state_error` | 경로의 기기 id가 없음 |
| 404 | `NOT_FOUND` | `state_error` | 경로의 복약 건·알림 id가 없음 |
| 409 | `ALREADY_REGISTERED` | `state_error` | 이미 등록된 기기 |
| 409 | `ALREADY_TAKEN` | `state_error` | 이미 복용 처리된 복약 건 |

- **`401`과 `403`** — `401`은 "출입증을 안 보여줬다"(인증), `403`은 "출입증은 봤는데 이 층은 못 들어간다"(인가)
- **`404`를 두 갈래로 나눈 이유** — 등록 폼에서 일련번호를 잘못 친 것은 "주소가 없다"가 아니라 **"입력값이 틀렸다"** 이므로 `400`으로 보낸다. `404`는 **URL 경로의 자원이 없을 때만** 쓴다

---

### 8.1 공통 객체 모양

이 넷이 여러 API에 반복해서 나온다. **어느 API에서든 같은 필드 목록**을 갖는다.

#### Device

```json
{
  "id": "d3f1a2b4-9c8e-4f21-b7a3-0e5d6c1f8a92",
  "serial": "PB-2026-0001",
  "nickname": "할아버지 기기",
  "patient_phone": "01012345678",
  "timezone": "Asia/Seoul"
}
```

- **`registration_code`와 `device_api_key`는 절대 내려주지 않는다.** 등록코드가 새면 "기기를 만져본 사람만 안다"는 전제가 깨지고, API 키가 새면 아무나 `taken` 거짓 보고를 보낼 수 있다
- **DB 테이블의 컬럼 목록 ≠ API 응답의 필드 목록.** 새 컬럼이 생겨도 자동 포함하지 않고 그때 판단한다
- `patient_phone` — 없으면 `null`. 있을 때만 화면 4에 [전화 걸기]가 나온다
- `timezone` — 어느 화면도 안 쓴다. 서버가 "매일 08:00"을 UTC로 바꿀 때 쓰는 내부 계산용

#### Dose (복약 건 = 출석부 한 줄, §3.1)

```json
{
  "id": "8821",
  "device_id": "d3f1a2b4-9c8e-4f21-b7a3-0e5d6c1f8a92",
  "scheduled_at": "2026-08-19T23:00:00Z",
  "status": "missed",
  "notified_at": "2026-08-19T23:00:00Z",
  "dispensed_at": null,
  "taken_at": null,
  "taken_source": null
}
```

- `status` — `scheduled` / `notified` / `dispensed` / `taken` / `missed` (§3.2)
- `taken_source` — `ir` / `camera` / `manual` / `null` (§3.3)
- **시각 4개를 항상 다 내려준다.** `missed`가 두 종류인 것을 화면이 구분해야 한다 (§3.3)
- `notified_at`은 지금 어느 화면도 안 쓰지만, 복약 건의 본질적 필드이므로 담는다

#### Medication (약 설정 = 시간표, §3.1)

```json
{
  "id": "31",
  "device_id": "d3f1a2b4-9c8e-4f21-b7a3-0e5d6c1f8a92",
  "name": "혈압약",
  "dosage": "1정",
  "time": "08:00",
  "days": [0, 1, 2, 3, 4, 5, 6]
}
```

- **★ `time`은 UTC가 아니다 — §8.0 시각 규칙의 유일한 예외**

  "매일 08:00"은 **순간이 아니라 규칙**이라 UTC로 바꿀 수 없다. UTC로 바꾸려면 "어느 날의 08시"인지가 있어야 하는데, 매일 반복되는 값에는 날짜가 없다.

  ```
  medications.time      "08:00"                    시간표 (벽시계)
  devices.timezone      "Asia/Seoul"
          ↓ 서버가 그날의 복약 건을 만들 때 계산
  doses.scheduled_at    "2026-08-20T23:00:00Z"     출석부 (UTC 순간)
  ```

  **시간표는 벽시계, 출석부는 UTC.** §3.1의 구분이 시각 형식에서도 갈린다.

- `days` — `0`=일 ~ `6`=토 (JS `Date.getDay()`와 같은 번호)
- 기기당 **1줄**이므로 배열에 0개 또는 1개뿐

#### Notification

```json
{
  "id": "412",
  "device_id": "d3f1a2b4-9c8e-4f21-b7a3-0e5d6c1f8a92",
  "dose_id": "8821",
  "type": "missed_not_dispensed",
  "message": "약을 안 꺼내셨어요",
  "created_at": "2026-08-12T23:05:00Z",
  "read_at": null
}
```

- `type` — `missed_not_dispensed`(안 꺼냄) / `missed_not_taken`(꺼냈으나 미복용)
- **`message`는 서버가 `type`으로부터 만든다. DB에 저장하지 않는다**
  - 저장 안 하는 이유: 문구를 고치면 과거 알림까지 같이 바뀐다
  - 서버가 만드는 이유: 나중에 FCM 푸시(§10)도 같은 함수를 써야 **목록과 푸시 문구가 어긋나지 않는다**
- `read_at` — 안 읽었으면 `null`
- **기기 별명과 복약 건은 여기 안 담는다.** `devices`·`doses` 배열을 따로 주고 화면이 id로 짝을 맞춘다

---

### 8.2 보호자 앱용 (Supabase Auth 토큰 필요)

| # | 메서드 | 경로 | 화면 |
|---|---|---|---|
| 1 | GET | `/api/devices` | 2 |
| 2 | POST | `/api/user-devices` | 3 |
| 3 | GET | `/api/devices/:id/doses` | **4 · 6 공용** |
| 4 | GET | `/api/devices/:id/medications` | 5 |
| 5 | PUT | `/api/devices/:id/medications` | 5 |
| 6 | GET | `/api/notifications` | 7 |
| 7 | POST | `/api/notifications/:id/read` | 7 |
| 8 | POST | `/api/doses/:id/taken` | 공용 팝업 |

> **경로의 `:id`는 UUID(`Device.id`)다. `serial`이 아니다.**
> `serial`은 `PB-2026-0001`처럼 순차적이라 URL에 쓰면 옆 번호를 찍어볼 수 있다.
> `serial`을 쓰는 곳은 **사람이 스티커를 보고 입력하는** 화면 3의 등록 폼뿐이다.

#### 1. `GET /api/devices` — 기기 목록 + 오늘 상태 (화면 2)

```
쿼리 : ?limit=20&offset=0   (둘 다 선택, 기본 limit=20 / offset=0)
정렬 : 별명 가나다순 고정 (쿼리로 못 바꿈)
```

```json
{
  "devices":     [ { "...Device..." } ],
  "today_doses": [ { "...Dose..." } ],
  "unread_count": 2,
  "has_more": false
}
```

- `today_doses` — **기기 타임존 기준 오늘**의 복약 건. 화면이 `device_id`로 짝을 맞춘다. 오늘이 복용 요일이 아니거나 약 미설정이면 그 기기의 줄이 **없다**
- `unread_count` — 하단 탭 배지. **별도 API 없이 여기에 얹는다**
  - 배지가 보이는 화면은 2·7 둘뿐이고, 그 둘의 응답에 각각 넣으면 별도 API가 필요 없다
  - 화면 2는 30초마다 이 API를 다시 부른다 → **배지와 오늘 상태가 함께 갱신된다**
- **`offset` 페이지네이션인 이유** — 기기 목록은 보호자가 직접 등록할 때만 늘어나므로 순번이 밀릴 일이 없다. 정렬이 별명 가나다순이라 커서로 쓸 값(별명)이 중복 가능하고 한글 정렬 순서가 DB 설정에 따라 달라진다
- 빈 상태 : `200` + 두 배열 모두 `[]`, `unread_count: 0` (404 아님)

#### 2. `POST /api/user-devices` — 기기 등록 (화면 3)

```json
// 요청
{ "serial": "PB-2026-0001", "code": "A1B2C3",
  "nickname": "할아버지 기기", "patient_phone": "01012345678" }

// 응답 201
{ "devices": [ { "...Device..." } ] }
```

- **이 API가 만드는 것은 기기가 아니라 연결(`user_devices` 한 줄)이다.** `devices` 테이블은 **읽기만 하고 쓰지 않는다.** 그래서 경로가 `/api/devices`가 아니라 `/api/user-devices`다
- `nickname`·`patient_phone`은 **`user_devices`의 그 줄에 함께 저장한다**(§7). 보호자별 값이므로 `devices`를 건드릴 일이 없다
- `nickname`은 선택. 비면 **서버가 `serial`을 별명으로 채운다** (가나다순 정렬에서 `null`이 몰리는 것을 피함)
- `patient_phone`은 선택. 비면 `null`로 저장하고, 화면 4에 [전화 걸기]가 안 나온다. **다른 보호자가 입력한 값을 가져오지 않는다** — 각자 자기 줄의 값만 본다
- `serial`/`code`는 **서버가 대소문자·공백·하이픈을 무시하고 찾는다.** 화면도 다듬지만 **서버 정리가 필수** — `curl`로 직접 보낼 수 있다
- 방금 등록한 기기는 약 설정도 복약 건도 없으므로 **dose는 안 담는다**
- 등록 성공 → 화면 2로 이동 후 **`GET /api/devices`를 다시 호출**한다. 가나다순 정렬 + 페이지네이션 때문에 프론트가 끼워 넣을 위치를 판단할 수 없다
- 실패

| HTTP | code | message (화면에 그대로 출력) |
|---|---|---|
| 400 | `VALIDATION_ERROR` | 일련번호를 입력해 주세요 |
| 400 | `VALIDATION_ERROR` | 등록코드는 영문·숫자 6자리입니다 |
| 400 | `SERIAL_NOT_FOUND` | 해당 일련번호의 기기를 찾을 수 없습니다 |
| 400 | `INVALID_CODE` | 등록코드가 일치하지 않습니다 |
| 409 | `ALREADY_REGISTERED` | 이미 등록된 기기입니다 |

> **★ `409`는 "내가 이미 등록한 기기"일 때만이다.** 다른 보호자가 등록해 둔 기기는 **통과시킨다** — 공동 관리가 이 조건 하나에 달려 있다.
> ```sql
> SELECT 1 FROM user_devices
> WHERE device_id = ? AND user_id = ?;   -- ← user_id 조건이 빠지면 공동 관리가 막힌다
> ```
> 등록코드가 기기 뒷면 스티커에만 있어(실물을 만져본 사람만 안다) 이렇게 열어도 안전하다. 더 엄격한 "첫 보호자가 승인" 방식은 §10.

> **4단계 반영** — `devices`에 `serial_normalized` 컬럼을 추가한다. 컬럼에 함수를 씌우면(`UPPER(REPLACE(...))`) 인덱스를 못 쓴다.

#### 3. `GET /api/devices/:id/doses` — 복약 건 조회 (화면 4 **와** 화면 6 공용)

```
쿼리 : ?from=2026-08-17&to=2026-08-23

  화면 4      ?from=2026-08-17&to=2026-08-23    이번 주 월~일
  화면 6      ?from=2026-07-25&to=2026-08-23    최근 30일
  [더 보기]   ?from=2026-06-25&to=2026-07-24    그 이전 30일
```

```json
{
  "devices":     [ { "...Device..." } ],
  "doses":       [ { "...Dose..." } ],
  "medications": [ { "...Medication..." } ],
  "has_more": false
}
```

- **화면 4·6이 같은 API를 쓴다.** 같은 자원(복약 건)을 기간만 다르게 본다. 라우터 하나로 두 화면이 해결된다
- **날짜 범위로 자르므로 `limit`·`before`·`offset`이 필요 없다.** `7/25 ~ 8/23`은 새 기록이 쌓여도 같은 구간이라 순번이 밀리는 문제가 없다 — 커서 방식의 장점을 그냥 얻는다
- `has_more` — `from` 이전에 더 오래된 기록이 있는지. 화면 6의 [더 보기] 표시 판단용
- `devices` — 화면 4가 별명·전화번호를 쓴다. 요청을 2번 하지 않기 위해 같이 담는다. 라우터를 붙이면 이 화면으로 **바로 들어오거나 새로고침**할 수 있으므로 "화면 2를 거쳐 왔다"고 가정하면 안 된다
- `doses` — **7개가 아닐 수 있다.** 복용 요일이 월·수·금이면 3개다. 화면이 자기가 보낸 `from`~`to`로 날짜 7칸을 먼저 그리고 끼워 넣는다
- `medications` — **약 미설정 구분용**
  - `[]` → 약을 아직 설정 안 함 → 화면 4는 "약 설정하기" 버튼
  - `[{...}]` → 설정은 있는데 이번 주에 복용 요일이 없음 → 그냥 빈 주
- 정렬 : `scheduled_at` 내림차순
- 빈 상태 : 기기가 없으면 `404 DEVICE_NOT_FOUND` / 복약 건이 없으면 `200` + `"doses": []`

> **날짜를 프론트가 계산하는 것은 MVP 한정.** §3.4가 "화면에는 KST로 표시"라고 못박아 이 앱은 한국 전용이므로 브라우저 시계와 서버 시계가 일치한다.
> 해외 거주 보호자를 지원하게 되면 — **API 모양은 그대로 두고** `from`/`to`가 없을 때 서버가 기기 타임존 기준 기본값을 채우도록 한 줄만 추가하면 된다.

#### 4. `GET /api/devices/:id/medications` — 약 설정 조회 (화면 5)

```json
{
  "devices":     [ { "...Device..." } ],
  "medications": [ { "...Medication..." } ]
}
```

- 약을 아직 설정 안 했으면 `200` + `"medications": []` — **404가 아니다.** 새 기기는 "없는 게 정상"이지 오류가 아니다
- `devices`를 같이 담는 이유는 화면 4와 같다 (헤더에 기기 별명을 쓴다)
- 기기가 없으면 `404 DEVICE_NOT_FOUND`

#### 5. `PUT /api/devices/:id/medications` — 약 설정 저장 (화면 5)

```json
// 요청
{ "name": "혈압약", "dosage": "1정", "time": "08:00", "days": [1, 3, 5] }

// 응답 200
{ "medications": [ { "...Medication..." } ] }
```

- **`POST`가 아니라 `PUT`인 이유 — 멱등하기 때문.** 기기당 1줄이라 저장은 항상 **덮어쓰기**다. 저장 버튼을 두 번 눌러도, 네트워크가 느려 요청이 두 번 나가도 결과가 같아야 한다. PostgreSQL의 `ON CONFLICT (device_id) DO UPDATE`로 구현한다
- **저장 이후의 복약 건부터 적용된다.** 이미 만들어진 과거 건은 바뀌지 않는다 (§5 화면 5)
- 실패 : `400 VALIDATION_ERROR`(이름·용량 빈칸, 시각 형식 오류, 요일 0개) / `403 FORBIDDEN` / `404 DEVICE_NOT_FOUND`

#### 6. `GET /api/notifications` — 알림 목록 (화면 7)

```
쿼리 : ?limit=30   (선택, 기본 30)
```

```json
{
  "notifications": [ { "...Notification..." } ],
  "devices":       [ { "...Device..." } ],
  "doses":         [ { "...Dose..." } ],
  "unread_count": 2,
  "has_more": false
}
```

- `created_at` **내림차순.** 기기별로 묶지 않는다 (§11)
- **화면 2와 똑같은 패턴** — 배열을 나눠 주고 화면이 `device_id` / `dose_id`로 짝을 맞춘다
  - `devices` — 한 줄에 표시할 **기기 별명**
  - `doses` — 항목을 탭하면 열리는 **공용 팝업**이 쓴다. 요청 한 번을 아낀다
- **날짜 범위가 아니라 개수(`limit`)인 이유** — 알림은 드물게 쌓여서 "최근 30일"이 2건일 수도 있다. 요청 파라미터는 목록 성격에 맞춘다 (응답 모양은 그래도 동일하다)
- 빈 상태 : `200` + 세 배열 모두 `[]`, `unread_count: 0`

#### 7. `POST /api/notifications/:id/read` — 읽음 처리 (화면 7)

```json
// 요청 본문 없음
// 응답 200
{ "notifications": [ { "...갱신된 Notification..." } ], "unread_count": 1 }
```

- **`unread_count`를 같이 돌려주므로** 항목을 탭하는 즉시 하단 탭 배지가 정확한 숫자로 줄어든다. 목록 전체를 다시 안 불러도 된다
- 이미 읽은 알림에 또 보내도 **`read_at`은 처음 값 그대로 둔다**
- 실패 : `403 FORBIDDEN`(내 알림이 아님) / `404 NOT_FOUND`

#### 8. `POST /api/doses/:id/taken` — 수동 복용 처리 (공용 팝업)

```json
// 요청 본문 없음
// 응답 200
{ "doses": [ { "...갱신된 Dose..." } ] }
```

- 서버가 `status='taken'`, `taken_source='manual'`, `taken_at=현재시각`으로 갱신한다. 무엇으로 바꿀지는 서버가 알므로 요청 본문이 없다
- **바뀐 복약 건을 돌려주므로** 그 한 건만 갈아끼우면 화면 4·6·7이 갱신된다. 같은 팝업을 세 화면이 공유하므로, 어느 화면에서 눌렀든 id가 같은 항목만 교체하면 된다
- **미래 복약 건은 서버도 막는다.** 화면이 버튼을 감추지만 **화면을 못 믿는 게 서버의 기본자세**다
- 실패 : `400 DOSE_NOT_DUE` / `403 FORBIDDEN` / `404 NOT_FOUND` / `409 ALREADY_TAKEN`

---

### 8.3 디바이스용 (기기 API 키 필요)

**앱용 API와 객체 모양을 공유하지 않는다** — 기기가 자기 별명이나 환자 전화번호를 알 필요가 없다.

#### `GET /api/device/schedule` — 스케줄 받아가기

```json
{
  "devices":     [ { "id": "d3f1a2b4-...", "timezone": "Asia/Seoul" } ],
  "medications": [ { "name": "혈압약", "dosage": "1정",
                     "time": "08:00", "days": [1, 3, 5] } ]
}
```

- 약 설정이 없으면 `"medications": []` → 디바이스는 아무 안내도 하지 않는다
- 디바이스는 부팅 시와 주기적으로 이걸 받아 **스스로 시각을 재고 5분 타이머를 돌린다** (§4)
- **`Device` 객체를 통째로 주지 않는다.** 별명·일련번호·환자 전화번호는 기기에게 불필요한 노출이다

#### `POST /api/device/events` — 상태 보고 ★

```json
// 요청
{
  "scheduled_at": "2026-08-12T23:00:00Z",
  "status": "dispensed",
  "occurred_at": "2026-08-12T23:03:12Z",
  "taken_source": null
}

// 응답 200
{ "doses": [ { "...Dose..." } ] }
```

- **★ `scheduled_at`으로 어느 복약 건인지 특정한다.**

  디바이스는 `dose_id`를 **모른다** — 그 번호는 서버가 만든 것이다. 초안(`{ status, occurred_at, taken_source? }`)만으로는 서버가 **`doses` 테이블의 어느 줄에 적을지 알 수 없었다.** 디바이스는 스케줄을 받아 갔으니 "오늘 08:00분"이 언제인지 안다.

- 해당 복약 건이 아직 없으면 **서버가 그때 만든다.** 스케줄대로 미리 만들어두는 크론이 필요 없어진다
- **`occurred_at`은 "실제로 일어난 시각"이다. 서버는 "받은 시각"을 쓰지 않는다** (§3.4)
  → 나중에 오프라인 큐(§10)를 붙여 밀린 기록을 한꺼번에 보내도 시각이 안 어긋난다
- `status`가 `taken`이면 `taken_source`를 반드시 함께 보낸다 (`ir` 또는 `camera`)
- 실패 : `401 INVALID_DEVICE_KEY` / `400 VALIDATION_ERROR`

---

### 8.4 화면 ↔ API 대응표

| 화면 | 열릴 때 | 동작할 때 |
|---|---|---|
| 1 로그인 | (Supabase Auth 직접) | — |
| 2 기기 목록 | `GET /devices` | — |
| 3 기기 등록 | 없음 | `POST /user-devices` |
| 4 기기 상세 | `GET /devices/:id/doses?from=&to=` | — |
| 5 약 설정 | `GET /devices/:id/medications` | `PUT /devices/:id/medications` |
| 6 복약 기록 | `GET /devices/:id/doses?from=&to=` | 같은 API로 [더 보기] |
| 7 알림 목록 | `GET /notifications` | `POST /notifications/:id/read` |
| 공용 팝업 | 없음 (부모 화면이 이미 들고 있는 dose를 씀) | `POST /doses/:id/taken` |

**모든 화면이 열릴 때 요청 1번이다.** 화면 2의 배지도 `GET /devices` 응답에 얹어 해결했다.

---

### 8.5 모든 API가 지켜야 할 규칙 (접근 제어)

> **요청한 보호자가 `user_devices`에 그 기기와 연결돼 있는지 매번 확인한다.**
> 확인 없이 device_id만 믿으면, 남의 device_id를 넣어 보내는 것만으로 다른 사람의 복약 기록이 노출된다.
> 디바이스 요청도 같다 — API 키에 묶인 기기의 데이터만 읽고 쓸 수 있다.

**누구인지는 토큰이 말하고, 무엇을 요청했는지는 URL이 말한다.** 클라이언트가 보낸 id를 그대로 믿으면 뚫린다.

`:id`가 기기가 아닌 API도 마찬가지다.

| API | 확인할 것 |
|---|---|
| `POST /api/doses/:id/taken` | 그 복약 건의 `device_id`를 꺼내서, 그 기기가 내 것인지 (`doses` → `devices` → `user_devices`) |
| `POST /api/notifications/:id/read` | 그 알림의 `user_id`가 나인지 (`notifications`에 `user_id`가 직접 있으므로 한 단계) |

**남의 것이면 `403`을 준다.** `404`를 주면 "그런 게 존재하는지"조차 안 알려줘 더 안전하다는 견해도 있지만, 이 프로젝트는 등록코드라는 자물쇠가 따로 있어 존재 여부 노출의 위험이 낮고, 디버깅이 쉬운 `403`으로 통일한다.

---

### 8.6 초안에서 바뀐 것

화면 7장을 만든 뒤 §9의 3단계로 확정하면서 발견하거나 바꾼 것들.

| # | 문제 | 해결 |
|---|---|---|
| 1 | 기기 하나를 가져오는 길이 없었다 | 화면 4·5의 응답에 `devices`를 같이 담는 것으로 해결 (별도 API 불필요) |
| 2 | 화면 4가 기기 정보를 얻으려면 요청 2번 | 응답에 `devices` 포함 |
| 3 | [더 보기]에 쓸 파라미터가 없었다 | `?from=&to=` + `has_more` |
| 4 | 알림에 기기 별명이 없었다 | `devices` 배열을 따로 담아 화면이 `device_id`로 짝을 맞춤 |
| 5 | 알림 탭 → 팝업에 쓸 복약 건이 없었다 | `doses` 배열을 따로 담음 |
| 6 | 안 읽은 개수를 따로 못 가져왔다 | 별도 API 대신 `GET /devices`·`GET /notifications` 응답에 `unread_count`를 얹음 |
| 7 | 쓰기 API가 갱신된 데이터를 안 돌려줬다 | `taken` / `read` / `medications` 저장이 바뀐 객체를 응답 |
| 8 | 약 미설정을 구분할 수 없었다 | `medications` 배열의 길이로 구분 |
| 9 | 약 설정이 없을 때의 응답 모양이 불명확 | 404가 아니라 `{ "medications": [] }` |
| 10 | **디바이스 이벤트가 어느 복약 건인지 특정 불가** | 요청에 `scheduled_at` 추가 |
| 11 | 기기 등록 경로가 자원과 안 맞았다 | `POST /api/devices` → `POST /api/user-devices` (만드는 것은 기기가 아니라 연결) |
| 12 | 화면 4·6이 같은 자원을 다른 API로 보고 있었다 | **하나로 합침.** `GET /api/devices/:id/doses?from=&to=` |

**공동 관리를 MVP에 넣으면서 추가로 발견한 것 (2026-08-25)**

| # | 문제 | 해결 |
|---|---|---|
| 13 | **`patient_phone`을 등록하는 길이 아예 없었다.** 응답에 담기기만 하고 보내는 API가 없어 화면 4의 [전화 걸기]가 영영 안 나올 상태였다 | 화면 3 등록 폼에 입력칸 추가 + 요청 바디에 `patient_phone` |
| 14 | `nickname`을 `devices`에 두면 보호자 2명이 같은 별명을 강제로 공유한다. "`devices`는 손대지 않는다"(2번 API 설명)와도 모순이었다 | `nickname`·`patient_phone`을 **`user_devices`로 이동**. 모순도 함께 해소 |
| 15 | `409 ALREADY_REGISTERED`의 판정 기준이 "누구 기준으로 이미"인지 없었다. 기기 기준이면 두 번째 보호자가 막힌다 | **"내가 이미 등록한 기기"** 기준으로 명시 (SQL에 `user_id` 조건) |
| 16 | 알림을 "한 줄 저장"으로 적어 두어, 보호자 2명이면 한 명만 받게 될 상태였다 | **연결된 보호자 수만큼** 저장 (`INSERT ... SELECT`) |

> **§8.1 공통 객체 4개(`Device`/`Dose`/`Medication`/`Notification`)는 하나도 바뀌지 않았다.** 컬럼이 옮겨간 것은 DB 안쪽 사정이고, 응답 모양은 계약이라 그대로다 — *"DB 테이블의 컬럼 목록 ≠ API 응답의 필드 목록"*(§8.1 Device). 2단계에서 만든 화면 7장도 영향 없다.

> **10번이 가장 위험했다.** 초안의 `{ status, occurred_at, taken_source? }`만으로는 서버가 "어느 날 몇 시분 복약 건인지" 알 수 없다. 5단계에서 구현하다 발견했으면 디바이스 코드까지 같이 고쳐야 했다.

**§7(테이블)에 추가할 것은 `devices.serial_normalized` 하나뿐이다.** `device_nickname`·`message`·`medications` 유무는 전부 **저장하는 값이 아니라 서버가 만들어 내려주거나 계산하는 값**이다.

---

## 9. 개발 순서

| 단계 | 내용 | 담당 |
|---|---|---|
| 1 | 이 명세 확정 | — |
| 2 | **화면 7장 퍼블리싱** (목업 데이터, 서버 없음) | 퍼블 |
| 3 | 화면이 요구하는 데이터 확정 → API 응답 모양 고정 | 함께 |
| 4 | DB 테이블 설계 + Supabase에 생성 | 직접 타이핑 |
| 5 | **인증 없이** 서버 API 구현 (user_id 임시 하드코딩) + **Swagger 문서** | 직접 타이핑 |
| 6 | 화면에 데이터 연결 (Zustand, 라우터, fetch) | 직접 |
| 7 | 인증 붙이기 (Supabase Auth + 접근 제어) | 직접 타이핑 |
| 8 | 디바이스 — ① 음성 ② IR ③ 카메라 (§4.1) | 직접 타이핑 |
| 9 | Render / Vercel 배포 | 함께 |

**4단계에서 놓치기 쉬운 것 — `user_devices`에 UNIQUE 제약:**
`(user_id, device_id)` 두 컬럼을 **묶어서** UNIQUE로 잡는다. 각각이 아니라 **조합**이 유일해야 한다 — 한 보호자가 기기 여러 대를 갖고, 한 기기에 보호자 여러 명이 붙기 때문이다.

```sql
UNIQUE (user_id, device_id)
```

이걸 걸어두면 `409 ALREADY_REGISTERED`(§8.2 2번)를 DB가 대신 막아준다. 서버 코드가 "이미 있나?" 확인하고 `INSERT` 하는 사이에 같은 요청이 두 번 들어와도(더블클릭·재시도) 줄이 두 개 생기지 않는다.

**5단계에서 놓치기 쉬운 것 두 가지** — 둘 다 공동 관리 때문에 생겼고, 명세에 SQL까지 적어 두었다.
- `409` 판정에 **`user_id` 조건**을 빼먹으면 두 번째 보호자가 막힌다 (§8.2 2번)
- 알림을 한 줄만 넣으면 **보호자 한 명만 받는다.** `INSERT ... SELECT`로 연결된 보호자 수만큼 (§5 화면 7)

**왜 화면부터 만드나:** 리액트가 가장 익숙한 영역이라 속도가 붙고, 화면을 실제로 만들다 보면 "이 화면엔 이 데이터가 필요한데 API에 없네"가 튀어나온다. 그걸 DB 설계 **전에** 알게 되는 것이 이득이다.

**5단계가 왜 인증 없이인가:** 인증부터 붙이면 버그가 났을 때 "내 로직이 틀린 건가 / 로그인이 안 된 건가"를 구분할 수 없다. 자물쇠는 문틀을 다 세운 다음에 단다.

**5단계에 Swagger를 같이 묶는 이유:** Swagger는 API 메뉴판인데 **메뉴판에서 바로 주문 버튼을 누를 수 있는** 메뉴판이다(`/api-docs`에서 Try it out). API를 하나 만들 때마다 주석으로 적어두면, 6단계에서 화면 연결이 안 될 때 **"서버가 잘못 준 건가 / 내가 잘못 받은 건가"** 를 한 번에 가른다. 3단계에서 고정한 응답 모양을 그대로 옮겨 적는 작업이라 따로 설계할 것도 없다.
- 방식: `swagger-jsdoc` + `swagger-ui-express` — 라우터 코드 **바로 위 주석**에 적는다(문서를 파일로 떼어놓으면 안 고치고 방치된다)
- 7단계에서 인증을 붙이면 Swagger 화면에 **Authorize 버튼** 설정만 얹으면 되고, 5단계 문서는 그대로 쓴다

---

## 10. 나중에 (지금은 안 만들지만 자리는 만들어 둠)

| 항목 | 붙일 자리 | 지금 안 하는 이유 |
|---|---|---|
| **FCM 푸시** | 서버가 알림 테이블에 저장하는 그 지점에 발송 한 줄 추가 | 유일하게 무거운 작업. 화면·DB 변경 없이 나중에 꼭지만 달면 됨. **단, 이게 없으면 "5분"이 의미를 잃으므로 마지막에 반드시 붙일 것** |
| 외출/알림 일시정지 | 기기에 `paused_until` 컬럼 1개 | manual 체크로 사후 처리 가능. 알림이 먼저 온다는 차이만 감수 |
| 디바이스 오프라인 큐 | 디바이스 로컬 저장 | 서버는 이미 `occurred_at`을 받도록 설계돼 있어 나중에 그대로 붙음 |
| 소셜 로그인 | 화면 1의 빈 자리 | Supabase Auth 설정 문제라 화면 구조 안 바뀜 |
| 약 여러 종 / 하루 여러 번 | `medications` 여러 줄 + 복약 건을 "시간 슬롯" 단위로 | IR 센서는 칸만 감지하고 어떤 약인지 모르므로, 확장 시 슬롯 개념이 필요해짐 |
| 알림 종류 세분화·통계 | 이미 남기고 있는 시각 3개로 계산 가능 | — |
| **별명·전화번호 수정** | `PATCH /api/user-devices/:device_id` 1개 + 화면 4에 팝업 1개. 접근 제어는 `user_devices`의 **내 줄 하나**만 보면 되므로 §8.5 검사 중 가장 단순 | 등록할 때 한 번 입력하면 대개 안 바꾼다. 오타가 나도 **`tel:`은 다이얼러를 열 뿐 자동으로 걸지 않아** 번호를 보고 취소하면 그만이고, 보호자별 값이라 남에게 번지지도 않는다 |
| 공동 관리 승인 | 두 번째 보호자가 등록하면 첫 보호자에게 승인 요청 | 등록코드가 기기 실물 스티커에만 있어 아무나 붙을 수 없다. 승인 흐름은 화면과 상태가 늘어 비싸다 |

---

## 11. 결정 기록

| 결정 | 이유 |
|---|---|
| 약 1종·하루 1회 | MVP 최소화. IR 센서가 칸 단위라 여러 약 구분이 애초에 안 됨 |
| 5분 타이머는 디바이스가 잰다 | 서버 크론 불필요, 인터넷 끊겨도 음성 안내는 동작 |
| 기기 등록에 등록코드 요구 | 일련번호만으로는 남의 기기를 등록할 수 있음 |
| **보호자 공동 관리를 MVP에 포함** (2026-08-25) | 화면이 하나도 안 늘어난다. `user_devices`가 이미 그 구조이고, 컬럼 이사 2개 + 서버 로직 판단 2개로 끝난다. 나중에 하면 데이터 이사 작업이 된다 |
| 별명·전화번호를 보호자별 값으로 | 아들과 딸이 같은 기기를 다르게 부를 수 있고, **한 사람의 오타가 다른 보호자에게 전파되지 않는다** |
| 이벤트에 `occurred_at`을 실어 보냄 | 나중에 밀린 기록을 보내도 시각이 안 어긋남 |
| 알림은 시간순 단일 목록 | 기기가 몇 대 안 되어 기기별 묶음은 오히려 한 번 더 눌러야 함 |
| 복약 상세를 팝업으로 | 화면을 늘리지 않고 세 화면에서 재사용 |
| 푸시를 마지막 단계로 | 유일하게 무거운 작업. 없어도 앱은 완결되고, 나중에 화면 변경 없이 붙음 |
| **카메라를 MVP에 포함** | 하드웨어(주물)에 이미 카메라 자리가 있어 빼면 제품 설명이 안 됨. 판정 범위를 "손목-입 거리"로 좁혀 난이도를 낮춤. 디바이스 안에서 마지막 순서이고, 안 되면 `ir` 폴백이 있음 |
| **DB = PostgreSQL** | Supabase가 PostgreSQL만 제공. MySQL 무료 상시 호스팅이 마땅치 않음 |
| **화면을 DB보다 먼저** | 가장 익숙한 영역이라 완주 확률이 오르고, 필요한 데이터가 화면에서 확정됨 |
| **JavaScript + CSS Modules** | TS 경험이 없어 학습 부담을 줄임. CSS Modules는 추가 설치 없이 Vite가 지원 |
