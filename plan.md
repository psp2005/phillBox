# 4주 계획 — 시연 2026-09-25 기준

> 작성 2026-08-26. 남은 기간 **약 4주**.
> 현재 위치: `spec.md` §9의 **2단계 완료**(화면 7장 퍼블리싱), 3단계 완료(API 명세 확정).
> 이 문서는 "무엇을 언제"만 다룬다. "무엇을 어떻게"는 `spec.md`가 기준이다.

---

## 0. 이 계획을 관통하는 원칙 3개

### ① 가로가 아니라 **세로**로 자른다

```
✗ 가로 자르기 : API 8개 전부 → 화면 연결 전부 → 인증 전부
                 (3주차까지 아무것도 안 돌아간다. 막히면 시연할 게 없다)

✓ 세로 자르기 : [로그인 → 기기 목록] 하나를 끝까지 관통시킨 뒤,
                 그 옆에 [기기 상세]를 붙이고, 또 그 옆에…
                 (1주차부터 "돌아가는 것"이 있고, 매주 그게 두꺼워진다)
```

건물을 지을 때 **1층 전체를 완성하고 2층으로 가는 게 아니라, 계단실 한 칸을 옥상까지 먼저 뚫는** 방식이다. 중간에 시간이 모자라도 "뚫린 만큼은 확실히 동작하는" 상태가 된다.

### ② 파이프라인은 **빈 껍데기일 때** 뚫는다

Vercel·Render·Supabase 연결은 **내용물이 없을 때 미리** 해둔다. 배포는 처음 하면 반드시 막히는데(환경변수·빌드·경로·CORS), 그 문제를 **코드가 단순할 때** 만나야 원인을 좁힐 수 있다.

> 서버 코드 500줄을 짜놓고 첫 배포를 하면, 안 될 때 "내 코드 문제인가 / 배포 설정 문제인가"를 못 가른다. `{ "ok": true }` 한 줄만 올려서 성공시켜 두면, 이후 실패는 **전부 내 코드 문제**로 확정된다.

### ③ **버릴 것을 미리 정한다**

일정이 밀리는 건 정상이다. 밀렸을 때 무엇을 버릴지 **지금** 정해두면 그때 당황하지 않는다. → §5 컷 라인

---

## 1. 주차별 계획

### 0주차 — 안전벨트 ✅ **완료 (2026-08-26)**

| 할 일 | 담당 |
|---|---|
| ✅ `git init` + `.gitignore` (`node_modules`, `.env` 제외) | 직접 타이핑 |
| ✅ GitHub 저장소 생성 → 첫 푸시 | 직접 타이핑 |

**저장소:** https://github.com/psp2005/phillBox (첫 커밋 `db1fee9`, 47개 파일)

**겪은 것 — 다음에 다시 만나면:**
- `web/` 안에서 먼저 `git init` 을 해버림 → `error: 'web/' does not have a commit checked out`. **`rm -rf web/.git`** 으로 해결. 저장소는 **프로젝트 루트**에 하나만 있어야 한다(`spec.md`·`server/`·`device/`가 코드와 같이 버전 관리돼야 하므로)
- `git add .` 을 건너뛰고 `commit`·`push` → `error: src refspec main does not match any`. **커밋이 0개**라 보낼 게 없다는 뜻. `add`(장바구니) → `commit`(계산) → `push`(백업) 세 단계는 건너뛸 수 없다
- `warning: LF will be replaced by CRLF` 는 **경고이지 오류가 아니다.** `core.autocrlf=true`(윈도우 권장값)가 줄바꿈을 자동 통일하는 중. 무시한다

> **지금 백업이 하나도 없다.** 화면 7장이 이 폴더에만 있다. 시연 일정과 무관하게 최우선.
> 깃은 Vercel 배포의 **전제 조건**이기도 하다(깃 푸시 → 자동 배포).

---
### 1주차 — 파이프라인 3개 뚫기 + DB

**목표: 폰 홈 화면 아이콘을 눌러 목업 화면이 열린다.**

| # | 할 일 | 담당 | 만나는 함정 |
|---|---|---|---|
| 1 | ✅ Vercel 연결 → `web/` 배포 | 함께 | **빌드 오류** |
| 2 | ✅ `manifest.json` + 아이콘 3장 (`display: standalone`) | 직접 | — |
| 3 | ✅ 폰에서 홈 화면에 추가 → 주소창 없이 열리는지 확인 | 직접 | iOS/안드로이드 설치 방법 차이 |
| 4 | ✅ Supabase 프로젝트 생성 + 테이블 5개 SQL (`spec.md` §7) | 직접 타이핑 | — |
| 5 | ✅ `server/` 폴더 + Express 최소 서버(`GET /api/health` 하나) | 직접 타이핑 | **환경변수** |
| 6 | ✅ Render 연결 → 5번 배포 | 함께 | — |

### ✅ 1주차 완료 (2026-08-30)

**5·6 완료.** 서버 주소 https://pillbox-server-wdjx.onrender.com

**Render 설정 (다시 만들 일이 있으면)**

| 항목 | 값 |
|---|---|
| 종류 | **Web Service** (Static Site 아님 — 우리 코드가 계속 살아 있어야 한다) |
| Region | Singapore (무료 지역 중 한국에서 가장 가까움. 서울 없음) |
| **Root Directory** | **`server`** |
| Build Command | **`npm install`** ← 기본값이 `yarn` 이라 반드시 바꿀 것. 우리는 `package-lock.json`(npm)으로 관리한다 |
| Start Command | **`npm start`** |
| Compute | **$0/month Free** ← 기본이 $7 유료로 선택돼 있다 |

**겪은 것 · 알아둘 것**
- `const PORT = process.env.PORT || 3000` 이 핵심. **Render가 포트를 정해서 환경변수로 넣어준다**(실제로 `10000`이 들어왔다). `3000`으로 고정했으면 배포가 살아나지 않는다
- `package.json` 의 `"start"` 스크립트 이름은 **Render가 찾는 약속된 이름**이다. 다른 이름이면 못 켠다
- `"type": "module"` 이 없으면 `import` 문에서 `SyntaxError: Cannot use import statement outside a module`
- **Node 버전** — 로컬 `v24.16.0` / Render `v24.14.1` (2026-08-30 확인). **메이저가 24로 같아서 문제 없다.** 마이너·패치 차이는 하위 호환이 보장된다. 나중에 메이저가 갈라지면(예: Render가 22로) `package.json` 에 `"engines": { "node": ">=24" }` 를 넣어 고정한다. 지금 넣으면 조건을 잘못 적었을 때 오히려 배포가 깨지므로 필요해질 때 한다
- **포트 번호(10000)는 외울 값이 아니다.** Render가 매번 정하고 바뀔 수 있다. 기억할 것은 값이 아니라 원칙 — **포트를 고정하지 말고 `process.env.PORT` 를 읽어라**
- **Vercel과 성격이 다르다.** Vercel은 빌드 결과 파일을 놓아두는 곳이고, Render는 **프로그램을 계속 켜두는 곳**이다. 그래서 Render만 콜드 스타트가 있다

**4 완료 (2026-08-29).** Supabase 프로젝트 `pillbox` (Northeast Asia · Seoul) — `https://ptloyqxytijidbwbmeco.supabase.co`
- 프로젝트 생성 시 **`Automatically expose new tables` 를 껐다.** 켜두면 새 테이블이 anon 키로 접근 가능한 REST 주소에 자동 노출되는데, anon 키는 프론트엔드에 들어가 누구나 볼 수 있다 → 등록코드·기기 API 키·복약 기록이 통째로 샌다
- 테이블마다 `enable row level security` 도 걸었다(정책은 0개 = 전면 차단). **잠금 2겹.** 우리 Express 서버는 `service_role` 신분이라 둘 다 통과한다
- 테이블 5개: `devices` / `user_devices` / `medications` / `doses` / `notifications`. `users` 는 Supabase Auth가 관리하므로 만들지 않는다
- **`medications.dose_time` 은 `time` 형(벽시계), `doses.scheduled_at` 은 `timestamptz`(UTC 순간).** 시간표와 출석부의 구분이 자료형에서 갈린다
- **`notifications` 에 `message` 컬럼을 만들지 않았다.** 서버가 `type` 으로부터 만들어 내보낸다 → 문구를 고치면 과거 알림까지 함께 바뀐다

**1~3 완료 (2026-08-26).** 배포 주소 https://phill-box.vercel.app
- 아이콘 원본(`PhillBoxIcon.png`)에 **투명 배경처럼 보이는 회색 체크무늬가 실제 픽셀로 구워져 있었다.** 그대로 썼으면 홈 화면에 체크무늬 배경이 박혔을 것. 흰색으로 치환하고 로고를 80% 크기로 가운데 정렬(사방 10% = maskable 안전지대)해서 `icon-192` / `icon-512` / `apple-touch-icon`(180) 3장 생성
- **iOS는 `manifest.json`의 아이콘을 홈 화면에 안 쓴다.** `<link rel="apple-touch-icon">` 를 따로 걸어야 한다

**끝났다는 판정 (3개 다 되어야 함):**
- 폰 홈 화면 아이콘 → 주소창 없이 목업 화면이 뜬다
- Supabase SQL Editor에서 `SELECT * FROM devices` 가 에러 없이 돈다 (0행이어도 성공)
- 브라우저에서 Render 주소 + `/api/health` → `{"ok":true}`

> **이 주가 끝나면 이미 시연이 가능하다.** 목업 데이터지만 "폰에 앱이 깔리고 화면이 돈다"까지는 보여줄 수 있다. 이게 **안전망**이다 — 이후 무엇이 실패해도 최소한 이건 남는다.

#### Vercel 연결 절차 (2026-08-26 기준)

**사전 확인 — 로컬 빌드부터.** `cd web && npm run build` 가 성공하는지 먼저 본다. 배포 실패 원인은 ①내 코드 ②배포 설정 둘 중 하난데, 로컬 빌드가 되면 ①이 지워진다.
(2026-08-26 확인 완료 — `vite v8.2.1`, 869 modules, 5초, 성공)

1. [vercel.com](https://vercel.com) → **Continue with GitHub** (별도 가입 없음)
2. **Add New… → Project** → `phillBox` 옆 **Import**
   (목록에 없으면 **Adjust GitHub App Permissions** 로 저장소 접근 권한 부여)
3. **★ Root Directory 를 `web` 으로 변경** ← 유일하게 틀리기 쉬운 곳
   - 기본값 `./` 로 두면 루트에서 `package.json` 을 못 찾아 실패한다. 우리 저장소는 루트에 문서가 있고 리액트 앱은 `web/` 안에 있다
   - `web` 을 지정하면 Framework Preset 이 **Vite** 로 자동 인식되고 Build Command(`npm run build`) · Output Directory(`dist`) 도 자동으로 채워진다. **건드리지 않는다**
4. **환경변수는 지금 비워둔다.** Supabase 키는 아직 없다
5. **Deploy** (1~2분) → `phill-box-xxxx.vercel.app` 주소 발급

**끝났다는 판정:**
- PC 브라우저에서 그 주소 → 화면 1(로그인)이 뜬다
- 폰에서 같은 주소 → **Wi-Fi 상관없이** 열린다 (`192.168.x.x` 와 달라진 점)
- 폰에서 화면 3 전화번호 칸 → 숫자 키패드가 뜬다 (`type="tel"`)

**이후로는 `git push` 만 하면 자동 배포된다.** 이게 이 단계의 진짜 이득이다 — 남은 4주 동안 코드를 고칠 때마다 진짜 폰에서 확인할 수 있다.

#### 윈도우에서 만나는 함정 — PowerShell 실행 정책

VSCode 터미널이 PowerShell 일 때 `npm` · `npx` 가 이렇게 막힐 수 있다:

```
npm.ps1 이 로드될 수 없습니다. 이 시스템에서 스크립트를 실행할 수 없으므로…
```

윈도우가 **서명 없는 스크립트 실행을 기본 차단**하기 때문이다. `npm` 은 윈도우에서 `npm.ps1` 이라는 스크립트로 동작해서 걸린다.

해결은 둘 중 하나 (권장은 A):

- **A. Git Bash 를 쓴다** — 실행 정책이 적용되지 않는다. 이 프로젝트의 깃 작업도 Git Bash 에서 했다.
  VSCode 터미널 우측 `∨` → **Git Bash** 선택 (기본 터미널로 지정 가능)
- **B. 현재 사용자만 정책 완화** — PowerShell 에서
  ```powershell
  Set-ExecutionPolicy -Scope CurrentUser RemoteSigned
  ```
  `RemoteSigned` = "내가 만든 스크립트는 실행, 인터넷에서 받은 것은 서명 필요". 관리자 권한이 필요 없고 시스템 전체를 열지 않는다.

> **터미널이 두 종류라 명령 문법이 다르다.** 폴더 삭제가 Git Bash 는 `rm -rf web/.git`, PowerShell 은 `Remove-Item -Recurse -Force web\.git`. 안내받은 명령이 안 먹으면 **터미널 종류부터** 확인할 것.

---

### 2주차 — 서버 API (`spec.md` §9의 5단계)

**목표: Swagger에서 버튼을 눌러 진짜 DB 데이터가 나온다.**

인증 없이. `user_id`는 하드코딩. (`spec.md` §9 — *"자물쇠는 문틀을 다 세운 다음에 단다"*)

만드는 순서는 **세로 자르기** 원칙대로, 화면 2가 필요한 것부터:

| 순서 | API | 왜 이 순서 |
|---|---|---|
| 1 | ✅ `GET /api/devices` | 첫 화면. 이거 하나면 화면 2가 산다 |
| 2 | ✅ `GET /api/devices/:id/doses` | 화면 4·6 공용이라 **하나로 두 화면** |
| 3 | ✅ `GET`·`PUT /api/devices/:id/medications` | 화면 5 |
| 4 | ✅ `GET /api/notifications` + `POST .../read` | 화면 7 |
| 5 | ✅ **`POST /api/devices/:id/doses/taken`** | 공용 팝업. **경로가 `POST /api/doses/:id/taken` 에서 바뀌었다** — 아래 참고 |
| 6 | ✅ `POST /api/user-devices` | 화면 3 |

### ✅ 2주차 앱용 API 8개 완료 (2026-09-12)

**만들면서 정한 것**

- **`requireMyDevice` 미들웨어로 접근 제어를 한 곳에 모았다.** 라우트마다 복사하면 API를 늘리다 한 곳에서 빼먹기 쉽다. 미들웨어는 **붙이는 걸 잊으면 아예 동작을 안 해서** 즉시 발견된다
- **`POST /api/doses/:id/taken` → `POST /api/devices/:id/doses/taken` 로 경로를 바꿨다** (2026-09-11). 복약 건 `id` 대신 **기기 + `scheduled_at`** 으로 지목하고, 행이 없으면 `on conflict` 로 그때 만든다. 기기가 꺼져 있던 날도 수동 체크가 가능해진다. 이유와 비용은 `spec.md` §8.2 8번
  - 덤으로 접근 제어가 2단계에서 **기기 한 단계**로 짧아져 `requireMyDevice` 를 그대로 쓴다
  - 화면에서 **빈 칸도 탭 가능하게** 하는 작업은 §10으로 미뤘다. 서버는 이미 받아준다
- **오류 형식(`{ error: { type, code, message } }`)은 `server/errors.js` 의 `fail()` 로 통일한다.** `code` 만 주면 `type` 을 표에서 찾아 채우므로 **둘이 어긋날 수 없다**. `POST /api/user-devices` 부터 적용했고, **나머지 라우트는 아직 `{ error: { message } }` 라 통일이 남아 있다**
- **`on conflict` 를 세 곳에서 썼다** — `PUT medications`(덮어쓰기) · `POST doses/taken`(없으면 생성) · `POST user-devices`(중복이면 409). 전부 **확인과 실행을 한 문장으로 묶어** 경쟁 조건을 없애는 목적

**아직 안 한 것**
- 디바이스용 2개(`GET /api/device/schedule`, `POST /api/device/events`)는 **4주차**. 파이썬 코드와 함께 만드는 게 빠르다
- `try`/`catch` 중복을 중앙 에러 미들웨어로 옮기는 정리

Swagger(`swagger-jsdoc` + `swagger-ui-express`)는 **API 하나 만들 때마다 그 자리에서** 주석으로 적는다. 몰아서 하면 안 한다.

**오류 처리 — API 3~4개를 만든 뒤 중앙 처리로 옮긴다** (2026-09-02 결정)
- 지금은 라우트마다 `try`/`catch` 로 시작한다. 라우트가 하나뿐이라 중복이 없고, `spec.md` §8.0의 오류 형식(`{ error: { type, code, message } }`)을 어떻게 만들지는 몇 개 짜본 뒤가 판단하기 좋다
- 같은 `catch` 가 반복되는 게 눈에 보이면 **인자 4개짜리 에러 미들웨어**(`app.use((err, req, res, next) => ...)`)로 옮긴다
- **Express 5는 `async` 함수의 오류를 자동으로 에러 미들웨어에 넘긴다.** 옮길 때 `try`/`catch` 를 지우기만 하면 된다 (4버전은 `next(err)` 를 직접 불러야 했다)

**끝났다는 판정:** `/api-docs`에서 Try it out → 6개 API가 `spec.md` §8의 응답 모양 그대로 돌려준다.

> **가장 큰 덩어리이자 가장 위험한 주.** 처음 배우는 영역이다. 밀리면 §5 컷 라인을 먼저 본다.

---

### 3주차 — 화면 연결 + 인증 (6·7단계)

**목표: 폰에서 로그인하면 진짜 데이터가 보인다. ← 이게 최소 시연본**

**셋으로 나뉜다** (2026-09-12 상세화)

| | 무엇 | 대략 |
|---|---|---|
| **Ⓐ** | **라우터** — 화면 이동을 주소 기반으로 | 1일 |
| **Ⓑ** | **API 연결** — 목업을 `fetch` 로 교체 | 2~3일 |
| **Ⓒ** | **인증** — 로그인 + 토큰 + 하드코딩 제거 | 1~2일 |

**왜 이 순서인가**
- **Ⓐ가 먼저**: 라우터 없이 `fetch` 를 붙이면 `App.jsx` 한 곳에 전부 몰린다. 라우터를 나중에 붙이면 **그걸 전부 다시 옮겨야** 한다. 같은 일을 두 번 안 하려고 먼저 한다
- **Ⓒ가 마지막**: `spec.md` §9 — *"자물쇠는 문틀을 다 세운 다음에 단다."* 인증부터 붙이면 버그가 났을 때 "내 로직이 틀린 건가 / 로그인이 안 된 건가"를 못 가른다

---

#### Ⓐ 라우터 — 4단계

```
1. BrowserRouter + Routes 껍데기        ← 화면 1·2만 연결
2. 나머지 화면 5장 + useParams
3. 하단 탭(화면 2·7)을 중첩 라우트로
4. vercel.json 로 배포 404 막기
```

**주소 설계** (API 경로와 비슷하게 맞췄다)

| 화면 | 주소 |
|---|---|
| 1 로그인 | `/login` |
| 2 기기 목록 | `/devices` |
| 3 기기 등록 | `/devices/new` |
| 4 기기 상세 | `/devices/:id` |
| 5 약 설정 | `/devices/:id/medications` |
| 6 복약 기록 | `/devices/:id/history` |
| 7 알림 목록 | `/notifications` |

- **`App.jsx` 하나만 바뀐다.** 화면 컴포넌트 7장은 거의 안 건드린다 — 화면을 고르는 방식만 갈아끼우는 것
- 상태 셋 중 `screen` 은 **주소가**, `selectedDeviceId` 는 **`useParams()` 가** 대신한다. `detailDose`(공용 팝업)는 **그대로 둔다** — 팝업이 주소를 바꾸면 뒤로가기로 팝업만 닫혀 어색하다
- **개념 정리 문서**: `docs/react-router-notes.html` (History API · `pushState` · `popstate` · SPA)

> **라우터가 없어서 지금 생기는 증상 (2026-08-26 확인)** — PWA 설치 후 **안드로이드 뒤로가기를 누르면 앱이 그냥 닫힌다.** 화면 전환이 `useState`로만 일어나 히스토리가 안 쌓이기 때문. **시연 전에 반드시 해결돼 있어야 한다.**

#### Ⓑ API 연결 — 여기가 가장 크다

**먼저 준비 세 가지**

| # | 할 일 | 왜 |
|---|---|---|
| 1 | **API 주소를 환경변수로** (`VITE_API_URL`) | 로컬은 `localhost:3000`, 배포는 Render 주소 |
| 2 | **`fetch` 를 감싸는 함수 하나** | 주소 조립·오류 처리·**나중에 토큰 붙이기**를 한 곳에서 |
| 3 | **★ CORS 해결** | 붙이는 순간 브라우저가 막는다. 서버에 `cors` 한 줄 |

**그다음 화면별로 — 세로 자르기 순서**

| 순서 | 화면 | 붙일 API | 왜 이 순서 |
|---|---|---|---|
| 1 | **2 기기 목록** | `GET /api/devices` | 첫 화면. **여기서 대부분의 문제를 만난다** |
| 2 | 4 기기 상세 | `GET /api/devices/:id/doses` | 주소의 `:id` 를 처음 씀 |
| 3 | 6 복약 기록 | 같은 API, 기간만 다름 | 2번을 거의 복사 |
| 4 | 5 약 설정 | `GET` + `PUT medications` | 첫 **쓰기** 요청 |
| 5 | 7 알림 | `GET notifications` + `POST read` | 배지 숫자 연동 |
| 6 | 3 기기 등록 | `POST /api/user-devices` | **오류 `type` 별 분기**가 여기서 |
| 7 | 공용 팝업 | `POST /api/devices/:id/doses/taken` | 세 화면이 공유 |

- **1번에서 CORS·로딩/오류 상태·응답 모양 불일치가 전부 터진다.** 그 뒤로는 같은 패턴의 반복이라 빨라진다
- **상태 관리(화면별 `useState` vs Zustand)는 1번을 붙여본 뒤 정한다.** 실제로 해보면 뭐가 필요한지 보인다
- 화면에 **로딩·오류 상태는 이미 구현돼 있다**(`DEMO.loading` / `DEMO.error` 자리). 진짜 상태로 갈아끼우면 된다

**진행 기록**

- ✅ **준비 1·2·3 + 순서 1(기기 목록) 완료 (2026-09-14)** — 로컬·배포본 둘 다 진짜 DB 데이터로 카드가 뜬다
  - `App.jsx` 의 껍데기들을 `web/src/screens/` 8개 파일로 분할. `~Screen`(데이터·이동) → `~Page`(그리기만) 구조 유지
  - `web/src/lib/api.js` — `api(path, { method, body })`. 연결 실패/`!res.ok` 를 `Error` 로 던지고 `status`·`code`·`type` 을 붙인다
  - **응답 모양 불일치**: 서버는 `devices` 와 `today_doses` 를 따로 주고 Page 는 `device.today` 를 기대 → **Screen 에서 `map` + `find` 로 합친다**. 오늘 건이 없으면 `null` → StatusChip 이 "기록 없음"
  - **CORS**: 서버 `index.js` 에 `cors({ origin: ['http://localhost:5173', 'https://phill-box.vercel.app'] })`. 에러에 `304` 가 찍혔다 = **요청은 서버에 도착했고 브라우저가 응답을 버린 것**
  - **Vercel `VITE_API_URL`** = Render 주소 (Type **Config**, 끝에 `/` 없음) + **Redeploy**. 넣기 전에 push 하면 배포본은 `undefined/api/devices` 로 요청해 깨진다
- ⏳ **알림 배지는 아직 목업** — `TabLayout.jsx` 가 `mockNotifications` 로 센다(화면엔 2, 서버 `unread_count` 는 1). 순서 5(알림)에서 연결

#### Ⓒ 인증

```
프론트   Supabase Auth 로그인/회원가입 (화면 1)
         토큰을 요청 헤더에 실어 보내기
         토큰 저장 위치 결정            ← 책에서 건너뛴 부분(쿠키/세션)
         로그인 상태 유지 + 보호된 라우트  ← 아래 참고

서버     DEV_USER_ID 하드코딩 제거
         토큰을 검증해 user_id 를 꺼내는 미들웨어
         → §8.5 접근 제어가 진짜로 작동
```

**로그인 상태 유지 + 보호된 라우트** (2026-09-13에 사용자가 발견한 증상)

지금은 로그인이 화면 이동뿐이라 **앱을 껐다 켜면 다시 `/login` 부터** 시작하고, `/devices` 를 주소창에 직접 쳐도 그냥 들어가진다. Ⓒ에서 둘 다 해결한다.

- **상태 유지** — Supabase Auth 라이브러리가 토큰을 `localStorage`(브라우저가 사이트별로 주는 보관함)에 저장한다. 앱 시작 시 세션을 확인해 **있으면 `/devices`, 없으면 `/login`** 으로 보낸다. Access Token(1시간)이 만료돼도 Refresh Token으로 **라이브러리가 뒤에서 갱신**하므로 몇 주간 로그인이 유지된다
- **보호된 라우트** — `path` 없는 부모 라우트(`<Route element={<RequireAuth />}>`)로 로그인 필요 화면들을 감싼다. 세션이 있으면 `<Outlet />`, 없으면 `<Navigate to="/login" replace />`. **Ⓐ-3단계의 `TabLayout` 과 같은 구조**이고, 서버의 `requireMyDevice` 와 같은 발상이다 — 통과 여부를 한 곳에서 판단한다

**서버 쪽은 생각보다 작다.** `DEV_USER_ID` 를 쓰는 자리가 `devices.js`·`notifications.js`·`user_devices.js` 세 곳뿐이라, **미들웨어 하나로 `req.userId` 를 채우면** 끝난다.

**끝났다는 판정:** 폰에서 아이콘 → 로그인 → 내 기기 목록 → 상세 → 수동 체크가 서버에 저장된다.

> **여기까지 되면 시연은 성공이다.** 4주차는 "더 좋게"이지 "되게"가 아니다.

---

### 4주차 — 디바이스 + 리허설

**목표: 노트북이 복약기 흉내를 내고, 폰 화면이 그에 반응한다.**

디바이스는 배포하지 않고 **노트북에서 파이썬으로 로컬 실행**한다(`deployment-managed.md`). 라즈베리파이 실물이 없어도 시연이 된다.

| 순서 | 할 일 | 비고 |
|---|---|---|
| 1 | `GET /api/device/schedule` 받아오기 | 기기 API 키 인증 — **책에 없는 부분** |
| 2 | 복용 시각에 **음성 안내** (TTS) | 시연에서 가장 효과적인 장면 |
| 3 | IR 센서 대신 **키보드 입력**으로 "꺼냄" 흉내 | 하드웨어 없이 시연 가능 |
| 3-1 | **가상 기기 여러 대를 띄울 수 있게** (2026-09-01 결정) | 아래 참고 |
| 4 | `POST /api/device/events` 로 보고 → 폰 화면이 바뀜 | **시연의 클라이맥스** |
| **5** | **리허설 (최소 2일 확보)** | 아래 §4 |

#### 복약기 시뮬레이터 — 가상 기기 여러 대 (2026-09-01 결정)

실물 복약기는 1대뿐이다. **"한 사람이 기기 여러 대" 와 "여러 보호자가 같은 기기"** 를 보여주려면 가상 기기가 필요하다.

**시뮬레이터가 기기를 "만드는" 것이 아니다.** `spec.md` 설계상 기기는 공장 출고 시점에 이미 `devices` 에 있고, 보호자는 **연결**만 한다(§8.2 2번). 그러므로:

```
1. SQL 로 devices 에 한 줄 추가          ← 이것이 "가상 기기 생성"
   (serial · registration_code · device_api_key)
2. 시뮬레이터를 그 device_api_key 로 실행  ← 그 기기 역할을 연기
3. 화면 3에서 serial + code 로 등록       ← 보호자가 집에 설치하는 장면
```

**시뮬레이터에 필요한 것은 서버 주소와 `device_api_key` 뿐이다.** 개발용 seed(`server/sql/dev_seed.sql`)의 `dk_test_0001` 같은 값을 그대로 쓴다 — 지금 만드는 테스트 데이터가 시연에서 그대로 이어진다.

**시연 준비 때 추가로 할 것**
- **보호자 계정 2개**: Authentication 에 사용자를 하나 더 만들고 `user_devices` 에 같은 기기로 줄 추가 → 별명이 서로 다른 것까지 보여줄 수 있다(공동 관리의 핵심 장면). **개발 중에는 `user_id` 하드코딩이 단순하므로 미룬다**
- **`delete from doses;`** 로 복약 건을 비우고 시뮬레이터가 처음부터 만들게 한다. `notifications` 는 `on delete cascade` 로 함께 지워진다

---

#### 라즈베리파이 실물 셋업 (2026-09-04) — 실물이 일찍 도착해 앞당겨 진행

**실물이 왔다고 4주차 계획을 바꾸지 않는다.** 파이썬 코드는 **노트북에서 먼저 만들어 검증한 뒤** 라즈베리파이로 옮긴다. §0 ②와 같은 이유다 — 라즈베리파이에서 바로 시작하면 안 될 때 "내 코드가 틀렸나 / 하드웨어가 문제인가"를 못 가른다. 둘 다 파이썬이라 코드는 거의 그대로 옮겨간다.
**가상 기기 시뮬레이터도 그대로 필요하다.** 실물 1대로는 "기기 여러 대"와 "보호자 공동 관리"를 보여줄 수 없다(위 3-1 항목).

**하드웨어 순서 A~G** — 기준은 *"막혔을 때 잃는 게 적은 순"*. 앞이 안 되면 뒤는 시도조차 못 하도록 배치했다.

| | 단계 | 성격 | 시연 기여 | 상태 |
|---|---|---|---|---|
| A | 부팅 + **SSH 접속** | 리눅스 | — | ✅ 2026-09-04 |
| B | `apt` 시스템 업데이트 | 리눅스 | — | ⏳ |
| C | 파이썬 가상환경(venv) | 파이썬 | — | ⬜ |
| D | **스피커 → TTS 음성 안내** | 리눅스+파이썬 | ★★★ | ⬜ |
| E | **서버와 대화** (`requests`) | 파이썬 | ★★★ | ⬜ |
| F | IR 센서 (GPIO 배선) | 전자 | ★★ | 🔶 예행연습 완료 |
| G | 카메라 + MediaPipe | 전자+파이썬 | ★ | ⬜ (§5 컷 라인) |

**D·E가 진짜 목표다.** 이 둘만 되면 "복약기가 말을 하고, 그 사실이 폰에 뜬다"가 완성된다. F·G는 없어도 되게 이미 설계돼 있다(키보드 입력 대체 / `ir` 폴백).

> **낯선 것은 하드웨어가 아니라 리눅스 터미널이다.** 작업의 대부분은 SSH·패키지 설치·파이썬이고, 진짜 전자 작업은 **IR 센서에 점퍼선 3가닥 꽂기**뿐이다. 납땜도 회로 설계도 없다.

**Imager 설정 (SD카드를 다시 구울 일이 있으면)**

| 항목 | 값 |
|---|---|
| OS | **Raspberry Pi OS (64-bit) 데스크톱판** ← MediaPipe가 64비트만 제공. 데스크톱판인 이유는 G단계에서 카메라 화각을 눈으로 봐야 하기 때문 |
| 호스트 이름 | `pillbox` |
| 사용자 이름 | `academy` |
| Wi-Fi | 집 SSID + 무선 LAN 국가 `KR` |
| 로컬화 | 시간대 `Asia/Seoul` |
| 원격 접근 | **SSH 사용 + 비밀번호 인증** ← 빼먹으면 모니터·키보드가 필요해진다 |
| 라즈베리 파이 커넥트 | **끔.** 집 밖에서 접속할 일이 없고, 켜려면 계정 로그인이 필요하다. 나중에 명령 한 줄로 켤 수 있다 |

**겪은 것 — 다시 만나면**

- **`ssh academy@pillbox.local` 이 WSL에서는 안 된다** (`Could not resolve hostname`). WSL은 윈도우가 만든 가상 네트워크 뒤에 있어 `.local` 이름 방송이 닿지 않는다. **윈도우 PowerShell에서는 되고, IP를 알면 WSL에서도 된다.** IP는 공유기(ipTIME AX6008M) 관리도구 → 고급 설정 → 네트워크 관리에서 확인 — `192.168.0.8`, MAC `88:A2:9E:A8:12:80`
- **★ 시계가 이미지 제작일(6/18)에 멈춰 있다. 미해결.** Pi 5는 시계용 배터리가 없어 켤 때마다 인터넷(NTP)으로 맞춰야 하는데, **`123`번 포트가 막혀 있어 10개 서버 전부 무응답**이다(`Timed out waiting for reply`). 이름 찾기(DNS)는 정상이므로 인터넷 문제가 아니다
  - **그대로 두면 `apt` 가 인증서 오류로 실패한다.** 라즈베리파이가 자기를 6월이라 믿으면 7월 이후 발급된 인증서를 "아직 유효하지 않다"며 거부한다. **오류 메시지는 인증서를 말하는데 진짜 원인은 시계라서 찾기 어렵다**
  - 임시 해결 : `sudo date -s "2026-09-04 15:30:00"` (재부팅하면 다시 틀어진다)
  - **4주차 전에 반드시 잡을 것.** 복약 시각·5분 타이머·`occurred_at`(§3.4)이 전부 시계에 달려 있다. 후보 — 다른 NTP 서버 / 공유기를 시계 서버로 / **HTTP 응답 헤더로 맞추기**(어차피 우리 서버에 주기적으로 요청하므로 자연스럽다) / RTC 배터리 장착
- **Pi 5에는 `fake-hwclock` 이 없다** (`command not found`). 예전 모델용 프로그램이다
- **전원을 뽑기 전에 반드시 `sudo shutdown -h now`.** 그냥 뽑으면 SD카드 파일이 깨져 다시 구워야 할 수 있다. `exit` 는 SSH 연결만 끊을 뿐 기기는 계속 켜져 있다
- 배선은 **반드시 전원을 뽑고** 한다. GPIO는 **3.3V만** 견디므로 **`5V` 핀은 쓰지 않는다** — 이 프로젝트에서 하드웨어를 망가뜨릴 수 있는 거의 유일한 경우다

**부품 검수 결과 (2026-09-04)**

- ✅ **카메라가 USB 웹캠(Logitech C920)이다.** Pi 5는 카메라 커넥터 규격이 바뀌어 예전 카메라 모듈 케이블이 안 들어가는데, USB라 그 문제가 아예 없다. **G단계 위험이 크게 줄었다**
- ✅ IR 센서 `TCRT5000`(SZH-SSBH-003)이 3.3~5V 동작 → GPIO 직결 안전. LED 모듈은 저항 내장이라 저항 키트 불필요
- ✅ 공식 27W 어댑터(5V 5A) · Active Cooler(팬 커넥터 연결 확인) · 모니터 + micro HDMI 케이블
- ⚠️ **Pi 5에는 3.5mm 이어폰 잭이 없다.** 4번 모델까지 있던 것이 빠졌다. 부저(`BZ-HMR`)는 단음만 내므로 **음성 안내를 못 한다** → **USB 스피커가 필요**하다. D단계는 시연 기여가 가장 큰 항목이라 여기가 막히면 아프다
- **아두이노(Mega 2560)는 쓰지 않는 쪽을 권한다.** 부품이 늘면 실패 지점이 늘고 라즈베리파이↔아두이노 통신 코드가 추가된다. IR은 라즈베리파이가 직접 읽으면 된다 — 아두이노에만 있는 것은 아날로그 입력(`AO`)인데, **우리는 "꺼냈나 / 안 꺼냈나" 둘 중 하나면 되므로 `DO`로 충분하다**(§4)

**F단계 예행연습 완료 — IR 센서 → LED (2026-09-04)**

서버·타이머 없이 **"센서가 감지하면 LED가 켜진다"** 만 만들어 본 것. §0 ②의 *"빈 껍데기일 때 뚫는다"* 를 하드웨어에 적용한 것이고, **버려지는 코드가 없다** — `led.on()` 자리에 `requests.post(...)` 가 들어가면 그대로 4주차 F단계다.

| 모듈 | 배선 (물리 핀) |
|---|---|
| IR 센서 | `VCC → 1(3V3)` · `GND → 6` · `DO → 7(GPIO4)` · **`AO` 는 안 씀** |
| LED 모듈 | `VCC → 17(3V3)` · `GND → 9` · `OUT → 11(GPIO17)` |

- **★ IR은 감지되면 `0`, 평소가 `1`이다** (직관과 반대). 감지 거리가 **아주 짧고**(1cm도 멀 수 있다) **흰색이 잘 잡힌다**(검은색은 적외선을 먹는다). 모듈의 하얀 나사로 감도를 조절한다
- **핀 이름이 두 가지다** — 배선은 **물리 번호**(11번), 코드는 **GPIO 번호**(`GPIO17`). 섞으면 반드시 틀린다. `pinout` 명령으로 배치도를 볼 수 있다
- `gpiozero` 는 기본 설치돼 있다. 대화형 창(`python3`)이 핀을 잡고 있으면 스크립트 실행 시 "이미 사용 중" 오류가 나므로 **`exit()` 를 먼저** 한다
- `vim` 은 기본 설치가 아니다 (`sudo apt install vim`)

**빵판(브레드보드)은 아직 필요 없다.** 핀은 `3V3`×2 / `5V`×2 / `GND`×8 / `GPIO`×26 인데, **실제 제품에서 GPIO에 물릴 부품은 IR 센서 하나뿐**이다(카메라·스피커는 USB). 빵판은 *전기 멀티탭* 이라 **3.3V가 필요한 모듈이 3개 이상**이 될 때 전원을 갈라 쓰려고 넣는다. 미리 넣으면 부품이 늘어 실패 지점만 는다(특히 40핀 플랫케이블은 방향을 거꾸로 꽂기 쉽다).

---

## 2. 4대 함정이 나타나는 시점

`CLAUDE.md`에 적힌 "책에 없어서 반드시 막힐 4가지"가 실제로 언제 나오는지.

| 함정 | 언제 | 미리 알아둘 것 |
|---|---|---|
| **JSON API 만들기** | 2주차 | `res.send(HTML)` 대신 `res.json({...})` |
| **CORS** | 3주차 초 | 도메인이 다르면 브라우저가 막는다. `cors` 패키지 한 줄이지만 모르면 몇 시간 잃음 |
| **`.env` 환경변수** | 1주차 | Supabase `service_role` 키를 코드에 박지 않기. `.gitignore` 필수 |
| **디바이스 인증** | 4주차 | 브라우저가 없어 이메일/비번을 못 쓴다. 기기별 API 키 |

**추가로 만날 것:**
- **빌드 오류** (1주차) — 로컬에선 되는데 Vercel에선 실패. 대소문자·미사용 import가 원인인 경우가 많다
- **경로 문제** (3주차) — SPA에서 새로고침하면 404. `vercel.json` rewrite로 해결

---

## 3. 진행 규칙

- **하루의 끝에 반드시 커밋한다.** 되돌릴 지점이 있어야 막혔을 때 과감해질 수 있다
- **막히면 30분 룰** — 30분 넘게 같은 자리면 추측으로 건드리지 말고 원인을 좁히는 질문으로 전환
- **매주 금요일은 통합일** — 새 기능 대신 배포하고 폰에서 확인만 한다
- **`spec.md`를 고쳤으면 그 날 커밋한다** — 명세와 코드가 어긋나는 게 가장 비싼 버그

---

## 4. 리허설 (4주차, 최소 2일)

시연 실패는 대개 코드가 아니라 **환경**에서 난다.

- [ ] **개발용과 배포용이 같은 Supabase 프로젝트를 쓴다.** 로컬에서 `delete` 를 돌리면 시연용 데이터도 함께 사라진다. 시연 데이터를 준비한 뒤에는 로컬에서 데이터를 지우는 SQL을 실행하지 말 것 (무료 플랜 프로젝트 2개 한도 + 관리 부담 때문에 하나로 간다)
- [ ] 시연 장소 Wi-Fi에서 실제로 열리는가 (사내망 차단 확인)
- [ ] 폰 두 대(안드로이드·아이폰)에 **미리 설치**해 둔다 — 관객에게 설치시키지 않는다
- [ ] 노트북 파이썬 스크립트가 **한 번에** 뜨는가 (의존성·경로)
- [ ] Render 무료 플랜은 **15분 무접속 시 잠든다** → 시연 직전에 한 번 깨워둔다 ★
- [ ] **Supabase 무료 플랜은 일주일 무접속 시 프로젝트가 잠긴다(Pause)** → 시연 **전날** 대시보드에서 Resume. 깨우는 데 1~2분 걸리므로 당일에 발견하면 늦다 ★
- [ ] 인터넷이 끊겼을 때의 대비 — **미리 찍어둔 화면 녹화**를 준비
- [ ] 시연 대본 5분: 아이콘 탭 → 기기 목록 → 노트북에서 음성 → 꺼냄 → 폰이 바뀜

> ★ **Render 무료 플랜의 콜드 스타트**가 시연에서 가장 흔한 사고다. 첫 요청이 30초 넘게 걸린다. 시연 시작 5분 전에 아무 API나 한 번 호출해 깨워둘 것.

---

## 5. 컷 라인 — 밀리면 이 순서로 버린다

| 우선 | 항목 | 비고 |
|---|---|---|
| **반드시** | 로그인 / 기기 목록 / 기기 상세 / 수동 체크 | 이게 없으면 시연이 성립 안 함 |
| **반드시** | 노트북 음성 안내 + 이벤트 보고 | 제품의 핵심 서사 |
| 있으면 좋음 | 약 설정 저장(화면 5) | 없으면 DB에 직접 넣어두고 시연 |
| 있으면 좋음 | 알림 목록(화면 7) | 배지만 있어도 그림은 됨 |
| **버린다** | 카메라 판정 | `ir` 폴백이 이미 설계돼 있음(`spec.md` §4) |
| **버린다** | FCM 푸시 | `spec.md` §10. 알림 테이블만 있으면 화면 7은 돈다 |
| **버린다** | 페이지네이션 [더 보기] | 기기가 20대를 넘을 일이 없음 |
| **버린다** | 접근 제어 정교화 | 3주차에 기본형만. 시연에선 계정 하나만 씀 |

**버린 것은 "안 만든 것"이 아니라 "자리를 만들어 둔 것"이다.** 시연에서 "여기는 이렇게 확장됩니다"라고 설명하면 오히려 설계를 보여주는 장면이 된다.

---

## 6. 지금 당장 할 일

1. `git init` + `.gitignore`
2. GitHub 저장소 생성 + 첫 푸시
3. Vercel 연결

**1번부터 시작한다.**
