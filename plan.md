# 4주 계획 — 시연 2026-09-25 기준

> 작성 2026-08-26. 남은 기간 **약 4주**.
> 현재 위치: `spec.md` §9의 **2단계 완료**(화면 7장 퍼블리싱), 3단계 완료(API 명세 확정).
> 이 문서는 "무엇을 언제"만 다룬다. "무엇을 어떻게"는 `spec.md`가 기준이다.

---

## 0. 이 계획을 관통하는 원칙 3개

### ① 가로가 아니라 **세로**로 자른다

```
✗ 가로 자르기 : API 8개 전부 → 화면 연결 전부 → 인증 전부
                 (3주차까지 아무것도 안 돌아간다. 막히면 시연할 게 없다)

✓ 세로 자르기 : [로그인 → 기기 목록] 하나를 끝까지 관통시킨 뒤,
                 그 옆에 [기기 상세]를 붙이고, 또 그 옆에…
                 (1주차부터 "돌아가는 것"이 있고, 매주 그게 두꺼워진다)
```

건물을 지을 때 **1층 전체를 완성하고 2층으로 가는 게 아니라, 계단실 한 칸을 옥상까지 먼저 뚫는** 방식이다. 중간에 시간이 모자라도 "뚫린 만큼은 확실히 동작하는" 상태가 된다.

### ② 파이프라인은 **빈 껍데기일 때** 뚫는다

Vercel·Render·Supabase 연결은 **내용물이 없을 때 미리** 해둔다. 배포는 처음 하면 반드시 막히는데(환경변수·빌드·경로·CORS), 그 문제를 **코드가 단순할 때** 만나야 원인을 좁힐 수 있다.

> 서버 코드 500줄을 짜놓고 첫 배포를 하면, 안 될 때 "내 코드 문제인가 / 배포 설정 문제인가"를 못 가른다. `{ "ok": true }` 한 줄만 올려서 성공시켜 두면, 이후 실패는 **전부 내 코드 문제**로 확정된다.

### ③ **버릴 것을 미리 정한다**

일정이 밀리는 건 정상이다. 밀렸을 때 무엇을 버릴지 **지금** 정해두면 그때 당황하지 않는다. → §5 컷 라인

---

## 1. 주차별 계획

### 0주차 (이번 주 남은 며칠) — 안전벨트

| 할 일 | 담당 |
|---|---|
| `git init` + `.gitignore` (`node_modules`, `.env` 제외) | 직접 타이핑 |
| GitHub 저장소 생성 → 첫 푸시 | 직접 타이핑 |

**끝났다는 판정:** GitHub 웹페이지에서 `web/src/pages/`가 보인다.

> **지금 백업이 하나도 없다.** 화면 7장이 이 폴더에만 있다. 시연 일정과 무관하게 최우선.
> 깃은 Vercel 배포의 **전제 조건**이기도 하다(깃 푸시 → 자동 배포).

---

### 1주차 — 파이프라인 3개 뚫기 + DB

**목표: 폰 홈 화면 아이콘을 눌러 목업 화면이 열린다.**

| # | 할 일 | 담당 | 만나는 함정 |
|---|---|---|---|
| 1 | Vercel 연결 → `web/` 배포 | 함께 | **빌드 오류** |
| 2 | `manifest.json` + 아이콘 2장 (`display: standalone`) | 퍼블 | — |
| 3 | 폰에서 홈 화면에 추가 → 주소창 없이 열리는지 확인 | 직접 | iOS/안드로이드 설치 방법 차이 |
| 4 | Supabase 프로젝트 생성 + 테이블 6개 SQL (`spec.md` §7) | 직접 타이핑 | **환경변수** |
| 5 | `server/` 폴더 + Express 최소 서버(`GET /api/health` 하나) | 직접 타이핑 | — |
| 6 | Render 연결 → 5번 배포 | 함께 | **환경변수** |

**끝났다는 판정 (3개 다 되어야 함):**
- 폰 홈 화면 아이콘 → 주소창 없이 목업 화면이 뜬다
- Supabase SQL Editor에서 `SELECT * FROM devices` 가 에러 없이 돈다 (0행이어도 성공)
- 브라우저에서 Render 주소 + `/api/health` → `{"ok":true}`

> **이 주가 끝나면 이미 시연이 가능하다.** 목업 데이터지만 "폰에 앱이 깔리고 화면이 돈다"까지는 보여줄 수 있다. 이게 **안전망**이다 — 이후 무엇이 실패해도 최소한 이건 남는다.

---

### 2주차 — 서버 API (`spec.md` §9의 5단계)

**목표: Swagger에서 버튼을 눌러 진짜 DB 데이터가 나온다.**

인증 없이. `user_id`는 하드코딩. (`spec.md` §9 — *"자물쇠는 문틀을 다 세운 다음에 단다"*)

만드는 순서는 **세로 자르기** 원칙대로, 화면 2가 필요한 것부터:

| 순서 | API | 왜 이 순서 |
|---|---|---|
| 1 | `GET /api/devices` | 첫 화면. 이거 하나면 화면 2가 산다 |
| 2 | `GET /api/devices/:id/doses` | 화면 4·6 공용이라 **하나로 두 화면** |
| 3 | `POST /api/doses/:id/taken` | 공용 팝업. 쓰기 API를 한 번 경험 |
| 4 | `GET`·`PUT /api/devices/:id/medications` | 화면 5 |
| 5 | `GET /api/notifications` + `POST .../read` | 화면 7 |
| 6 | `POST /api/user-devices` | 화면 3. **409의 `user_id` 조건 주의**(`spec.md` §8.2) |

Swagger(`swagger-jsdoc` + `swagger-ui-express`)는 **API 하나 만들 때마다 그 자리에서** 주석으로 적는다. 몰아서 하면 안 한다.

**끝났다는 판정:** `/api-docs`에서 Try it out → 6개 API가 `spec.md` §8의 응답 모양 그대로 돌려준다.

> **가장 큰 덩어리이자 가장 위험한 주.** 처음 배우는 영역이다. 밀리면 §5 컷 라인을 먼저 본다.

---

### 3주차 — 화면 연결 + 인증 (6·7단계)

**목표: 폰에서 로그인하면 진짜 데이터가 보인다. ← 이게 최소 시연본**

| 할 일 | 담당 | 만나는 함정 |
|---|---|---|
| 라우터 붙이기 (`react-router`) | 직접 | **경로 문제** — Vercel에서 새로고침 시 404 → `vercel.json` rewrite |
| 목업 → `fetch`로 교체 (Zustand) | 직접 | **CORS** — Vercel ↔ Render 첫 통신 |
| Supabase Auth 로그인 | 직접 | 토큰을 어디에 두나 (쿠키 vs 메모리) |
| 접근 제어 (`spec.md` §8.5) | 직접 타이핑 | **★ 이 프로젝트의 심장** — 개념부터 설명 필요 |

**끝났다는 판정:** 폰에서 아이콘 → 로그인 → 내 기기 목록 → 상세 → 수동 체크가 서버에 저장된다.

> **여기까지 되면 시연은 성공이다.** 4주차는 "더 좋게"이지 "되게"가 아니다.

---

### 4주차 — 디바이스 + 리허설

**목표: 노트북이 복약기 흉내를 내고, 폰 화면이 그에 반응한다.**

디바이스는 배포하지 않고 **노트북에서 파이썬으로 로컬 실행**한다(`deployment-managed.md`). 라즈베리파이 실물이 없어도 시연이 된다.

| 순서 | 할 일 | 비고 |
|---|---|---|
| 1 | `GET /api/device/schedule` 받아오기 | 기기 API 키 인증 — **책에 없는 부분** |
| 2 | 복용 시각에 **음성 안내** (TTS) | 시연에서 가장 효과적인 장면 |
| 3 | IR 센서 대신 **키보드 입력**으로 "꺼냄" 흉내 | 하드웨어 없이 시연 가능 |
| 4 | `POST /api/device/events` 로 보고 → 폰 화면이 바뀜 | **시연의 클라이맥스** |
| **5** | **리허설 (최소 2일 확보)** | 아래 §4 |

---

## 2. 4대 함정이 나타나는 시점

`CLAUDE.md`에 적힌 "책에 없어서 반드시 막힐 4가지"가 실제로 언제 나오는지.

| 함정 | 언제 | 미리 알아둘 것 |
|---|---|---|
| **JSON API 만들기** | 2주차 | `res.send(HTML)` 대신 `res.json({...})` |
| **CORS** | 3주차 초 | 도메인이 다르면 브라우저가 막는다. `cors` 패키지 한 줄이지만 모르면 몇 시간 잃음 |
| **`.env` 환경변수** | 1주차 | Supabase `service_role` 키를 코드에 박지 않기. `.gitignore` 필수 |
| **디바이스 인증** | 4주차 | 브라우저가 없어 이메일/비번을 못 쓴다. 기기별 API 키 |

**추가로 만날 것:**
- **빌드 오류** (1주차) — 로컬에선 되는데 Vercel에선 실패. 대소문자·미사용 import가 원인인 경우가 많다
- **경로 문제** (3주차) — SPA에서 새로고침하면 404. `vercel.json` rewrite로 해결

---

## 3. 진행 규칙

- **하루의 끝에 반드시 커밋한다.** 되돌릴 지점이 있어야 막혔을 때 과감해질 수 있다
- **막히면 30분 룰** — 30분 넘게 같은 자리면 추측으로 건드리지 말고 원인을 좁히는 질문으로 전환
- **매주 금요일은 통합일** — 새 기능 대신 배포하고 폰에서 확인만 한다
- **`spec.md`를 고쳤으면 그 날 커밋한다** — 명세와 코드가 어긋나는 게 가장 비싼 버그

---

## 4. 리허설 (4주차, 최소 2일)

시연 실패는 대개 코드가 아니라 **환경**에서 난다.

- [ ] 시연 장소 Wi-Fi에서 실제로 열리는가 (사내망 차단 확인)
- [ ] 폰 두 대(안드로이드·아이폰)에 **미리 설치**해 둔다 — 관객에게 설치시키지 않는다
- [ ] 노트북 파이썬 스크립트가 **한 번에** 뜨는가 (의존성·경로)
- [ ] Render 무료 플랜은 **15분 무접속 시 잠든다** → 시연 직전에 한 번 깨워둔다 ★
- [ ] 인터넷이 끊겼을 때의 대비 — **미리 찍어둔 화면 녹화**를 준비
- [ ] 시연 대본 5분: 아이콘 탭 → 기기 목록 → 노트북에서 음성 → 꺼냄 → 폰이 바뀜

> ★ **Render 무료 플랜의 콜드 스타트**가 시연에서 가장 흔한 사고다. 첫 요청이 30초 넘게 걸린다. 시연 시작 5분 전에 아무 API나 한 번 호출해 깨워둘 것.

---

## 5. 컷 라인 — 밀리면 이 순서로 버린다

| 우선 | 항목 | 비고 |
|---|---|---|
| **반드시** | 로그인 / 기기 목록 / 기기 상세 / 수동 체크 | 이게 없으면 시연이 성립 안 함 |
| **반드시** | 노트북 음성 안내 + 이벤트 보고 | 제품의 핵심 서사 |
| 있으면 좋음 | 약 설정 저장(화면 5) | 없으면 DB에 직접 넣어두고 시연 |
| 있으면 좋음 | 알림 목록(화면 7) | 배지만 있어도 그림은 됨 |
| **버린다** | 카메라 판정 | `ir` 폴백이 이미 설계돼 있음(`spec.md` §4) |
| **버린다** | FCM 푸시 | `spec.md` §10. 알림 테이블만 있으면 화면 7은 돈다 |
| **버린다** | 페이지네이션 [더 보기] | 기기가 20대를 넘을 일이 없음 |
| **버린다** | 접근 제어 정교화 | 3주차에 기본형만. 시연에선 계정 하나만 씀 |

**버린 것은 "안 만든 것"이 아니라 "자리를 만들어 둔 것"이다.** 시연에서 "여기는 이렇게 확장됩니다"라고 설명하면 오히려 설계를 보여주는 장면이 된다.

---

## 6. 지금 당장 할 일

1. `git init` + `.gitignore`
2. GitHub 저장소 생성 + 첫 푸시
3. Vercel 연결

**1번부터 시작한다.**
