# 의존성 — package.json 만 있으면 npm install 로 다시 만들어진다
node_modules/

# 빌드 결과물 — 소스에서 다시 만들어진다
dist/
build/

# ★ 비밀 키. 절대 올리면 안 된다 (Supabase service_role 등)
.env
.env.local
.env.*.local

# 파이썬 (device/ 용, 4주차에 쓴다)
__pycache__/
*.pyc
venv/
.venv/

# Claude Code 개인 설정
.claude/settings.local.json

# OS
Thumbs.db
.DS_Store
